# QbAutopost.Api — Implementation Tracker

Updated: 2026-09-17 · Owner: Braj · Executor: Claude Code
Spec: `spec.md` · Plan: `plan.md`

**How to update (Claude Code does this after every task):**
1. Change the task's Status: `todo` → `doing` → `done` (or `blocked` with a note).
2. Fill *Files* (paths created/changed) and *Verified by* (test names or the command run).
3. Append one line to the Session log.
4. Never delete rows; never mark a **[server]** task `done` — leave it `ready-for-human` and the human flips it.

Status legend: `todo` · `doing` · `done` · `blocked` · `ready-for-human`

## Milestones

| M | Name | Tasks | Done | Status |
|---|---|---|---|---|
| M0 | Bootstrap | 5 | 5 | done (Windows; Linux run pending) |
| M1 | API host, lifecycle, CSV dry run | 7 | 7 | done (Windows; Linux run pending) |
| M2 | Hermes client, T1, G2 | 5 | 5 | done (Windows; Linux run pending) |
| M3 | XLSX, PDF, T2, G1 | 5 | 5 | done (Windows; Linux run pending) |
| M4 | Invoices T3 + matcher | 4 | 4 | done (Windows; Linux run pending) |
| M5 | Tiers 3–4, G3 | 4 | 4 | done (Windows; Linux run pending) |
| M6 | QuickBooks gateway, post, undo | 9 | 5 | doing |
| M7 | Rules, logging, hardening | 5 | 0 | todo |
| M8 | Deploy, shadow, go-live | 4 | 0 | todo |

## M0 — Bootstrap

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-001 | Solution + 3 src projects + 2 test projects, Directory.Build.props, .editorconfig, .gitignore | §4, plan §1 | `QbAutopost.sln`, `global.json`, `Directory.Build.props`, `.editorconfig`, `.gitignore`, `.gitattributes`, `src/*/*.csproj`, `src/QbAutopost.Api/Program.cs`, `tests/*/*.csproj`, `tests/QbAutopost.Api.Tests/HostSmokeTests.cs` | `dotnet build -warnaserror` (0 warnings); `HostSmokeTests` | done | CA1416 suppressed only in QbAutopost.QuickBooks. `global.json` pins SDK 8.0.x (SDK 10 also installed). Test packages added: Microsoft.NET.Test.Sdk 17.14.1, xunit 2.9.3, xunit.runner.visualstudio 3.1.5 (see Q-8) |
| T-002 | Port POC into Core (Models, Text, Rules, Fuzzy, Mapper, Gates, QbXmlBuilder/Parser, Ledger, BatchEnterSheet, CsvStatementParser, RegexSpecParser) | §7, §9, §11, §13 | `src/QbAutopost.Core/{Models,Text,Mapping,Gates,QbXml,Store,Output,Extract}/*.cs` | build green; covered by T-004 tests | done | **POC unavailable → written from spec (ADR-0007).** Gaps marked `SPEC-GAP T-002`, listed in Questions Q-1…Q-9. Mapper covers tiers 1–2 only; lines needing tiers 3–4 are held `no-account-rule` until T-502. `CsvStatementParser` delegates to `StatementGridParser` (shared with T-301 XLSX) |
| T-003 | Fixtures: samples/jobs/2026-08-tropicana, tests/fixtures/hermes/*.json, tests/fixtures/qbxml/*.golden.xml | §5, §9 | `samples/jobs/2026-08-tropicana/{requirement.txt,statements/*.csv,invoices/home-depot-88213.txt}`, `samples/rules.json`, `tests/fixtures/hermes/{spec,statement,invoice,account}.json`, `tests/fixtures/qbxml/{check,creditcard,deposit}.golden.xml`, `tests/fixtures/qbxml/add-response.xml`, `tests/fixtures/statements/*.csv` | files present; used by T-004 tests | done | All data synthetic (no POC samples). Bank CSV 7 rows (newest-first, balances reconcile 13195.87 → 10230.45), card CSV 4 rows. Invoice is a `.txt` stand-in for a text PDF (plan allows). Golden requestIDs computed independently with PowerShell SHA-256, not by the code under test |
| T-004 | First tests: CSV parser, fingerprint, G1 both orientations, FR-6 routing table, qbXML golden | §8 FR-3/4/6/9 | `tests/QbAutopost.Core.Tests/{TestSupport,Text,Extract,Gates,Mapping,QbXml,Store,Output,Architecture}/*.cs`, `SampleJobTests.cs`; fix in `src/QbAutopost.Core/Text/TextNormalizer.cs` | `dotnet test` → Core 91 passed, Api 1 passed (Windows, SDK 8.0.420) | done | Covers: CSV 7/4 rows (FR-3 AC), fingerprint vs independent SHA-256, G1 file/reverse order + failure modes, all 7 FR-6 rows + holds, tier 2 history, qbXML 3 golden files, response parser, ledger atomic save, sheets, RegexSpecParser FR-2 AC, Core has no COM tokens. Tests found one bug: apostrophes in names ("Joe's") broke fuzzy matching — fixed. Tests written in the same session as T-002 rather than strictly first. Linux run still to be done (no Linux box here) |
| T-005 | scripts/build.ps1, test.ps1, run-api.ps1; CLAUDE.md at repo root | plan §4 | `scripts/{build,test,run-api}.ps1`, `CLAUDE.md` | build.ps1 → 0 errors; test.ps1 → 92 passed; run-api.ps1 → "Now listening on http://127.0.0.1:5080" | done | CLAUDE.md copied, not moved (see Decisions) |

## M1 — API host, lifecycle, CSV dry run

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-101 | FolderReader + folder validation F1–F5, JobInput | §5 | `src/QbAutopost.Core/Jobs/{FolderReader,JobInput,InvalidJobFolderException}.cs`, `HoldReasons.SubfolderIgnored`; `tests/QbAutopost.Core.Tests/Jobs/FolderReaderTests.cs`, `TestSupport/TempJobFolder.cs` | `FolderReaderTests` (22); `dotnet test` → Core 113, Api 1 | done | F1 validate, F2 unreadable, F4 top-level only / `output/` never listed, F5 file-name last-four (content fallback stays in the parsers). F3 is checked by the endpoint (T-104). Job-id charset and subfolder reporting are SPEC-GAPs (Q-12, Q-13) |
| T-102 | JobRecord, JobStore (memory + status.json), JobQueue (Channel), JobWorker | §6 | `src/QbAutopost.Core/Jobs/{JobStatus,JobRecord,IJobStore}.cs`, `Core/Abstractions/IClock.cs`, `Core/Text/JsonOptions.cs` (camelCase enums); `src/QbAutopost.Api/Configuration/AppSettings.cs`, `Api/Jobs/{JobStore,JobQueue,IJobProcessor,JobWorker}.cs`; `tests/QbAutopost.Api.Tests/{Jobs/JobStoreTests,Jobs/JobWorkerTests,TestSupport/*}.cs`, `Core.Tests/Jobs/JobRecordTests.cs`, `Core.Tests/Text/JsonOptionsTests.cs` | `JobStoreTests` (7), `JobWorkerTests` (4), `JobRecordTests`, `JsonOptionsTests`; `dotnet test` → Core 139, Api 12 | done | One job at a time (single reader, asserted). State machine enforced by `JobRecord.MoveTo`. status.json is written atomically, before memory. Worker fail-safe: if the processor crashes, the job goes to `failed` (from analysing) or `partial (interrupted …)` (from posting); cancellation at shutdown leaves the status for startup recovery. Job index file is a SPEC-GAP (Q-14). Worker DI registration is in T-104 |
| T-103 | Pipeline orchestrator (RunAnalysisAsync / RunPostAsync) with RegexSpecParser stand-in | §8 | `src/QbAutopost.Core/Pipeline/{JobPipeline,PipelineOptions,ISpecReader,AnalysisResult}.cs`, `Core/Gates/{SpecGate,PostVerifier}.cs`, `Core/Abstractions/{IQbGateway,IHermesClient}.cs`, `Core/Store/QbListsStore.cs`, `HoldReasons` (pipeline codes); `src/QbAutopost.Api/Jobs/JobRunner.cs`; tests `Core.Tests/Pipeline/JobPipelineTests.cs`, `Core.Tests/Gates/{SpecGateTests,PostVerifierTests}.cs` | `JobPipelineTests` (17), `SpecGateTests` (10), `PostVerifierTests` (7); Api `PostingApiTests` | done | Analysis = FR-1…FR-9 (CSV only; xlsx/pdf held `extractor-not-available` until M3) + G1 per statement + **G2 (logic landed here, see T-203)** + ledger G4 (`already-posted`). Post = re-analysis → one qbXML call → G5 → ledger → posted/partial. Gateway exception → all lines held, batch recorded with 0 posted (F3 then refuses a re-run: check QuickBooks first); `QuickBooksUnavailableException` (nothing sent) → no ledger record. M6 adds live G4, retry, busy timeout, backup guard and the COM gateway. SPEC-GAPs Q-15…Q-18 |
| T-104 | Endpoints POST /jobs, GET /jobs/{id}, GET /jobs, POST /jobs/{id}/post; problem details; API key; Kestrel bind | §6 | `src/QbAutopost.Api/{Program.cs,appsettings.json,appsettings.Development.json}`, `Api/Endpoints/{JobEndpoints,JobView}.cs`, `Api/Jobs/JobAdmission.cs`, `Api/Security/ApiKeyMiddleware.cs`, `Api/QuickBooks/UnconfiguredQbGateway.cs`, `Api/Configuration/AppSettings.cs` (`ResolvePaths`); `.gitignore` (`src/QbAutopost.Api/data/`) | `JobsApiTests` (19), `AuthTests` (8); manual run below | done | RFC 7807 for 400/401/404/409 (400 lists `errors[]`). Key compared via SHA-256 + fixed-time compare; empty key → host refuses to start; `change-me` refused outside Development (Q-19). Kestrel binds `Api:Bind`. Env prefix `QBAUTOPOST__`. Admission (create/post) is serialised; an active job → 409, ledger job → 409 unless `force`. `POST /jobs/{id}/post` moves the job to `posting` (with batch id) before queueing. `GET /jobs?status=` implemented here (T-704). Default gateway is `UnconfiguredQbGateway` until T-601. Manual (Windows, Development): sample → 202 → `ready` with 8/2/1 and totals 4942.64; post → `partial` "nothing posted: QuickBooks unavailable"; second post → 409; no key → 401; log has no secrets |
| T-105 | Writers: analysis.json, result.json, status.json, paste-ready CSVs | §10 | `src/QbAutopost.Core/Output/{JobOutputWriter,OutputDocuments}.cs`, `Core/Store/AtomicFile.cs` (shared read + retry); tests `Core.Tests/Output/{ResultDocumentTests,AnalysisGoldenTests}.cs`, `Core.Tests/Store/AtomicFileTests.cs`, `tests/fixtures/output/sample-analysis.golden.json` | `AnalysisGoldenTests` (golden reviewed by hand against the FR-6 table), `ResultDocumentTests` (4), `AtomicFileTests` (3) | done | Writes spec.json, statements/&lt;file&gt;.rows.json, analysis.json, request.qbxml + 3 sheets (also in dry run), response.qbxml, result.json. result.json adds `company`, `error`, `counts.posted`, `totals.posted` to the spec shape (Q-20). Found while testing: on Windows a polling `GET /jobs/{id}` and the atomic replace block each other (500 / failed rename) → readers share delete access and both sides retry with backoff (≈1.5 s) |
| T-106 | Startup recovery of interrupted jobs | §6 | `src/QbAutopost.Api/Jobs/StartupRecovery.cs`, `Program.cs` | `RecoveryTests` (6) | done | Runs after `Build()`, before the worker starts. analysing → `failed (interrupted)`; posting → `partial (interrupted — run duplicates before re-post)`; queued → `failed` (Q-14). result.json keeps the last completed run; status.json is the authority |
| T-107 | Api tests: happy path to ready; 400/404/409; auth; recovery | §15 | `tests/QbAutopost.Api.Tests/TestSupport/{ApiFactory,FakeQbGateway,FakeHermesClient,TempDir,Fixtures,FixedClock}.cs`, `Api/{JobsApiTests,PostingApiTests,AuthTests,RecoveryTests}.cs`; removed `HostSmokeTests.cs` (covered by `AuthTests`) | `dotnet test` → Core 181, Api 54, 8 consecutive green runs | done | Every data file lives in a per-test temp folder; no real QuickBooks/Hermes. `FakeQbGateway` records requests, answers with sequential `FAKE-n` TxnIDs and echoed amounts, can refuse line N, throw, or hang. `FakeHermesClient` serves `tests/fixtures/hermes/*.json` (not called until M2). Posting covered: full post → partial (2 held), ledger entries, forced re-run skips posted lines and uses batch #2, dryRun=false, refused line, unavailable vs failed gateway, double post → 409 |

## M2 — Hermes client, T1, G2

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-201 | HermesClient: OpenAI-compatible call, auth, timeout, fence strip, retry-with-error, audit copies | §9 | `src/QbAutopost.Core/Abstractions/IHermesClient.cs` (`HermesRequest`, `HermesException`, `HermesValidationException`, `HermesUnavailableException`), `src/QbAutopost.Core/Hermes/{HermesClient,HermesOptions,HermesAudit,JsonReply}.cs`, `src/QbAutopost.Api/Program.cs` (typed `HttpClient`, `HermesOptions`); `tests/QbAutopost.Core.Tests/Hermes/{HermesClientTests,JsonReplyTests}.cs`, `TestSupport/StubHttpHandler.cs`; `tests/QbAutopost.Api.Tests/TestSupport/FakeHermesClient.cs` | `HermesClientTests` (19), `JsonReplyTests` (16); `dotnet test` → Core 216, Api 54 | done | Interface now `CompleteJsonAsync<T>(HermesRequest, ct)` (spec §9 signature: task, system prompt, user content, schema hint + audit folder). Invalid answer (prose, bad JSON, empty, `Validate()` errors) → one retry with the errors appended → `HermesValidationException`. Non-2xx, timeout, connection error, malformed envelope → `HermesUnavailableException`, no retry (Q-21). Audit: `output/hermes/<task>-<n>.request/response.json`, one pair per HTTP attempt; key only in the header, and scrubbed from audit text when ≥ 8 chars. Timeout is per call (`HttpClient.Timeout` infinite). Empty key → no `Authorization` header (Q-21). Host DI wiring is exercised by `HealthApiTests` (T-204) |
| T-202 | Prompt spec.md + T1 JobSpec + validation + regex fallback | §9.1, FR-2 | `src/QbAutopost.Core/Hermes/{prompts/spec.md,PromptTemplate,PromptLibrary,SpecAnswer}.cs`, `src/QbAutopost.Core/Pipeline/HermesSpecReader.cs`, `Pipeline/ISpecReader.cs` (`SpecReadResult.Note`), `Output/{OutputDocuments,JobOutputWriter}.cs` (`spec.json.note`), `QbAutopost.Core.csproj` (prompts copied to `<app>/Hermes/prompts`), `src/QbAutopost.Api/Program.cs` (`PromptLibrary` loaded before start, `HermesSpecReader` replaces `RegexSpecReader`); tests `Core.Tests/Hermes/{SpecAnswerTests,PromptLibraryTests}.cs`, `Core.Tests/Pipeline/HermesSpecReaderTests.cs`; `Api.Tests/TestSupport/FakeHermesClient.cs` (`Respond`, validates answers), `Api.Tests/Api/JobsApiTests.cs` (3 tests script their T1 answer) | `SpecAnswerTests` (26), `PromptLibraryTests` (6), `HermesSpecReaderTests` (6, two through the real client + stub HTTP); `dotnet test` → Core 254, Api 54; prompt file present in Api and test output folders | done | `CreditCard` → `CcCharge` + `CcCredit`; kinds matched ignoring case, only `Check`/`CreditCard`/`Deposit` accepted; `kinds`, `bankLast4`, `cardLast4` must be present (may be empty); last-four = exactly 4 ASCII digits; company `Company`/blank → null. Two invalid answers → `RegexSpecParser`, `source = regex-fallback`, `note` = the validation errors. Hermes unreachable → job `failed` (Q-22). Fixture answer and regex parse agree on the sample (FR-2 AC). Unknown `{{placeholder}}` or missing `spec.md` → error at startup |
| T-203 | Gate G2 → failed with explanation | FR-2 | `tests/QbAutopost.Api.Tests/Api/SpecGateApiTests.cs`, `tests/fixtures/output/sample-spec.golden.json`; gate code unchanged (`SpecGate`, T-103), `spec.json.note` added in T-202 | `SpecGateApiTests` (9); `dotnet test` → Core 254, Api 63 | done | Through the Hermes path: sample `spec.json` equals the golden file (reviewed by hand against the FR-2 AC: source `hermes`, kinds check/ccCharge/ccCredit/deposit, bank 4521, card 7788, gate ok). G2 negatives: stated account without a statement, no kinds, statement account neither stated nor in rules.json, another company → `failed`, error starts `requirement check failed (G2)`, `spec.json.gate.errors` lists the reasons, no analysis.json / request.qbxml. Placeholder company → configured name. Invalid answer → `regex-fallback` + `note` (ready on the sample; failed when the regex finds nothing). Hermes unreachable → failed, no fallback (Q-22) |
| T-204 | GET /health/hermes | FR-16 | `src/QbAutopost.Core/Abstractions/IHermesClient.cs` (`PingAsync`, `HermesPing`), `Core/Hermes/HermesClient.cs` (`PingAsync`; shared `PostAsync` with timeout), `src/QbAutopost.Api/Endpoints/HealthEndpoints.cs`, `Program.cs`; tests `Core.Tests/Hermes/HermesClientTests.cs` (+9), `Api.Tests/Api/HealthApiTests.cs`, fakes (`FakeHermesClient.Ping`) | `HermesClientTests` ping cases, `HealthApiTests` (4, two through the host's real `HermesClient` with a stub handler); `dotnet test` → Core 263, Api 67 | done | One completion with `max_tokens: 5`, no audit copy; ok iff HTTP 200 with non-empty content. Body `{ ok, model, latencyMs, message }` — `message` (null when ok) is an addition to spec §6 so an operator sees why (e.g. `HTTP 500`, `timed out after 120 s`, `empty completion`). 503 when not ok; no API key (middleware exempts `/health`). The real-client tests also prove the DI wiring (config → `HermesOptions`, bearer key, base URL) and that the key does not appear in the 503 body |
| T-205 | Tests: fixtures, retry, fallback, G2 negatives | §15 | `tests/QbAutopost.Api.Tests/TestSupport/ApiFactory.cs` (`Hermes:BaseUrl` = `hermes.invalid`), `Api.Tests/Api/HealthApiTests.cs` (guard test); coverage landed with T-201…T-204 | `dotnet test` → Core 263, Api 68, 6 consecutive green runs; manual run below | done | Fixtures: `FakeHermesClient` serves `tests/fixtures/hermes/*.json` (and validates). Malformed-then-valid: `HermesClientTests` (prose, bad JSON, empty, invalid → retry), `HermesSpecReaderTests.Should_ReturnHermesSpec_When_RealClientSucceedsOnRetry`. Fallback: `HermesSpecReaderTests`, `SpecGateApiTests` (ready and failed variants). G2 negatives: `SpecGateTests` (Core) + `SpecGateApiTests` (4 via Hermes). M2 "done when": `spec.json` golden ✓, health 503 with the fake failing ✓. Rule 1 guard: the test host points Hermes at `.invalid`. Manual (Windows, Development, no Hermes running): `/health/hermes` → 503 "actively refused (127.0.0.1:8642)" in ~2 s; sample job → `failed` "analysis failed: Hermes Spec call failed: …" with `output/hermes/spec-1.*.json` written, no key in files or log |

## M3 — XLSX, PDF, T2, G1

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-301 | XlsxStatementParser (ClosedXML → layout logic) | FR-3 | `src/QbAutopost.Core/Extract/{XlsxGrid,XlsxStatementParser}.cs`, `Extract/StatementGridParser.cs` (`acceptIsoDates`), `Models/HoldReasons.cs` (`unreadable-statement`), `Pipeline/JobPipeline.cs` (`.xlsx` routed; only `.pdf` still `extractor-not-available`), `QbAutopost.Core.csproj` (ClosedXML 0.105.1); tests `Core.Tests/Extract/XlsxStatementParserTests.cs`, `TestSupport/XlsxBuilder.cs`, `Extract/CsvStatementParserTests.cs` (+1), `Pipeline/JobPipelineTests.cs` (+2, pdf hold); fixture `tests/fixtures/statements/chase-checking-4521.xlsx` | `XlsxStatementParserTests` (17), `JobPipelineTests.Should_PostSameLines_When_BankStatementIsAWorkbook`, `…_When_WorkbookIsCorrupt`; `dotnet test` → Core 283, Api 68 | done | First worksheet only, blank rows dropped, first non-blank row = header, same layouts as CSV. Cells read without display formats: numbers → invariant digits (double → decimal keeps 15 digits, so `0.1+0.2` → 0.3; > 2 decimals still a row error), real date cells → `yyyy-MM-dd` (time dropped), booleans/errors → text that fails parsing, formulas → the result saved in the file (never recalculated; none saved → blank → statement held). **Date decision (Q-1 kept, Q-23):** text dates must match the layout format, no invariant fallback; the XLSX parser also accepts exact `yyyy-MM-dd`. A workbook ClosedXML cannot open → `unreadable-statement`. Fixture generated by a throw-away ClosedXML program from the sample bank CSV (synthetic). ClosedXML never writes formula results, so `XlsxBuilder` adds `<v>` to the sheet XML to imitate Excel |
| T-302 | PdfText (PdfPig), scanned detection, Ocr wrapper behind Ocr.Enabled | FR-3 | `src/QbAutopost.Core/Abstractions/IOcr.cs` (`IOcr`, `DisabledOcr`), `Core/Extract/PdfText.cs` (`PdfText`, `PdfTextResult`, `PdfPageText`), `Core/Pipeline/JobPipeline.cs` (new `IOcr` constructor argument; statements read async; PDFs go through `PdfText`), `QbAutopost.Core.csproj` (PdfPig 0.1.16); `src/QbAutopost.Api/Ocr/TesseractOcr.cs`, `Api/Program.cs` (`IOcr` by `Ocr:Enabled`, resolved at startup), `Api/Configuration/AppSettings.cs` (relative `TessDataPath` resolved), `QbAutopost.Api.csproj` (Tesseract 5.2.0); tests `Core.Tests/Extract/PdfTextTests.cs`, `TestSupport/{PdfBuilder,TestImage,FakeOcr}.cs`, `Pipeline/JobPipelineTests.cs` (+3, pdf test now uses a real PDF), `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor), `Api.Tests/Api/OcrApiTests.cs` | `PdfTextTests` (12), `JobPipelineTests` PDF cases (4), `OcrApiTests` (3); `dotnet test` → Core 298, Api 71. Manual (Windows, scratch program, tessdata_fast `eng.traineddata`, not committed): real `TesseractOcr` read "08/15/2026 ACH DEBIT UNKNOWN PLUMBER SVC -3199.70" exactly, both from a PNG and from a one-image PDF through `PdfText` | done | Page text via `ContentOrderTextExtractor`; pages joined with `\n\f\n`. Scanned = fewer than 40 **non-whitespace** characters (Q-24). OCR disabled + any scanned page → whole file `scanned-pdf-ocr-disabled` (error names the pages). OCR enabled → each image on a scanned page is OCR'd (JPEG streams passed as-is, other images re-encoded to PNG by PdfPig; an image neither can give → `unreadable-statement`); OCR text is appended to the page's own text. Scanned page with no images → its short text is kept (Q-24). Damaged/encrypted/non-PDF file or OCR exception → `unreadable-statement`; cancellation propagates. Readable PDFs are still held `extractor-not-available` until T-303 (T2). `Ocr:Enabled` with a missing or empty `TessDataPath` stops the host at startup. The Tesseract package bundles Windows x86/x64 native libraries only; Linux needs system Tesseract 5 + Leptonica (OCR is off by default, spec Q3). Only `eng` is loaded |
| T-303 | StatementLlmExtractor T2: prompt, chunking, merge, validation | §9.2 | `src/QbAutopost.Core/Hermes/{prompts/statement.md,StatementAnswer}.cs`, `Core/Extract/StatementLlmExtractor.cs` (`StatementChunk`), `Core/Extract/StatementParseResult.cs` (`Totals`, `StatementTotals`), `Core/Models/HoldReasons.cs` (`hermes-failed`, `extraction-conflict`), `src/QbAutopost.Api/Program.cs` (`statement.md` required at startup); tests `Core.Tests/Hermes/StatementAnswerTests.cs`, `Core.Tests/Extract/StatementLlmExtractorTests.cs`, `TestSupport/ScriptedHermes.cs` (moved out of `HermesSpecReaderTests`); fixture `tests/fixtures/statements/chase-checking-4521.pdf.txt` | `StatementAnswerTests` (27), `StatementLlmExtractorTests` (17, one through the real `HermesClient` retry); `dotnet test` → Core 342, Api 71 | done | Fixture text + `hermes/statement.json` give the same rows and fingerprints as the sample bank CSV (FR-3 AC, T-305). Validation: kind `bank`/`card` and direction `debit`/`credit` (any case), ISO dates (real dates), amounts present, > 0 and ≤ 2 decimals, balances ≤ 2 decimals, `accountLast4` 4 digits or null, period start ≤ end, count ≥ 0, `rows` required (may be empty), `checkNo` a string. Chunking: whole text ≤ 60 000 chars → one request; otherwise consecutive page groups each ≤ 60 000; a page over the limit goes alone, never cut. Each request says "part i of n (pages a-b)". Merge: rows joined in part order, `lineNo` 1…n; kind, last-four, period, balances and count must agree across parts (null = not shown) else `extraction-conflict` (Q-25). File-name last-four wins; a different last-four in the text → `conflicting-last4`; none anywhere → `unknown-account`. Any `HermesException` → `hermes-failed`, remaining parts not sent; cancellation propagates. **Not wired into the pipeline yet** — PDFs stay `extractor-not-available` until T-304 adds the T2 G1 checks, so no T2 row can post unchecked |
| T-304 | G1 extended + rows.json writer | FR-4 | `src/QbAutopost.Core/Gates/ReconcileGate.cs` (`CheckExtraction`, partial running-balance check), `Core/Pipeline/StatementReader.cs` (new: csv/xlsx/pdf → rows), `Core/Pipeline/JobPipeline.cs` (takes `StatementReader` instead of `IOcr`; T2 statements checked with `CheckExtraction`), `Core/Pipeline/AnalysisResult.cs` + `Core/Output/OutputDocuments.cs` (`totals` on the statement summary and `rows.json`), `src/QbAutopost.Api/Program.cs` (`StatementLlmExtractor`, `StatementReader`); tests `Core.Tests/Gates/ReconcileGateExtractionTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (PDF cases rewritten, +5), `TestSupport/TestStatementReader.cs`, `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor), `Api.Tests/Api/PdfStatementApiTests.cs` | `ReconcileGateExtractionTests` (18), `JobPipelineTests` PDF cases, `PdfStatementApiTests` (2, through the host with `FakeHermesClient`); `dotnet test` → Core 365, Api 73. No manual run: no Hermes on the dev box, so a real job fails at T1 (Q-22) before reaching T2 | done | PDFs now post: PdfText → T2 → G1 → mapping. With the bank CSV replaced by a text PDF of the same statement the sample job gives the same 8 postable lines (same requestIDs, accounts, payees) and 8/2/1 through the API. T2 G1 = `\|opening + Σeffects − closing\| ≤ 0.01`; `rows == transactionCount` when printed; every date inside `[periodStart, periodEnd]` (each bound checked when given); printed running balances consistent in file or reverse order (rows without a balance are summed into the next printed one). All failures are listed together. **Card sign reversed** (charges raise the balance), **no opening/closing → `reconcile-failed` "not-verifiable"**, verified = false (Q-26). `rows.json` carries `layout: "hermes-t2"`, `totals` and `reconcile {ok, verified, message}`; T2 holds (`hermes-failed`, `extraction-conflict`, `conflicting-last4`) and a kind that contradicts the requirement hold only that statement. Hermes audit copies go to `output/hermes/statement-<n>.*`. Open: re-analysis at post time calls T2 again (Q-27) |
| T-305 | Tests: fixture text → rows; corrupted → held; XLSX detection | §15 | tests `Core.Tests/Extract/{StatementGridParserTests,PdfFixtureTests}.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+2 card PDF cases; PDF cases use the committed fixtures), `Api.Tests/Api/PdfStatementApiTests.cs` (committed fixture); fixtures `tests/fixtures/statements/{chase-checking-4521,chase-card-7788}.pdf` (rendered by a throw-away PdfPig program from the matching `.pdf.txt`), `chase-card-7788.pdf.txt`, `tests/fixtures/hermes/statement-card-7788.json`; fix `src/QbAutopost.Core/Extract/StatementGridParser.cs` (account column vs file name); `.gitattributes` (`*.pdf *.xlsx *.png *.jpg binary`) | `StatementGridParserTests` (13), `PdfFixtureTests` (2); `dotnet test` → Core 382, Api 73 | done | Gap check against plan T-305 / spec §15. Fixture PDF text → expected rows: `StatementLlmExtractorTests` (text + `statement.json` = CSV rows and fingerprints) and `PdfFixtureTests` (committed PDFs read back line for line). With **both** sample statements as PDFs the pipeline gives the same lines, decisions and reasons as the CSVs; the card PDF is verified with the card sign and held when only the bank sign would reconcile. Chunk merge and conflicts: `StatementLlmExtractorTests`. Every G1 failure mode: `ReconcileGateTests` (CSV chain) + `ReconcileGateExtractionTests` (T2). Corrupt → held: xlsx, pdf, T2 answer, Hermes down. Layout detection (CSV/XLSX shared): matched layout, case/space-insensitive header, ambiguous → `ambiguous-csv-layout` (was untested), empty `HeaderContains`, missing layout column, empty grid, `PositiveIsDebit`, debit/credit pair rules. **Behaviour change:** a last-four in the account column that contradicts the file name now holds the statement `conflicting-last4` (it was silently ignored — the file name won); same rule as T2 (Q-25). The fix and its tests were written together. `.gitattributes` now marks binaries: with `text=auto` git could have treated PdfPig's mostly-text PDFs as text and changed their line endings |

## M4 — Invoices

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-401 | InvoiceExtractor T3 + validation; images via Ocr or unreadable | §9.3 | `src/QbAutopost.Core/Hermes/{InvoiceAnswer.cs,prompts/invoice.md}`, `Core/Extract/InvoiceExtractor.cs` (`InvoiceExtractor`, `InvoiceReadResult`), `Core/Models/HoldReasons.cs` (`unreadable`), `src/QbAutopost.Api/Program.cs` (`invoice.md` required at startup, `InvoiceExtractor` registered); tests `Core.Tests/Hermes/InvoiceAnswerTests.cs`, `Core.Tests/Extract/InvoiceExtractorTests.cs`, `Extract/PdfFixtureTests.cs` (+invoice); fixtures `tests/fixtures/invoices/home-depot-88213.pdf(.txt)` (rendered by a throw-away PdfPig program) | `InvoiceAnswerTests` (20), `InvoiceExtractorTests` (21), `PdfFixtureTests` (+1); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 424, Api 73 | done | `ReadAsync(file, company, auditDir, ct)`: `.pdf` → `PdfText` (scanned pages → OCR; OCR off → `scanned-pdf-ocr-disabled`; unopenable → `unreadable`); `.png/.jpg` → `IOcr` when enabled, else `unreadable`; OCR error, no text, or text > 60 000 chars → `unreadable` (no chunking); other extension → `unsupported-extension`; any `HermesException` → `hermes-failed`; cancellation propagates. One request per invoice; the user message names the file and, when known, "Our company" so the model can tell vendor from customer. Validation: party non-blank, role vendor/customer (any case), number null or non-blank, date ISO or null, total present, > 0, ≤ 2 decimals; text trimmed, blank hint → null. Invoices are not read by the pipeline yet (T-402); the sample's `.txt` stand-in is unchanged (Q-28) |
| T-402 | InvoiceMatcher + invoices/<file>.json + unmatchedInvoices | FR-5 | `src/QbAutopost.Core/Mapping/InvoiceMatcher.cs` (`InvoiceMatcher`, `InvoiceMatch`), `Core/Pipeline/AnalysisResult.cs` (`InvoiceSummary`, `AnalysisResult.Invoices`), `Core/Pipeline/JobPipeline.cs` (new `InvoiceExtractor` argument; invoices read after G2), `Core/Output/JobOutputWriter.cs` (`WriteInvoice`), `Core/Output/OutputDocuments.cs` (`unmatchedInvoices` = objects), `Core/Models/HoldReasons.cs` (`no-matching-line`, `no-invoice-date`); sample `samples/jobs/2026-08-tropicana/invoices/home-depot-88213.pdf` replaces the `.txt` stand-in; tests `Core.Tests/Mapping/InvoiceMatcherTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+10, T3 scripted), `TestSupport/TestInvoiceExtractor.cs`, `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor), `Api.Tests/Api/{JobsApiTests,PdfStatementApiTests,SpecGateApiTests}.cs` (the sample now makes a T3 call; no `.txt` in `unreadable`) | `InvoiceMatcherTests` (24), `JobPipelineTests` invoice cases (10); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 458, Api 73 | done | Matching (FR-5): candidates `\|amount − total\| ≤ 0.005` and `\|days\| ≤ InvoiceMatchDays` (both bounds inclusive); one candidate → match whatever its direction or text; several → same direction first (vendor↔debit, customer↔credit; none of that direction → all stay), then the single highest `Fuzzy.MatchScore(description, party)` ≥ `FuzzyThreshold`; a tie or no score ≥ threshold → `ambiguous`. Only lines of statements that passed G1 are candidates (all of them, including lines later skipped or held). Sample: Home Depot invoice (08-21) matches the 184.32 line (08-22); decisions unchanged (8/2/1). `output/invoices/<file>.json` = `{ file, facts (with matchedRequestId), reason, note, errors, candidates }`, written for every invoice file; `result.json.unmatchedInvoices[]` = the same objects for every invoice not matched, unreadable ones included. Invoices are read only when G2 passes; a failed invoice never fails or holds anything. SPEC-GAPs in Q-29. Posting re-analysis calls T3 again (Q-27) |
| T-403 | MappedTxn.InvoiceRef + payee supply | FR-5/6 | `src/QbAutopost.Core/Mapping/Mapper.cs` (optional `invoices` argument, `ResolvePayee`), `Core/Pipeline/JobPipeline.cs` (matched invoices passed to the mapper); tests `Core.Tests/Mapping/MapperInvoiceTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+2); golden `tests/fixtures/output/sample-analysis.golden.json` (Home Depot line `invoiceRef` = `home-depot-88213.pdf`, the only change, reviewed by hand) | `MapperInvoiceTests` (14), `JobPipelineTests.Should_SetInvoiceRefOnHomeDepotLine_…`, `…_SupplyPayeeFromInvoice_…`, `AnalysisGoldenTests`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 474, Api 73 | done | `InvoiceRef` = the matched invoice's file name, set on the line whatever its decision (post, hold or skip). Payee: the description is resolved first (FR-6); only when that finds no name does a matched invoice supply one, and only (a) for lines that take a payee — vendor for card lines and ACH debits, customer for deposits; numbered checks and transfers stay blank — (b) when the invoice role fits (vendor ↔ vendor line, customer ↔ deposit), and (c) when the party resolves to a known name through the same alias/fuzzy rules (known spelling is used). Otherwise the line stays `unknown-payee` and the note names the invoice party. A description payee is never replaced by the invoice's. Note `payee from invoice <file>` records the source. Tier 3 account evidence (`CategoryHint` → T4) is M5 (T-502). Sample decisions unchanged (8/2/1). SPEC-GAPs in Q-30 |
| T-404 | Tests: unique/ambiguous/direction/unmatched | §15 | tests `Api.Tests/Api/InvoiceApiTests.cs` (new), `Core.Tests/Extract/InvoiceExtractorTests.cs` (+1 real client), `Core.Tests/Output/ResultDocumentTests.cs` (+1 round-trip); `src/QbAutopost.Api/Endpoints/JobView.cs` (`unmatchedInvoices`) | `InvoiceApiTests` (5), `InvoiceExtractorTests.Should_ReturnFacts_When_RealClientSucceedsOnRetry`, `ResultDocumentTests.Should_RoundTripUnmatchedInvoices_…`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 476, Api 78, 3 consecutive green runs | done | Gap check against plan T-404 / spec §15. Already covered by T-401…T-403: unique match (one candidate whatever direction, window and tolerance bounds), ambiguous → none (tie, nothing ≥ threshold, one line claimed by two invoices), direction preference (vendor → debit, customer → credit, none preferred → all), unmatched reported (`JobPipelineTests` via `ResultDocument`), T3 validation and holds, `InvoiceRef` and payee supply. Added: the host path (sample matches, nothing unmatched, `invoices/<file>.json` written; unmatched total → reported in view and `result.json` with counts unchanged; Hermes down → job still `ready`, invoice `hermes-failed`; image with OCR off → `unreadable`; duplicate invoice file → both `ambiguous`, nothing extra held), T3 through the real `HermesClient` (invalid answer → retry with the errors → fenced valid answer), and `result.json` read back with unmatched invoices (a failed read would silently drop the whole result). **Addition to spec §6:** `GET /jobs/{id}` now carries `unmatchedInvoices` (same objects as `result.json`), like the existing `unreadable` addition |

## M5 — Tiers 3–4, G3

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-501 | AccountChooser T4, list-constrained validation, candidates | §9.4 | `src/QbAutopost.Core/Hermes/{AccountAnswer.cs,prompts/account.md}`, `Core/Mapping/AccountChooser.cs` (`AccountQuestion`, `AccountChoice`, `AccountChoiceResult`), `Core/Abstractions/IHermesClient.cs` (`HermesRequest.Check`), `Core/Hermes/{JsonReply,HermesClient}.cs` (check runs inside the retry), `Core/Models/HoldReasons.cs` (`no-accounts`), `src/QbAutopost.Api/Program.cs` (`account.md` required at startup, `AccountChooser` registered); tests `Core.Tests/Hermes/AccountAnswerTests.cs`, `Core.Tests/Mapping/AccountChooserTests.cs`, `Core.Tests/TestSupport/ScriptedHermes.cs` and `Api.Tests/TestSupport/FakeHermesClient.cs` (apply `Check`) | `AccountAnswerTests` (18), `AccountChooserTests` (19, two through the real `HermesClient`); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 513, Api 78 | done | `IValidatable.Validate()` cannot see the job's accounts, so `HermesRequest` gained an optional `Check` that the client runs after `Validate()`; a list violation is a failed answer and gets the one §9 retry with the error appended (then `hermes-failed`). Validation: account non-blank and in the list (ordinal, no trimming); confidence present, 0…1 inclusive. `ChoosableAccounts(QbLists)`: when any account has a type, only Expense/Income/CostOfGoodsSold/OtherExpense (any case), else all; blank and repeated names dropped, list order kept. User message = date, description, amount (`0.00`), direction, kind (camelCase), payee, invoice hint (or `none`), then the accounts one per line. `Candidates` = account + alternatives, listed names only, distinct, at most 3. Empty list → `no-accounts`, no call. Threshold and G3 are T-502; the pipeline does not call T4 yet. SPEC-GAPs in Q-31 |
| T-502 | Mapper tiers Rule→History→Invoice→Model; G3 policy | FR-6/7 | `src/QbAutopost.Core/Mapping/ModelTiers.cs` (`ModelTiers`, `ModelTierInput`), `Core/Gates/ConfidenceGate.cs` (G3), `Core/Mapping/Mapper.cs` (`InvoicesByLine` shared), `Core/Mapping/{LineAccountTiers,Rules}.cs` (threshold must be in (0, 1]), `Core/Models/MappedTxn.cs` (`ModelConfidence`), `Core/Models/HoldReasons.cs` (`low-confidence`, `no-prior-posting`), `Core/Pipeline/JobPipeline.cs` (new `AccountChooser` argument; tiers 3–4 after the job gates); tests `Core.Tests/Gates/ConfidenceGateTests.cs`, `Core.Tests/Mapping/{ModelTiersTests,RulesTests}.cs`, `TestSupport/TestAccountChooser.cs`, `Pipeline/JobPipelineTests.cs` (+7, T4 scripted; plumber test now `no-accounts`), `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor) | `ConfidenceGateTests` (13), `ModelTiersTests` (18), `RulesTests` (8), `JobPipelineTests` tier cases (7); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 559, Api 78; analysis golden unchanged | done | `Mapper` stays synchronous and still holds tier-1/2 misses `no-account-rule` (payee already resolved); the pipeline then runs `ModelTiers` on those lines, one T4 call each, **after** the job gates, so duplicate, already-posted and unrequested-kind lines never reach Hermes (the kind gate now also applies to `no-account-rule` lines). Tier 3: matched **vendor** invoice with a non-blank hint → T4 with the hint; score ≥ threshold → post `Invoice`, tier 3 (no ledger requirement, FR-7). Otherwise tier 4 with the same answer (no second call): G3 posts `Model` only when score ≥ threshold **and** a live ledger entry has the same payee and line account (ignoring case); else held `low-confidence` (checked first) or `no-prior-posting`, `Tier` 4, `LineAccount` empty, `Candidates` = top-3 listed accounts, `ModelConfidence` kept. Chooser holds (`hermes-failed`, `no-accounts`) keep `Tier` null. The note records `model: <account> (<score>) [with hint from invoice <file>] — <reason>` after any payee note. `rules.json` `ModelConfidenceThreshold` outside (0, 1] now fails the job. Sample job unchanged (no qb-lists → no T4 call). `analysis.json` does not show `ModelConfidence` yet (T-503). Posting re-analysis calls T4 again (Q-27). SPEC-GAPs in Q-32 |
| T-503 | analysis.json complete (tier, confidence, candidates, decision) | §10 | `src/QbAutopost.Core/Output/OutputDocuments.cs` (`AnalysisLine.ModelConfidence`); `tests/fixtures/output/sample-analysis.golden.json` (+`modelConfidence: null` per line); tests `Core.Tests/Output/AnalysisLineTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+1) | `AnalysisLineTests` (5: every §10 field present, G3-held tier-4 line, invoice-tier line, no model call, round trip), `JobPipelineTests.Should_WriteTierScoreAndCandidatesToAnalysisJson_When_G3HoldsTheLine`, `AnalysisGoldenTests` (golden diff reviewed: only the 11 added null fields); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 565, Api 78 | done | Every §10 field was already written; the missing piece was the T4 score. `confidence` stays the FR-6 category (a G3-held line reads `hold`, tier 4) and the score is a new `modelConfidence` (null when the model was not asked); held tier-4 lines show the top-3 account `candidates`, `reason` and `decision: hold`. `result.json`/`JobView` held items are unchanged (they already carry `candidates`). SPEC-GAP in Q-33 |
| T-504 | Tests: per-tier, thresholds, list violation, G3 matrix | §15 | tests `Core.Tests/Mapping/ModelTiersTests.cs` (+14: G3 matrix theory, given-threshold theory), `Core.Tests/Pipeline/JobPipelineTests.cs` (+4: `rules.json` threshold reaches the pipeline, rule and history tiers never ask the model; `_rulesFile`/`UseRules` helper), `Api.Tests/Api/AccountTierApiTests.cs` (3: invoice tier posts, G3 hold shows account candidates in `JobView`, unlisted account → `hermes-failed` and absent from `request.qbxml`) | Gap check against §15/FR-6/FR-7: tier 1 `MapperRoutingTests`/`MapperInvoiceTests`, tier 2 `MapperTierTests`, tiers 3–4 `ModelTiersTests`, thresholds `ConfidenceGateTests` + `ModelTiersTests` + `JobPipelineTests.Should_UseRulesThreshold_*`, list violation `AccountAnswerTests`/`AccountChooserTests` (incl. the real client) + `AccountTierApiTests`, G3 matrix `ModelTiersTests.Should_FollowTheG3Matrix_When_ModelAnswers` (10 rows) + `ConfidenceGateTests`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 583, Api 81 | done | No production code changed. Linux run still open |

## M6 — QuickBooks gateway

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-601 | QbSession (COM) + QbGateway with busy timeout; FakeQbGateway; DI switch | FR-11 | `src/QbAutopost.QuickBooks/{QbSession,QbGateway}.cs`, `Core/Gateway/ResilientQbGateway.cs`, `Core/QbXml/QbXmlRequests.cs`, `Core/Abstractions/IQbGateway.cs` (+`QuickBooksBusyException`, `QuickBooksCallException`), `Core/Pipeline/JobPipeline.cs` (busy → `quickbooks-busy`), `Api/QuickBooks/{QbConnection,SimulatedQuickBooks,SimulatedQbGateway,UnconfiguredQbGateway}.cs`, `Api/Program.cs`, `Api/Configuration/AppSettings.cs` (`QuickBooks:Fake`, `RetryDelaySeconds`); tests `Core.Tests/Gateway/ResilientQbGatewayTests.cs`, `Core.Tests/QbXml/QbXmlRequestsTests.cs`, `Api.Tests/Api/QbConnectionTests.cs`, `Api.Tests/TestSupport/{FakeQbGateway,ApiFactory}.cs` | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 597, Api 91; `QbConnectionTests.Should_EndPartialWithQuickBooksBusy_When_PostingCallHangs` | done | COM path not run here (no QuickBooks on the dev box); T-609 checks it. `FakeQbGateway` now answers from `SimulatedQuickBooks` (adds, G4 queries, lists, host, TxnDel); tests replace `QbConnection`, so the host still wraps the fake in `ResilientQbGateway`. New gap Q-34 |
| T-602 | POST /qb/sync-lists + missingInRules | FR-15 | `Core/QbXml/{QbMessageSet,QbListQuery,QbStatus}.cs`, `Core/Pipeline/QbListSync.cs`, `Core/Mapping/Rules.cs` (`AccountNames()`), `Api/Endpoints/QuickBooksEndpoints.cs` (+ shared `QuickBooksProblem` 503/502 mapping), `Api/Jobs/{JobQueue,JobWorker}.cs` (`JobAction.Exclusive`, `RunExclusiveAsync`), `Api/Program.cs`; fixtures `qbxml/list-query.golden.xml`, `qbxml/list-response.xml`; tests `Core.Tests/QbXml/QbListQueryTests.cs` (7), `Core.Tests/Pipeline/QbListSyncTests.cs` (6), `Api.Tests/Api/SyncListsApiTests.cs` (6), `Api.Tests/Jobs/JobWorkerTests.cs` (+2) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 610, Api 99 | done | Sync runs on the single worker (after any job ahead of it), so it never overlaps a job that reads `qb-lists.json`. New gap Q-35 |
| T-603 | G4 live duplicate query + query audit files | FR-8 | `Core/QbXml/QbTxnQuery.cs` (`ExistingTxn`), `Core/Gates/LiveDuplicateGate.cs`, `Core/Pipeline/{JobPipeline,PipelineOptions}.cs` (G4 before the add; `NothingPosted`), `Core/Models/HoldReasons.cs` (`duplicate-check-failed`), `Core/Output/OutputDocuments.cs` (`SkippedItem.Note`), `Api/Program.cs`; fixtures `qbxml/txn-query-{check,deposit}.golden.xml`, `qbxml/{check,deposit}-query-response.xml`; tests `Core.Tests/QbXml/QbTxnQueryTests.cs` (11), `Core.Tests/Gates/LiveDuplicateGateTests.cs` (13), `Api.Tests/Api/DuplicateCheckApiTests.cs` (6), `PostingApiTests` counts writes only | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 634, Api 105 | done | Posting now sends one query message set per (kind, account) before the add; `analysis.json`, `request.qbxml` and the sheets are rewritten when G4 changes a line. Dry runs send nothing. `DepositQuery` with `AccountFilter` to be confirmed in T-609. New gap Q-36 |
| T-604 | Posting: retry once, backup-age guard, posting transition, response.qbxml | FR-11 | `Core/Gateway/ResilientQbGateway.cs` (retry once after `RetryDelay`), `Core/Gates/BackupGuard.cs`, `Core/Jobs/JobStatus.cs` (posting → failed), `Core/Pipeline/{JobPipeline,PipelineOptions,AnalysisResult}.cs`, `Api/Configuration/AppSettings.cs` (backup folder resolved), `Api/Program.cs`; tests `Core.Tests/Gateway/ResilientQbGatewayTests.cs` (+7), `Core.Tests/Gates/BackupGuardTests.cs` (8), `Core.Tests/Jobs/JobRecordTests.cs` (posting → failed allowed), `Api.Tests/Api/PostingSafetyApiTests.cs` (9) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 650, Api 114 | done | The posting transition is saved before the worker runs (since T-104; `Should_PersistPostingBeforeCallingQuickBooks_When_PostIsAccepted`), and `response.qbxml` is written before G5 (since T-103). The backup guard also covers T-703's "failed (backup-too-old)"; T-703 still owns any config work. New gap Q-37 |
| T-605 | G5 verify + ledger (atomic) + result.json + posted/partial | FR-12, §13 | code from T-103 (`Core/Gates/PostVerifier.cs`, `JobPipeline.RecordInLedger`, `Core/Store/LedgerStore.cs` via `AtomicFile`); tests `Core.Tests/Store/LedgerStoreTests.cs` (+1: §13 field names and order), `Api.Tests/Api/PostResultApiTests.cs` (4: `posted` when nothing is held, every ledger field of a posted check, `result.json` posted items and totals, deposit recorded from the `DepositTotal` echo) | Reviewed against FR-12/§13: G5 rules `PostVerifierTests` (7), atomic save `LedgerStoreTests.Should_LeaveNoTempFile_When_Saved`, posted/partial `PostResultApiTests`/`PostingApiTests`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 651, Api 118 | done | No production code changed. The fake echoes what was sent; T-609 compares a real SDK response (checklist item added) |
| T-606 | POST /batches/{id}/undo | FR-13 | | Api test | todo | |
| T-607 | GET /health/quickbooks | FR-16 | | Api test | todo | |
| T-608 | Api tests: full post, refused line → partial, hang → busy, undo, sync-lists | §15 | | `dotnet test` | todo | |
| T-609 | **[server]** Manual verification on a COPY of the company file: health → sync-lists → dry run → post → undo | §15 | | human checklist below | todo | |

T-609 checklist (human):
- [ ] `GET /health/quickbooks` → ok (certificate dialog answered "Yes, always"; user HermesBridge)
- [ ] `POST /qb/sync-lists` → counts plausible; `missingInRules` empty after fixing rules.json
- [ ] Dry run on a real folder → `ready`; `analysis.json` reviewed
- [ ] `POST /jobs/{id}/post` on the copy → `posted`; transactions visible in QuickBooks
- [ ] `POST /batches/{id}/undo` → all deleted
- [ ] Bitness confirmed (x64/x86) and recorded here: ______

## M7 — Rules, logging, hardening

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-701 | POST /rules/alias, /rules/account; validation vs qb-lists; hot reload | FR-14 | | Api test | todo | |
| T-702 | Serilog console + rolling file; correlation id; secret scrubbing | §14 | | manual + test | todo | |
| T-703 | Backup-age guard config + failed(backup-too-old) | FR-11 | | Api test | todo | |
| T-704 | GET /jobs?status= | §6 | | Api test | todo | Implemented in T-104 (`JobsApiTests.Should_ListJobsAndFilterByStatus_When_Asked`); confirm and close in M7 |
| T-705 | docs/runbook.md | plan M7 | | review | todo | |

## M8 — Deploy

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-801 | deploy/hermes docker-compose.yml, .env.example, README-hermes.md (WSL 2 + Docker Engine; Docker Desktop alt) | §4 | | compose config valid | todo | |
| T-802 | deploy/start-all.ps1, install-task.ps1 | §4 | | dry run on server | todo | |
| T-803 | **[server]** Shadow week: 5 real folders dry run, diff vs manual, rules added | plan M8 | | log below | todo | |
| T-804 | **[server]** Go-live one company, then all | plan M8 | | log below | todo | |

## Questions raised during implementation

| # | Task | Question | Conservative choice taken | Answer (human) |
|---|---|---|---|---|
| Q-0 | T-002 | The POC (`poc/QbAutopost`) is not available. Will it be supplied? | Core written from the spec (ADR-0007); every item below is a guess to confirm | Owner: build from spec (2026-09-16) |
| Q-1 | T-002 | What is the `rules.json.CsvLayouts` format, and what are the real bank/card CSV columns? | Layout = `HeaderContains[]` + column names + `DateFormat` + one signed `AmountColumn` (`PositiveIsDebit`) or `DebitColumn`/`CreditColumn`, plus optional check-number/balance/last-four columns. No layout or 2+ layouts match → statement held. A date that fails the layout format is a row error (no fallback). Sample layouts imitate Chase exports | |
| Q-2 | T-002 | What does `Normalize(description)` do? | Trim, collapse whitespace, upper-case invariant; digits and punctuation kept. Fuzzy matching also drops apostrophes and turns other punctuation into spaces | |
| Q-3 | T-002 | Fingerprint field spelling (kind, direction, missing check number)? | `bank`/`card`, `debit`/`credit`, missing check number = "" | |
| Q-4 | T-002 | G1 "both orientations" — row order, balance sign, or both? | Row order only (file order or reversed). The sign is fixed by kind: bank credits raise the balance, card charges raise it. A partly filled balance column fails G1 | |
| Q-5 | T-002 | Which columns do the paste-ready sheets have, and in what date format? | Checks: Bank, Date, Number, Payee, Account, Amount, Memo. Card: Card, Date, Number, Payee, Type, Account, Amount, Memo. Deposits: Date, Received From, From Account, Memo, Amount, Deposit To. ISO dates, UTF-8 BOM, CRLF; text cells starting with `= + - @` get a leading `'` | |
| Q-6 | T-002 | What is the base `rules.json` schema, and what are the defaults? | Keys as in `Rules.cs`; `FuzzyThreshold` 0.85, `HistoryMinCount` 3. Missing `HoldingExpenseAccount` → numbered checks held; missing `DepositIncomeAccount` → deposits held. Pattern `Match` is a plain substring, not a regex | |
| Q-7 | T-002 | What happens when a check number is over 11 characters, or several aliases / equal fuzzy scores match? | Line held (`refnumber-too-long` / `unknown-payee` with a note); never truncated or guessed. Memo over 4095 characters is truncated (no money impact) | |
| Q-8 | T-001 | CLAUDE.md's NuGet list doesn't include the xUnit runtime packages (`Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`) | Added — xUnit tests can't run without them | |
| Q-9 | T-002 | File name with several 4-digit groups (e.g. `2026-chase-4521.csv`)? | No last-four taken from the name; fall back to the content, else hold `unknown-account` | |
| Q-10 | T-002 | One unparseable row in a CSV: hold the row or the whole statement? | Whole statement held (`unparsable-rows`), because a missing row makes the statement incomplete | |
| Q-11 | T-003 | How should `requirement.txt` state real last-four values? | Assumed as 4-digit groups on the account lines ("Bank Account=… 4521"). Please share a real example | |
| Q-12 | T-101 | Which characters may a job folder name (= job id) contain? | Only letters, digits, `.`, `_`, `-`; anything else → 400. The id appears in URLs and in batch ids `<jobId>#<attempt>` | |
| Q-13 | T-101 | Subfolders inside `statements\` / `invoices\`? | Not read; each is listed in `result.json.unreadable[]` with reason `subfolder-ignored`. `.jpeg`/`.tif` invoices are `unsupported-extension` (spec lists only pdf/png/jpg) | |
| Q-14 | T-102 | Where are the "known job folders" (spec §6 `GET /jobs`, startup recovery) recorded? | New setting `Paths.JobIndex` (default `jobs.json`) maps job id → folder and is rewritten atomically when a job is first saved. A job found `queued` at startup is set `failed (interrupted)`, not re-queued, because it might be a non-dry run | |
| Q-15 | T-103 | Must the company named in `requirement.txt` match `Company.Name`? | Yes: a different name (compared ignoring case and punctuation) fails G2, because the app always posts to the configured company file. The `Company` placeholder or no name → the configured name is used. An empty `Company.Name` fails every job | |
| Q-16 | T-103 | Statement-level hold codes the spec does not name | `reconcile-failed` (G1), `extractor-not-available` (xlsx/pdf until M3), `kind-mismatch` (layout says bank but the requirement lists the last-four only as a card, or the reverse) | |
| Q-17 | T-103 | Line-level: identical lines in one job (same fingerprint → same requestID), and kinds the requirement did not ask for | Every copy of an identical line is held `duplicate-line` (never post one of them); a postable line whose kind is not in the spec's kinds is held `kind-not-requested`. Precedence: skip-pattern → duplicate-line → already-posted → kind-not-requested | |
| Q-18 | T-103 | What happens when the QuickBooks call itself fails? | Any exception → job `partial`, every sent line held `quickbooks-no-response`, and the batch is recorded in the ledger with 0 posted, so `POST /jobs` refuses a re-run until someone checks QuickBooks and uses `force`. `QuickBooksUnavailableException` (thrown only before anything is sent) → `partial` "nothing posted", no ledger record. A response line with status 0 but a different echoed amount is held `amount-mismatch`, and its note names the TxnID to delete | |
| Q-19 | T-104 | API key defaults | An empty `Api:ApiKey` stops the host at startup; the shipped `change-me` is accepted only in Development. `appsettings.Development.json` uses `dev` (plan §4 curl) and local `data/` paths | |
| Q-20 | T-105 | result.json fields beyond spec §10 | Adds `company`, `error`, `counts.posted`, `totals.posted`; held items carry `file`, `lineNo`, `kind`, `note`, `candidates`. `unmatchedInvoices` stays `[]` until M4. The sample's `invoices/home-depot-88213.txt` stand-in is reported `unsupported-extension` (invoices accept pdf/png/jpg only) | |
| Q-21 | T-201 | Should a Hermes transport failure (timeout, connection refused, HTTP 5xx, malformed envelope) be retried, and must the `Authorization` header be sent when `Hermes:ApiKey` is empty? | No retry: spec §9 names only the validation retry, and the caller already falls back (T1) or holds (T2–T4). Empty key → no header (a local Hermes may run without one). Audit copies are written for every HTTP attempt, including failed ones | |
| Q-22 | T-202 | FR-2 falls back to the regex parser when T1 fails validation twice. What if Hermes is unreachable (timeout, refused, HTTP 5xx)? | The job fails (`analysis failed: Hermes Spec call failed: …`) instead of falling back, because the regex reading was not what the operator expected and the rule is to post less when unsure. Re-submit once Hermes is back. Changing this to a fallback is one `catch` in `HermesSpecReader` | |
| Q-23 | T-301 | FR-3 says a date that fails the layout format falls back to invariant `DateOnly.TryParse`; Q-1 chose "row error, no fallback". Which wins, and how are XLSX date cells handled? A workbook that cannot be opened? | Q-1 kept (session 4, owner said go ahead with it): a text date must match the layout format, otherwise the row fails and the statement is held, because an invariant (US) parse can silently swap day and month. XLSX real date cells are read as their date (`yyyy-MM-dd`), so the XLSX parser also accepts an exact ISO date; CSV does not. A plain-number "date" is a row error. A formula with no saved result is blank → row error. A workbook ClosedXML cannot open → statement held `unreadable-statement`. Following the spec instead is one `\|\|` in `StatementGridParser.TryParseDate` | |
| Q-24 | T-302 | FR-3 "text per page < 40 characters → scanned": are spaces and line breaks counted? What if OCR is enabled but a scanned page has no images, or an image OCR cannot read? Which OCR languages? | Whitespace is not counted (a page of blanks is scanned). OCR disabled → any scanned page holds the whole file, even a near-blank last page. OCR enabled: a scanned page without images keeps its own short text (nothing to OCR; T2 and G1 still check the rows); an image that is neither JPEG nor decodable by PdfPig, or any OCR error → file held `unreadable-statement`. English (`eng`) only. `Ocr:Enabled` with missing language data stops the host at startup | |
| Q-25 | T-303 | §9.2 T2: reason codes for a Hermes failure; how statement-level fields (kind, last-four, period, balances, count) combine across chunks; which last-four wins; is a statement with zero rows valid? | `hermes-failed` (validation twice or unreachable; later parts are not sent) and `extraction-conflict`. Each field must be the same in every part that reports it (null = not shown); any difference holds the statement — including a "balance forward" misread as the opening balance on a later page. File-name last-four wins (spec F5); a different one in the text → `conflicting-last4` (since T-305 the same for a CSV/XLSX account column). Zero rows is accepted here; G1 (T-304) decides with the totals. `transactionCount` is only what the statement prints; the model is told never to count | |
| Q-26 | T-304 | FR-4's T2 formula `opening + Σcredits − Σdebits = closing` fits bank statements. For cards? And what if the T2 answer has no opening or closing balance (FR-4 only says CSV/XLSX without totals pass `not-verifiable`)? Partly printed running balances? | Card: charges (debits) raise the balance, as for CSV G1. T2 without both opening and closing balance → G1 fails `not-verifiable`, statement held — model-read amounts are never posted unchecked (CLAUDE.md rule 3). Running balances printed on only some rows are checked between the printed ones (for CSV a partly filled balance column still fails, Q-4). A zero-row statement passes when opening = closing (and the printed count, if any, is 0) | |
| Q-27 | T-304 | FR-10 re-runs the analysis before posting. For PDFs that means a second T2 call, whose rows could differ from the dry run the operator reviewed (still G1-checked). Should posting reuse the reviewed `rows.json` when the PDF is unchanged? | Not decided; today T2 is called again (temperature 0) and the post uses whatever passes G1 then. Suggest before M6: store the PDF's SHA-256 in `rows.json` and reuse those rows when the hash matches, so what is posted is what was reviewed. Since T-402 the same applies to invoices (T3 is called again at post time; `invoices/<file>.json` could be reused the same way) | |
| Q-28 | T-401 | §9.3 T3: hold codes for invoices, long invoices, and how the model tells vendor from customer | `unreadable` (plan M4) for an image with OCR off, an unopenable PDF, an OCR error, no text, or text over 60 000 characters (one request per invoice, never chunked, so a total is never read from part of an invoice); `scanned-pdf-ocr-disabled` for a scanned PDF with OCR off; `hermes-failed` for a Hermes failure. The job's company name is sent as "Our company" so the model can decide the role. A held invoice never holds a line; how it is reported (`result.json.unreadable[]` vs `unmatchedInvoices[]`) is decided in T-402. Sample `invoices/home-depot-88213.txt` still reported `unsupported-extension`; T-402 replaces it with the PDF fixture | |
| Q-29 | T-402 | FR-5 matching details the spec leaves open: invoice without a date; no candidate in the preferred direction; what "payee similarity" compares before payees are resolved; one line matched by several invoices; which lines are candidates; where unreadable invoices are reported | No date → no match (`no-invoice-date`). No same-direction candidate → all candidates go to the similarity step. Similarity = `Fuzzy.MatchScore(line description, invoice party)` (the FR-6 payee is resolved later). A line matched by 2+ invoices → none of them match (`ambiguous`, note names the other files). Candidates = every line of a statement that passed G1. No candidate → `no-matching-line`. Unreadable/`hermes-failed` invoices are listed in `result.json.unmatchedInvoices[]` with their hold code as `reason` (not in `unreadable[]`, which stays the folder-level F2 list); answers Q-28's open point | |
| Q-30 | T-403 | FR-5 "supplies Payee (if the line has none)": which value is `InvoiceRef`; may the invoice party be used when it is not a QuickBooks name; which lines and roles qualify | `InvoiceRef` = invoice file name (unique in the job; the invoice number may be missing). The party is used only when it resolves to a known vendor/customer (QuickBooks lists, `VendorAccounts` keys, ledger payees, aliases) — otherwise QuickBooks would reject the line or a new name would be invented. Only lines that take a payee (FR-6 table) and only a fitting role: vendor invoice → card line / ACH debit, customer invoice → deposit. Numbered checks ("Blank if Check") and transfers never get one | |
| Q-31 | T-501 | §9.4 T4: how is the account list filtered when only some accounts have a type; is OtherIncome allowed; must `alternatives` be listed names; what if there are no accounts? | When any account has a type, untyped accounts are dropped and only Expense, Income, CostOfGoodsSold and OtherExpense are kept (OtherIncome is not named in §9.4); when none has a type, all are kept. An unlisted alternative does not reject the answer (§9.4 rejects only on `account`) but is dropped from `candidates`, so a held line never suggests an unknown name. No accounts (qb-lists.json not synced) → line held `no-accounts`, Hermes not called. A list violation is retried once like any invalid answer, then `hermes-failed` | |
| Q-32 | T-502 | FR-6/FR-7 tiers 3–4 and G3: what happens when the invoice-hinted answer is below the threshold; which invoices count as tier-3 evidence; G3 hold codes; what "prior posting to the same account" compares; is the threshold range checked? | A tier-3 answer below the threshold becomes the tier-4 answer (Hermes is not asked again without the hint) and G3 holds it. Only a matched **vendor** invoice with a non-blank `categoryHint` is evidence (tiers only run for vendor lines). Holds: `low-confidence` (score < threshold, checked first), `no-prior-posting` (no live ledger entry with the same payee and line account, compared ignoring case as in tier 2). Held model lines show no `lineAccount`; the suggestion is `candidates[0]` and in the note. T4 runs only for lines that passed the job gates. `ModelConfidenceThreshold` must be in (0, 1], else `rules.json` fails to load and the job fails | |
| Q-33 | T-503 | §10 lists one `confidence` field per `analysis.json` line. Is it the FR-6 category (rule/history/invoice/model/holding/hold) or the Hermes T4 score? | Both are written: `confidence` keeps the category (a line held by G3 reads `hold` with `tier` 4) and the score is an extra field `modelConfidence` (0…1, null when the model was not asked). `result.json` / `JobView` held items do not show the score | |
| Q-34 | T-601 | Plan T-601 says DI picks the fake gateway when `QuickBooks:Fake=true` or the host is not Windows, but a fake that answers "posted" would put invented TxnIDs in the real ledger. Also: what happens to a call still running when the busy timeout fires, and may another call start meanwhile? | `QuickBooks:Fake=true` → an in-memory simulated company (`SimulatedQuickBooks`, TxnIDs `SIM-n`), allowed only in the Development or Testing environment (startup fails otherwise). Not Windows and not faked → `UnconfiguredQbGateway`: nothing is ever sent and posting ends `partial` "nothing posted". Every QuickBooks call (worker, undo, sync-lists, health) goes one at a time through `ResilientQbGateway`. A call past `BusyTimeoutSeconds` is abandoned (`quickbooks-busy`; lines held and batch recorded as in Q-18) but keeps the lock until the COM call really ends; a caller that cannot get the lock within the busy timeout gets `quickbooks-unavailable` (nothing sent). Empty `Company:FilePath` → unavailable. `QuickBooks:RetryDelaySeconds` (default 5) exists only so tests do not wait | |
| Q-35 | T-602 | FR-15 details: which name field is stored, how `missingInRules` compares names, where sync's audit copies go (FR-17 names only job files), and what happens when QuickBooks refuses a list query? | Accounts and customers store `FullName` (sub-accounts `Utilities:Electric`, jobs `Customer:Job`), vendors `Name`; `AccountType` kept as the type. `missingInRules` = distinct account names in `BankAccounts`, `CardAccounts`, `VendorAccounts`, `KeywordAccounts`, `TransferPatterns`, `HoldingExpenseAccount`, `DepositIncomeAccount` not found **exactly** (ordinal) in QuickBooks, so a case difference is reported. Audit copies: `qb-audit/sync-lists.request.qbxml` / `.response.qbxml` next to `qb-lists.json` (overwritten each sync). Queries use `onError="stopOnError"`; status 1 (no match) = empty list; any other status → 502, and `qb-lists.json` is not changed. Unreadable `rules.json` → nothing sent, 500. The sync waits for the job worker (it may wait behind a running job) | |
| Q-36 | T-603 | FR-8 G4 details: how the query filters, how payees and RefNumbers compare, what a deposit's "payee" is, what happens when a query fails, and how the audit files are named (FR-17 says `query-<n>.qbxml`) | One message set per (kind, header account, compared ignoring case) with `TxnDateRangeFilter` + `AccountFilter`; results whose header account differs are ignored (QuickBooks' `AccountFilter` also matches line accounts). Same amount = exactly equal. "Exact" = same date and (same RefNumber other than `ACH`, ignoring case, or same payee ignoring case); a deposit's payee is its first line's `EntityRef` (`IncludeLineItems`). Several candidates → the first exact one skips the line, else one hold note lists them all. A refused or unreadable query → that group's lines held `duplicate-check-failed`; a gateway failure (unavailable, busy, COM) → nothing is added, job `partial` "nothing posted", no ledger record. Files: `output/query-<n>.qbxml` (request) and `query-<n>.response.qbxml`, old ones deleted at the start of each post. A skipped line's note (TxnID) is now shown in `result.json.skipped[].note` | |
| Q-37 | T-604 | FR-11 retries "a COMException" once after 5 s, but a COM error from `ProcessRequest` on an add may already have posted the lines. And FR-11 says a stale backup makes the job `failed`, while §6 allows only `posting → posted/partial` | Retried once: a session that could not open (nothing sent) and a COM error on a read-only (query) message set. **Not** retried: a COM error on an add or delete (lines held `quickbooks-no-response`, batch recorded, Q-18), a busy timeout, or a wait for a still-running abandoned call. Backup guard runs in `posting` just before G4 and only when there is something to post: blank `BackupFolder` → no check; missing folder, no `*.QBB` (top level only, ignoring case), or newest `LastWriteTimeUtc` older than `BackupMaxAgeHours` → job `failed` with `backup-too-old: …`, nothing sent, no ledger record (so a plain re-submit works). `posting → failed` was added to the state machine for this case only | |

## Decisions

| Date | Decision | Why |
|---|---|---|
| 2026-09-15 | Core is cross-platform; COM isolated in QbAutopost.QuickBooks | tests run anywhere; server only for M6/M8 |
| 2026-09-15 | Invoices are evidence only (Q1 assumed) | avoids a second posting pipeline in v1 |
| 2026-09-15 | Deposits post straight to income (Q2 assumed) | pending answer on open customer invoices |
| 2026-09-16 | Build Core from the spec; POC unavailable (ADR-0007) | owner chose to proceed without the POC; gaps listed in Questions |
| 2026-09-16 | ADRs live in `docs/adr/` (0001–0007) | architecture decisions recorded with their alternatives |
| 2026-09-16 | `global.json` pins SDK 8.0.x (`rollForward: latestFeature`) | SDK 10 is installed on the dev box and would otherwise be used |
| 2026-09-16 | JSON enums are written camelCase (`"ready"`, `"post"`, `"ccCharge"`) and read case-insensitively | spec §6/§10 show lowercase values; `rules.json` `"Kind": "Bank"` still loads; `ledger.json` `kind` becomes `"check"` (no real ledger exists yet) |
| 2026-09-16 | M1 already posts through `IQbGateway` (G5 + ledger), with an `UnconfiguredQbGateway` that sends nothing until M6 | T-103 names `RunPostAsync`; the state machine needs a real posting path to test; M6 tasks keep the COM, live-G4, retry, busy and backup work |
| 2026-09-16 | Repo root is `D:\qb_post` (plan calls it `qb-autopost/`); `CLAUDE.md` copied to the root, `docs/CLAUDE.md` kept | plan T-005; copies must be kept in sync until one is removed |

## Blockers

| Date | Task | Blocker | Owner | Resolved |
|---|---|---|---|---|
| | | | | |

## Session log

| Date | Session | Tasks touched | Result |
|---|---|---|---|
| 2026-09-16 | 1 | ADRs, T-001…T-005 | M0 done on Windows: build has 0 warnings; 92 tests pass (Core 91, Api 1); scripts run. Linux test run still open. Found and fixed a `.gitignore` rule that hid `Output/` sources |
| 2026-09-16 | 2 | T-101…T-107 | M1 done on Windows: 235 tests (Core 181, Api 54), 8 consecutive green runs; sample job reaches `ready` via HTTP in tests and by hand. No owner answers to Q-1…Q-11 yet, so the conservative choices stand. New gaps Q-12…Q-20. Found and fixed a Windows file-sharing race between polling readers and atomic replace. Linux test run still open |
| 2026-09-16 | 3 | T-201…T-205 | M2 done on Windows: 331 tests (Core 263, Api 68), 6 consecutive green runs. Hermes client (retry with errors, audit copies, ping), T1 prompt + `SpecAnswer`, `HermesSpecReader` with regex fallback, G2 via Hermes, `/health/hermes`, `spec.json` golden. No owner answers yet. New gaps Q-21 (no transport retry; empty key → no header) and Q-22 (Hermes unreachable → job fails, no fallback). **Behaviour change:** without a running Hermes the sample job now fails instead of reaching `ready`. Linux test run still open |
| 2026-09-16 | 4 | T-301…T-305 | XLSX statements parsed (ClosedXML → shared grid/layout logic) and reconciled like CSV (T-301, Core 283 / Api 68). PDF text per page with PdfPig, scanned-page detection, `IOcr` with a Tesseract engine behind `Ocr:Enabled` (T-302); 369 tests (Core 298, Api 71); real Tesseract checked by hand on Windows. New gap Q-24 (what counts as a character; scanned page without images). Still no owner answers in the tracker. Date rule decided: Q-1 kept, with ISO accepted for XLSX date cells (Q-23). New hold code `unreadable-statement`. Found: ClosedXML never saves formula results, so tests imitate Excel's saved `<v>`. T-303: Hermes T2 extractor (prompt, validation, 60 000-char page-group chunking, merge) with 44 new tests → Core 342, Api 71; not yet wired into the pipeline (T-304). New gap Q-25. T-304: T2 wired into the pipeline via `StatementReader`, G1 for T2 (totals, count, period, partial running balances; card sign reversed; no totals → held), `rows.json` with `totals` → Core 365, Api 73; a text-PDF bank statement gives the same 8/2/1 as the CSV. New gaps Q-26, Q-27 (T2 re-run at post time). T-305: gap check — grid-parser tests (ambiguous layout was untested), committed PDF fixtures for both sample statements, card PDF end to end; an account column contradicting the file name now holds the statement; binary fixtures marked in `.gitattributes`. **M3 done on Windows: 455 tests (Core 382, Api 73).** Linux test run still open |
| 2026-09-16 | 5 | T-401, T-402 | Hermes T3 invoice extractor: `InvoiceAnswer` validation, `invoice.md` prompt (required at startup), `InvoiceExtractor` (PDF via PdfText, PNG/JPG via OCR or `unreadable`, Hermes failure → `hermes-failed`), committed invoice PDF fixture → Core 424, Api 73. No owner answers yet. New gap Q-28. Not yet wired into the pipeline (T-402). Linux test run still open. T-402: `InvoiceMatcher` (FR-5 amount/date window, direction, payee similarity, ties → `ambiguous`, one line claimed by two invoices → neither), invoices read after G2, `output/invoices/<file>.json`, `result.json.unmatchedInvoices[]` as objects; sample invoice is now a PDF and matches the Home Depot line → Core 458, Api 73. New gap Q-29 |
| 2026-09-17 | 5 (cont.) | T-403 | Mapper takes the matched invoices: `InvoiceRef` = invoice file on the matched line; payee supplied from a fitting invoice only when the description names none and the party is a known name; numbered checks and transfers stay blank. Golden: Home Depot line gains `invoiceRef` → Core 474, Api 73. New gap Q-30 |
| 2026-09-17 | 5 (cont.) | T-404 | Gap check: API tests for invoices (match, unmatched, Hermes down, image without OCR, duplicate invoice), T3 through the real client, `result.json` round-trip with unmatched invoices; `GET /jobs/{id}` shows `unmatchedInvoices`. **M4 done on Windows: 554 tests (Core 476, Api 78).** No owner answers yet (Q-1…Q-30). Linux test run still open |
| 2026-09-17 | 6 | T-501 | Owner answers checked: none yet. Hermes T4: `AccountAnswer` (account non-blank, 0 ≤ confidence ≤ 1), list check through the new `HermesRequest.Check` (inside the client's retry), `account.md` prompt (required at startup), `AccountChooser` (§9.4 type filter, top-3 listed candidates, `no-accounts`, `hermes-failed`) → Core 513, Api 78. Not wired into mapping yet (T-502). New gap Q-31. Linux test run still open |
| 2026-09-17 | 6 (cont.) | T-502 | Tiers 3–4 + G3: `ModelTiers` pass after the job gates (lines held `no-account-rule`), `ConfidenceGate` (invoice tier ≥ threshold; model tier ≥ threshold and a prior ledger posting of the payee to the account), holds `low-confidence` / `no-prior-posting` with top-3 candidates, `MappedTxn.ModelConfidence`, threshold range checked in `rules.json` → Core 559, Api 78. Sample job and golden unchanged. New gap Q-32 |
| 2026-09-17 | 7 | T-503 | Owner answers checked: none yet. `analysis.json` lines gain `modelConfidence` (T4 score; `confidence` stays the category); every §10 field checked by `AnalysisLineTests`; golden updated (11 null fields only) → Core 565, Api 78. New gap Q-33 |
| 2026-09-17 | 7 (cont.) | T-504 | Gap check for tiers and G3: G3 matrix (10 rows) and threshold theories in `ModelTiersTests`, `rules.json` threshold and tier precedence through the pipeline, `AccountTierApiTests` (tiers 3–4 through the host) → Core 583, Api 81. **M5 done on Windows.** No owner answers yet (Q-1…Q-33). Linux test run still open |
| 2026-09-17 | 8 | T-601 | Owner answers checked: none yet (Q-1…Q-33). `QbSession`/`QbGateway` (late-bound COM on an STA thread, one session per call), `ResilientQbGateway` (one call at a time, busy timeout → `quickbooks-busy`), DI switch via `QbConnection` (SDK / simulated with `QuickBooks:Fake` in Development / unavailable), `FakeQbGateway` backed by `SimulatedQuickBooks` → Core 597, Api 91. COM path unverified until T-609. New gap Q-34 |
| 2026-09-17 | 8 (cont.) | T-602 | `POST /qb/sync-lists`: one read-only message set (Account/Vendor/Customer query, active only) → `qb-lists.json` + `missingInRules`; runs as exclusive work on the job worker; QuickBooks failures → 503/502 problem details → Core 610, Api 99. New gap Q-35 |
| 2026-09-17 | 8 (cont.) | T-603 | G4 against QuickBooks while posting: `QbTxnQuery` (Check/CreditCardCharge/CreditCardCredit/Deposit query over [min−W, max+W]), `LiveDuplicateGate` (exact → `already-in-quickbooks`, else `possible-duplicate`; refused query → `duplicate-check-failed`), `query-<n>.qbxml` audit files; a gateway failure during the check posts nothing and records nothing → Core 634, Api 105. New gap Q-36 |
| 2026-09-17 | 8 (cont.) | T-604 | FR-11 posting safety: one retry after 5 s when repeating cannot double-post (session not opened; COM error on queries), backup-age guard (`failed` `backup-too-old`, new `posting → failed` move), tests for the persisted `posting` state and `response.qbxml` → Core 650, Api 114. New gap Q-37 |
| 2026-09-17 | 8 (cont.) | T-605 | G5/ledger/result reviewed against FR-12 and §13; added tests for the `posted` end state, §13 ledger field names, ledger entry contents, `result.json` posted items, deposit echo → Core 651, Api 118. No production change |
