# QbAutopost.Api — Implementation Tracker

Updated: 2026-09-17 · Owner: Braj · Executor: Claude Code
Spec: `spec.md` · Plan: `plan.md`

**How to update (Claude Code does this after every task):**
1. Change the task's Status: `todo` → `doing` → `done` (or `blocked` with a note).
2. Fill *Files* (paths created/changed) and *Verified by* (test names or the command run).
3. Append one line to the Session log.
4. Never delete rows; never mark a **[server]** task `done` — leave it `ready-for-human` and the human flips it.

Status legend: `todo` · `doing` · `done` · `blocked` · `ready-for-human`

## Milestones

| M | Name | Tasks | Done | Status |
|---|---|---|---|---|
| M0 | Bootstrap | 5 | 5 | done (Windows and Linux) |
| M1 | API host, lifecycle, CSV dry run | 7 | 7 | done (Windows and Linux) |
| M2 | Hermes client, T1, G2 | 5 | 5 | done (Windows and Linux) |
| M3 | XLSX, PDF, T2, G1 | 5 | 5 | done (Windows and Linux) |
| M4 | Invoices T3 + matcher | 4 | 4 | done (Windows and Linux) |
| M5 | Tiers 3–4, G3 | 4 | 4 | done (Windows and Linux) |
| M6 | QuickBooks gateway, post, undo | 9 | 8 | done on Windows except T-609 (ready-for-human, server); tests green on Linux |
| M7 | Rules, logging, hardening | 5 | 5 | done (Windows and Linux) |
| M8 | Deploy, shadow, go-live | 4 | 1 | agent work done on Windows: T-801 done; T-802, T-803, T-804 ready-for-human (server) |
| M9 | API v1: versioned routes, health/SDK/connection/file validation, direct post, remote auth | 14 | 13 | **agent work done** (branch `m9-api-v1`): T-901, T-902, T-904…T-914 done; T-903 ready-for-human (server) — contract is `docs/spec-api-v1.md` (owner decisions D-1…D-4, 2026-09-22). The final report `docs/M9-COMPLETE.md` is **written** (2026-09-23). Remaining: only the server verification nobody but a person can do — T-903, T-609, T-802/803/804, T-806 and the new direct-post checklist, all consolidated in `docs/M9-COMPLETE.md` §3 |

## M0 — Bootstrap

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-001 | Solution + 3 src projects + 2 test projects, Directory.Build.props, .editorconfig, .gitignore | §4, plan §1 | `QbAutopost.sln`, `global.json`, `Directory.Build.props`, `.editorconfig`, `.gitignore`, `.gitattributes`, `src/*/*.csproj`, `src/QbAutopost.Api/Program.cs`, `tests/*/*.csproj`, `tests/QbAutopost.Api.Tests/HostSmokeTests.cs` | `dotnet build -warnaserror` (0 warnings); `HostSmokeTests` | done | CA1416 suppressed only in QbAutopost.QuickBooks. `global.json` pins SDK 8.0.x (SDK 10 also installed). Test packages added: Microsoft.NET.Test.Sdk 17.14.1, xunit 2.9.3, xunit.runner.visualstudio 3.1.5 (see Q-8) |
| T-002 | Port POC into Core (Models, Text, Rules, Fuzzy, Mapper, Gates, QbXmlBuilder/Parser, Ledger, BatchEnterSheet, CsvStatementParser, RegexSpecParser) | §7, §9, §11, §13 | `src/QbAutopost.Core/{Models,Text,Mapping,Gates,QbXml,Store,Output,Extract}/*.cs` | build green; covered by T-004 tests | done | **POC unavailable → written from spec (ADR-0007).** Gaps marked `SPEC-GAP T-002`, listed in Questions Q-1…Q-9. Mapper covers tiers 1–2 only; lines needing tiers 3–4 are held `no-account-rule` until T-502. `CsvStatementParser` delegates to `StatementGridParser` (shared with T-301 XLSX) |
| T-003 | Fixtures: samples/jobs/2026-08-tropicana, tests/fixtures/hermes/*.json, tests/fixtures/qbxml/*.golden.xml | §5, §9 | `samples/jobs/2026-08-tropicana/{requirement.txt,statements/*.csv,invoices/home-depot-88213.txt}`, `samples/rules.json`, `tests/fixtures/hermes/{spec,statement,invoice,account}.json`, `tests/fixtures/qbxml/{check,creditcard,deposit}.golden.xml`, `tests/fixtures/qbxml/add-response.xml`, `tests/fixtures/statements/*.csv` | files present; used by T-004 tests | done | All data synthetic (no POC samples). Bank CSV 7 rows (newest-first, balances reconcile 13195.87 → 10230.45), card CSV 4 rows. Invoice is a `.txt` stand-in for a text PDF (plan allows). Golden requestIDs computed independently with PowerShell SHA-256, not by the code under test |
| T-004 | First tests: CSV parser, fingerprint, G1 both orientations, FR-6 routing table, qbXML golden | §8 FR-3/4/6/9 | `tests/QbAutopost.Core.Tests/{TestSupport,Text,Extract,Gates,Mapping,QbXml,Store,Output,Architecture}/*.cs`, `SampleJobTests.cs`; fix in `src/QbAutopost.Core/Text/TextNormalizer.cs` | `dotnet test` → Core 91 passed, Api 1 passed (Windows, SDK 8.0.420) | done | Covers: CSV 7/4 rows (FR-3 AC), fingerprint vs independent SHA-256, G1 file/reverse order + failure modes, all 7 FR-6 rows + holds, tier 2 history, qbXML 3 golden files, response parser, ledger atomic save, sheets, RegexSpecParser FR-2 AC, Core has no COM tokens. Tests found one bug: apostrophes in names ("Joe's") broke fuzzy matching — fixed. Tests written in the same session as T-002 rather than strictly first. Linux run still to be done (no Linux box here) |
| T-005 | scripts/build.ps1, test.ps1, run-api.ps1; CLAUDE.md at repo root | plan §4 | `scripts/{build,test,run-api}.ps1`, `CLAUDE.md` | build.ps1 → 0 errors; test.ps1 → 92 passed; run-api.ps1 → "Now listening on http://127.0.0.1:5080" | done | CLAUDE.md copied, not moved (see Decisions) |

## M1 — API host, lifecycle, CSV dry run

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-101 | FolderReader + folder validation F1–F5, JobInput | §5 | `src/QbAutopost.Core/Jobs/{FolderReader,JobInput,InvalidJobFolderException}.cs`, `HoldReasons.SubfolderIgnored`; `tests/QbAutopost.Core.Tests/Jobs/FolderReaderTests.cs`, `TestSupport/TempJobFolder.cs` | `FolderReaderTests` (22); `dotnet test` → Core 113, Api 1 | done | F1 validate, F2 unreadable, F4 top-level only / `output/` never listed, F5 file-name last-four (content fallback stays in the parsers). F3 is checked by the endpoint (T-104). Job-id charset and subfolder reporting are SPEC-GAPs (Q-12, Q-13) |
| T-102 | JobRecord, JobStore (memory + status.json), JobQueue (Channel), JobWorker | §6 | `src/QbAutopost.Core/Jobs/{JobStatus,JobRecord,IJobStore}.cs`, `Core/Abstractions/IClock.cs`, `Core/Text/JsonOptions.cs` (camelCase enums); `src/QbAutopost.Api/Configuration/AppSettings.cs`, `Api/Jobs/{JobStore,JobQueue,IJobProcessor,JobWorker}.cs`; `tests/QbAutopost.Api.Tests/{Jobs/JobStoreTests,Jobs/JobWorkerTests,TestSupport/*}.cs`, `Core.Tests/Jobs/JobRecordTests.cs`, `Core.Tests/Text/JsonOptionsTests.cs` | `JobStoreTests` (7), `JobWorkerTests` (4), `JobRecordTests`, `JsonOptionsTests`; `dotnet test` → Core 139, Api 12 | done | One job at a time (single reader, asserted). State machine enforced by `JobRecord.MoveTo`. status.json is written atomically, before memory. Worker fail-safe: if the processor crashes, the job goes to `failed` (from analysing) or `partial (interrupted …)` (from posting); cancellation at shutdown leaves the status for startup recovery. Job index file is a SPEC-GAP (Q-14). Worker DI registration is in T-104 |
| T-103 | Pipeline orchestrator (RunAnalysisAsync / RunPostAsync) with RegexSpecParser stand-in | §8 | `src/QbAutopost.Core/Pipeline/{JobPipeline,PipelineOptions,ISpecReader,AnalysisResult}.cs`, `Core/Gates/{SpecGate,PostVerifier}.cs`, `Core/Abstractions/{IQbGateway,IHermesClient}.cs`, `Core/Store/QbListsStore.cs`, `HoldReasons` (pipeline codes); `src/QbAutopost.Api/Jobs/JobRunner.cs`; tests `Core.Tests/Pipeline/JobPipelineTests.cs`, `Core.Tests/Gates/{SpecGateTests,PostVerifierTests}.cs` | `JobPipelineTests` (17), `SpecGateTests` (10), `PostVerifierTests` (7); Api `PostingApiTests` | done | Analysis = FR-1…FR-9 (CSV only; xlsx/pdf held `extractor-not-available` until M3) + G1 per statement + **G2 (logic landed here, see T-203)** + ledger G4 (`already-posted`). Post = re-analysis → one qbXML call → G5 → ledger → posted/partial. Gateway exception → all lines held, batch recorded with 0 posted (F3 then refuses a re-run: check QuickBooks first); `QuickBooksUnavailableException` (nothing sent) → no ledger record. M6 adds live G4, retry, busy timeout, backup guard and the COM gateway. SPEC-GAPs Q-15…Q-18 |
| T-104 | Endpoints POST /jobs, GET /jobs/{id}, GET /jobs, POST /jobs/{id}/post; problem details; API key; Kestrel bind | §6 | `src/QbAutopost.Api/{Program.cs,appsettings.json,appsettings.Development.json}`, `Api/Endpoints/{JobEndpoints,JobView}.cs`, `Api/Jobs/JobAdmission.cs`, `Api/Security/ApiKeyMiddleware.cs`, `Api/QuickBooks/UnconfiguredQbGateway.cs`, `Api/Configuration/AppSettings.cs` (`ResolvePaths`); `.gitignore` (`src/QbAutopost.Api/data/`) | `JobsApiTests` (19), `AuthTests` (8); manual run below | done | RFC 7807 for 400/401/404/409 (400 lists `errors[]`). Key compared via SHA-256 + fixed-time compare; empty key → host refuses to start; `change-me` refused outside Development (Q-19). Kestrel binds `Api:Bind`. Env prefix `QBAUTOPOST__`. Admission (create/post) is serialised; an active job → 409, ledger job → 409 unless `force`. `POST /jobs/{id}/post` moves the job to `posting` (with batch id) before queueing. `GET /jobs?status=` implemented here (T-704). Default gateway is `UnconfiguredQbGateway` until T-601. Manual (Windows, Development): sample → 202 → `ready` with 8/2/1 and totals 4942.64; post → `partial` "nothing posted: QuickBooks unavailable"; second post → 409; no key → 401; log has no secrets |
| T-105 | Writers: analysis.json, result.json, status.json, paste-ready CSVs | §10 | `src/QbAutopost.Core/Output/{JobOutputWriter,OutputDocuments}.cs`, `Core/Store/AtomicFile.cs` (shared read + retry); tests `Core.Tests/Output/{ResultDocumentTests,AnalysisGoldenTests}.cs`, `Core.Tests/Store/AtomicFileTests.cs`, `tests/fixtures/output/sample-analysis.golden.json` | `AnalysisGoldenTests` (golden reviewed by hand against the FR-6 table), `ResultDocumentTests` (4), `AtomicFileTests` (3) | done | Writes spec.json, statements/&lt;file&gt;.rows.json, analysis.json, request.qbxml + 3 sheets (also in dry run), response.qbxml, result.json. result.json adds `company`, `error`, `counts.posted`, `totals.posted` to the spec shape (Q-20). Found while testing: on Windows a polling `GET /jobs/{id}` and the atomic replace block each other (500 / failed rename) → readers share delete access and both sides retry with backoff (≈1.5 s) |
| T-106 | Startup recovery of interrupted jobs | §6 | `src/QbAutopost.Api/Jobs/StartupRecovery.cs`, `Program.cs` | `RecoveryTests` (6) | done | Runs after `Build()`, before the worker starts. analysing → `failed (interrupted)`; posting → `partial (interrupted — run duplicates before re-post)`; queued → `failed` (Q-14). result.json keeps the last completed run; status.json is the authority |
| T-107 | Api tests: happy path to ready; 400/404/409; auth; recovery | §15 | `tests/QbAutopost.Api.Tests/TestSupport/{ApiFactory,FakeQbGateway,FakeHermesClient,TempDir,Fixtures,FixedClock}.cs`, `Api/{JobsApiTests,PostingApiTests,AuthTests,RecoveryTests}.cs`; removed `HostSmokeTests.cs` (covered by `AuthTests`) | `dotnet test` → Core 181, Api 54, 8 consecutive green runs | done | Every data file lives in a per-test temp folder; no real QuickBooks/Hermes. `FakeQbGateway` records requests, answers with sequential `FAKE-n` TxnIDs and echoed amounts, can refuse line N, throw, or hang. `FakeHermesClient` serves `tests/fixtures/hermes/*.json` (not called until M2). Posting covered: full post → partial (2 held), ledger entries, forced re-run skips posted lines and uses batch #2, dryRun=false, refused line, unavailable vs failed gateway, double post → 409 |

## M2 — Hermes client, T1, G2

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-201 | HermesClient: OpenAI-compatible call, auth, timeout, fence strip, retry-with-error, audit copies | §9 | `src/QbAutopost.Core/Abstractions/IHermesClient.cs` (`HermesRequest`, `HermesException`, `HermesValidationException`, `HermesUnavailableException`), `src/QbAutopost.Core/Hermes/{HermesClient,HermesOptions,HermesAudit,JsonReply}.cs`, `src/QbAutopost.Api/Program.cs` (typed `HttpClient`, `HermesOptions`); `tests/QbAutopost.Core.Tests/Hermes/{HermesClientTests,JsonReplyTests}.cs`, `TestSupport/StubHttpHandler.cs`; `tests/QbAutopost.Api.Tests/TestSupport/FakeHermesClient.cs` | `HermesClientTests` (19), `JsonReplyTests` (16); `dotnet test` → Core 216, Api 54 | done | Interface now `CompleteJsonAsync<T>(HermesRequest, ct)` (spec §9 signature: task, system prompt, user content, schema hint + audit folder). Invalid answer (prose, bad JSON, empty, `Validate()` errors) → one retry with the errors appended → `HermesValidationException`. Non-2xx, timeout, connection error, malformed envelope → `HermesUnavailableException`, no retry (Q-21). Audit: `output/hermes/<task>-<n>.request/response.json`, one pair per HTTP attempt; key only in the header, and scrubbed from audit text when ≥ 8 chars. Timeout is per call (`HttpClient.Timeout` infinite). Empty key → no `Authorization` header (Q-21). Host DI wiring is exercised by `HealthApiTests` (T-204) |
| T-202 | Prompt spec.md + T1 JobSpec + validation + regex fallback | §9.1, FR-2 | `src/QbAutopost.Core/Hermes/{prompts/spec.md,PromptTemplate,PromptLibrary,SpecAnswer}.cs`, `src/QbAutopost.Core/Pipeline/HermesSpecReader.cs`, `Pipeline/ISpecReader.cs` (`SpecReadResult.Note`), `Output/{OutputDocuments,JobOutputWriter}.cs` (`spec.json.note`), `QbAutopost.Core.csproj` (prompts copied to `<app>/Hermes/prompts`), `src/QbAutopost.Api/Program.cs` (`PromptLibrary` loaded before start, `HermesSpecReader` replaces `RegexSpecReader`); tests `Core.Tests/Hermes/{SpecAnswerTests,PromptLibraryTests}.cs`, `Core.Tests/Pipeline/HermesSpecReaderTests.cs`; `Api.Tests/TestSupport/FakeHermesClient.cs` (`Respond`, validates answers), `Api.Tests/Api/JobsApiTests.cs` (3 tests script their T1 answer) | `SpecAnswerTests` (26), `PromptLibraryTests` (6), `HermesSpecReaderTests` (6, two through the real client + stub HTTP); `dotnet test` → Core 254, Api 54; prompt file present in Api and test output folders | done | `CreditCard` → `CcCharge` + `CcCredit`; kinds matched ignoring case, only `Check`/`CreditCard`/`Deposit` accepted; `kinds`, `bankLast4`, `cardLast4` must be present (may be empty); last-four = exactly 4 ASCII digits; company `Company`/blank → null. Two invalid answers → `RegexSpecParser`, `source = regex-fallback`, `note` = the validation errors. Hermes unreachable → job `failed` (Q-22). Fixture answer and regex parse agree on the sample (FR-2 AC). Unknown `{{placeholder}}` or missing `spec.md` → error at startup |
| T-203 | Gate G2 → failed with explanation | FR-2 | `tests/QbAutopost.Api.Tests/Api/SpecGateApiTests.cs`, `tests/fixtures/output/sample-spec.golden.json`; gate code unchanged (`SpecGate`, T-103), `spec.json.note` added in T-202 | `SpecGateApiTests` (9); `dotnet test` → Core 254, Api 63 | done | Through the Hermes path: sample `spec.json` equals the golden file (reviewed by hand against the FR-2 AC: source `hermes`, kinds check/ccCharge/ccCredit/deposit, bank 4521, card 7788, gate ok). G2 negatives: stated account without a statement, no kinds, statement account neither stated nor in rules.json, another company → `failed`, error starts `requirement check failed (G2)`, `spec.json.gate.errors` lists the reasons, no analysis.json / request.qbxml. Placeholder company → configured name. Invalid answer → `regex-fallback` + `note` (ready on the sample; failed when the regex finds nothing). Hermes unreachable → failed, no fallback (Q-22) |
| T-204 | GET /health/hermes | FR-16 | `src/QbAutopost.Core/Abstractions/IHermesClient.cs` (`PingAsync`, `HermesPing`), `Core/Hermes/HermesClient.cs` (`PingAsync`; shared `PostAsync` with timeout), `src/QbAutopost.Api/Endpoints/HealthEndpoints.cs`, `Program.cs`; tests `Core.Tests/Hermes/HermesClientTests.cs` (+9), `Api.Tests/Api/HealthApiTests.cs`, fakes (`FakeHermesClient.Ping`) | `HermesClientTests` ping cases, `HealthApiTests` (4, two through the host's real `HermesClient` with a stub handler); `dotnet test` → Core 263, Api 67 | done | One completion with `max_tokens: 5`, no audit copy; ok iff HTTP 200 with non-empty content. Body `{ ok, model, latencyMs, message }` — `message` (null when ok) is an addition to spec §6 so an operator sees why (e.g. `HTTP 500`, `timed out after 120 s`, `empty completion`). 503 when not ok; no API key (middleware exempts `/health`). The real-client tests also prove the DI wiring (config → `HermesOptions`, bearer key, base URL) and that the key does not appear in the 503 body |
| T-205 | Tests: fixtures, retry, fallback, G2 negatives | §15 | `tests/QbAutopost.Api.Tests/TestSupport/ApiFactory.cs` (`Hermes:BaseUrl` = `hermes.invalid`), `Api.Tests/Api/HealthApiTests.cs` (guard test); coverage landed with T-201…T-204 | `dotnet test` → Core 263, Api 68, 6 consecutive green runs; manual run below | done | Fixtures: `FakeHermesClient` serves `tests/fixtures/hermes/*.json` (and validates). Malformed-then-valid: `HermesClientTests` (prose, bad JSON, empty, invalid → retry), `HermesSpecReaderTests.Should_ReturnHermesSpec_When_RealClientSucceedsOnRetry`. Fallback: `HermesSpecReaderTests`, `SpecGateApiTests` (ready and failed variants). G2 negatives: `SpecGateTests` (Core) + `SpecGateApiTests` (4 via Hermes). M2 "done when": `spec.json` golden ✓, health 503 with the fake failing ✓. Rule 1 guard: the test host points Hermes at `.invalid`. Manual (Windows, Development, no Hermes running): `/health/hermes` → 503 "actively refused (127.0.0.1:8642)" in ~2 s; sample job → `failed` "analysis failed: Hermes Spec call failed: …" with `output/hermes/spec-1.*.json` written, no key in files or log |

## M3 — XLSX, PDF, T2, G1

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-301 | XlsxStatementParser (ClosedXML → layout logic) | FR-3 | `src/QbAutopost.Core/Extract/{XlsxGrid,XlsxStatementParser}.cs`, `Extract/StatementGridParser.cs` (`acceptIsoDates`), `Models/HoldReasons.cs` (`unreadable-statement`), `Pipeline/JobPipeline.cs` (`.xlsx` routed; only `.pdf` still `extractor-not-available`), `QbAutopost.Core.csproj` (ClosedXML 0.105.1); tests `Core.Tests/Extract/XlsxStatementParserTests.cs`, `TestSupport/XlsxBuilder.cs`, `Extract/CsvStatementParserTests.cs` (+1), `Pipeline/JobPipelineTests.cs` (+2, pdf hold); fixture `tests/fixtures/statements/chase-checking-4521.xlsx` | `XlsxStatementParserTests` (17), `JobPipelineTests.Should_PostSameLines_When_BankStatementIsAWorkbook`, `…_When_WorkbookIsCorrupt`; `dotnet test` → Core 283, Api 68 | done | First worksheet only, blank rows dropped, first non-blank row = header, same layouts as CSV. Cells read without display formats: numbers → invariant digits (double → decimal keeps 15 digits, so `0.1+0.2` → 0.3; > 2 decimals still a row error), real date cells → `yyyy-MM-dd` (time dropped), booleans/errors → text that fails parsing, formulas → the result saved in the file (never recalculated; none saved → blank → statement held). **Date decision (Q-1 kept, Q-23):** text dates must match the layout format, no invariant fallback; the XLSX parser also accepts exact `yyyy-MM-dd`. A workbook ClosedXML cannot open → `unreadable-statement`. Fixture generated by a throw-away ClosedXML program from the sample bank CSV (synthetic). ClosedXML never writes formula results, so `XlsxBuilder` adds `<v>` to the sheet XML to imitate Excel |
| T-302 | PdfText (PdfPig), scanned detection, Ocr wrapper behind Ocr.Enabled | FR-3 | `src/QbAutopost.Core/Abstractions/IOcr.cs` (`IOcr`, `DisabledOcr`), `Core/Extract/PdfText.cs` (`PdfText`, `PdfTextResult`, `PdfPageText`), `Core/Pipeline/JobPipeline.cs` (new `IOcr` constructor argument; statements read async; PDFs go through `PdfText`), `QbAutopost.Core.csproj` (PdfPig 0.1.16); `src/QbAutopost.Api/Ocr/TesseractOcr.cs`, `Api/Program.cs` (`IOcr` by `Ocr:Enabled`, resolved at startup), `Api/Configuration/AppSettings.cs` (relative `TessDataPath` resolved), `QbAutopost.Api.csproj` (Tesseract 5.2.0); tests `Core.Tests/Extract/PdfTextTests.cs`, `TestSupport/{PdfBuilder,TestImage,FakeOcr}.cs`, `Pipeline/JobPipelineTests.cs` (+3, pdf test now uses a real PDF), `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor), `Api.Tests/Api/OcrApiTests.cs` | `PdfTextTests` (12), `JobPipelineTests` PDF cases (4), `OcrApiTests` (3); `dotnet test` → Core 298, Api 71. Manual (Windows, scratch program, tessdata_fast `eng.traineddata`, not committed): real `TesseractOcr` read "08/15/2026 ACH DEBIT UNKNOWN PLUMBER SVC -3199.70" exactly, both from a PNG and from a one-image PDF through `PdfText` | done | Page text via `ContentOrderTextExtractor`; pages joined with `\n\f\n`. Scanned = fewer than 40 **non-whitespace** characters (Q-24). OCR disabled + any scanned page → whole file `scanned-pdf-ocr-disabled` (error names the pages). OCR enabled → each image on a scanned page is OCR'd (JPEG streams passed as-is, other images re-encoded to PNG by PdfPig; an image neither can give → `unreadable-statement`); OCR text is appended to the page's own text. Scanned page with no images → its short text is kept (Q-24). Damaged/encrypted/non-PDF file or OCR exception → `unreadable-statement`; cancellation propagates. Readable PDFs are still held `extractor-not-available` until T-303 (T2). `Ocr:Enabled` with a missing or empty `TessDataPath` stops the host at startup. The Tesseract package bundles Windows x86/x64 native libraries only; Linux needs system Tesseract 5 + Leptonica (OCR is off by default, spec Q3). Only `eng` is loaded |
| T-303 | StatementLlmExtractor T2: prompt, chunking, merge, validation | §9.2 | `src/QbAutopost.Core/Hermes/{prompts/statement.md,StatementAnswer}.cs`, `Core/Extract/StatementLlmExtractor.cs` (`StatementChunk`), `Core/Extract/StatementParseResult.cs` (`Totals`, `StatementTotals`), `Core/Models/HoldReasons.cs` (`hermes-failed`, `extraction-conflict`), `src/QbAutopost.Api/Program.cs` (`statement.md` required at startup); tests `Core.Tests/Hermes/StatementAnswerTests.cs`, `Core.Tests/Extract/StatementLlmExtractorTests.cs`, `TestSupport/ScriptedHermes.cs` (moved out of `HermesSpecReaderTests`); fixture `tests/fixtures/statements/chase-checking-4521.pdf.txt` | `StatementAnswerTests` (27), `StatementLlmExtractorTests` (17, one through the real `HermesClient` retry); `dotnet test` → Core 342, Api 71 | done | Fixture text + `hermes/statement.json` give the same rows and fingerprints as the sample bank CSV (FR-3 AC, T-305). Validation: kind `bank`/`card` and direction `debit`/`credit` (any case), ISO dates (real dates), amounts present, > 0 and ≤ 2 decimals, balances ≤ 2 decimals, `accountLast4` 4 digits or null, period start ≤ end, count ≥ 0, `rows` required (may be empty), `checkNo` a string. Chunking: whole text ≤ 60 000 chars → one request; otherwise consecutive page groups each ≤ 60 000; a page over the limit goes alone, never cut. Each request says "part i of n (pages a-b)". Merge: rows joined in part order, `lineNo` 1…n; kind, last-four, period, balances and count must agree across parts (null = not shown) else `extraction-conflict` (Q-25). File-name last-four wins; a different last-four in the text → `conflicting-last4`; none anywhere → `unknown-account`. Any `HermesException` → `hermes-failed`, remaining parts not sent; cancellation propagates. **Not wired into the pipeline yet** — PDFs stay `extractor-not-available` until T-304 adds the T2 G1 checks, so no T2 row can post unchecked |
| T-304 | G1 extended + rows.json writer | FR-4 | `src/QbAutopost.Core/Gates/ReconcileGate.cs` (`CheckExtraction`, partial running-balance check), `Core/Pipeline/StatementReader.cs` (new: csv/xlsx/pdf → rows), `Core/Pipeline/JobPipeline.cs` (takes `StatementReader` instead of `IOcr`; T2 statements checked with `CheckExtraction`), `Core/Pipeline/AnalysisResult.cs` + `Core/Output/OutputDocuments.cs` (`totals` on the statement summary and `rows.json`), `src/QbAutopost.Api/Program.cs` (`StatementLlmExtractor`, `StatementReader`); tests `Core.Tests/Gates/ReconcileGateExtractionTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (PDF cases rewritten, +5), `TestSupport/TestStatementReader.cs`, `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor), `Api.Tests/Api/PdfStatementApiTests.cs` | `ReconcileGateExtractionTests` (18), `JobPipelineTests` PDF cases, `PdfStatementApiTests` (2, through the host with `FakeHermesClient`); `dotnet test` → Core 365, Api 73. No manual run: no Hermes on the dev box, so a real job fails at T1 (Q-22) before reaching T2 | done | PDFs now post: PdfText → T2 → G1 → mapping. With the bank CSV replaced by a text PDF of the same statement the sample job gives the same 8 postable lines (same requestIDs, accounts, payees) and 8/2/1 through the API. T2 G1 = `\|opening + Σeffects − closing\| ≤ 0.01`; `rows == transactionCount` when printed; every date inside `[periodStart, periodEnd]` (each bound checked when given); printed running balances consistent in file or reverse order (rows without a balance are summed into the next printed one). All failures are listed together. **Card sign reversed** (charges raise the balance), **no opening/closing → `reconcile-failed` "not-verifiable"**, verified = false (Q-26). `rows.json` carries `layout: "hermes-t2"`, `totals` and `reconcile {ok, verified, message}`; T2 holds (`hermes-failed`, `extraction-conflict`, `conflicting-last4`) and a kind that contradicts the requirement hold only that statement. Hermes audit copies go to `output/hermes/statement-<n>.*`. Open: re-analysis at post time calls T2 again (Q-27) |
| T-305 | Tests: fixture text → rows; corrupted → held; XLSX detection | §15 | tests `Core.Tests/Extract/{StatementGridParserTests,PdfFixtureTests}.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+2 card PDF cases; PDF cases use the committed fixtures), `Api.Tests/Api/PdfStatementApiTests.cs` (committed fixture); fixtures `tests/fixtures/statements/{chase-checking-4521,chase-card-7788}.pdf` (rendered by a throw-away PdfPig program from the matching `.pdf.txt`), `chase-card-7788.pdf.txt`, `tests/fixtures/hermes/statement-card-7788.json`; fix `src/QbAutopost.Core/Extract/StatementGridParser.cs` (account column vs file name); `.gitattributes` (`*.pdf *.xlsx *.png *.jpg binary`) | `StatementGridParserTests` (13), `PdfFixtureTests` (2); `dotnet test` → Core 382, Api 73 | done | Gap check against plan T-305 / spec §15. Fixture PDF text → expected rows: `StatementLlmExtractorTests` (text + `statement.json` = CSV rows and fingerprints) and `PdfFixtureTests` (committed PDFs read back line for line). With **both** sample statements as PDFs the pipeline gives the same lines, decisions and reasons as the CSVs; the card PDF is verified with the card sign and held when only the bank sign would reconcile. Chunk merge and conflicts: `StatementLlmExtractorTests`. Every G1 failure mode: `ReconcileGateTests` (CSV chain) + `ReconcileGateExtractionTests` (T2). Corrupt → held: xlsx, pdf, T2 answer, Hermes down. Layout detection (CSV/XLSX shared): matched layout, case/space-insensitive header, ambiguous → `ambiguous-csv-layout` (was untested), empty `HeaderContains`, missing layout column, empty grid, `PositiveIsDebit`, debit/credit pair rules. **Behaviour change:** a last-four in the account column that contradicts the file name now holds the statement `conflicting-last4` (it was silently ignored — the file name won); same rule as T2 (Q-25). The fix and its tests were written together. `.gitattributes` now marks binaries: with `text=auto` git could have treated PdfPig's mostly-text PDFs as text and changed their line endings |

## M4 — Invoices

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-401 | InvoiceExtractor T3 + validation; images via Ocr or unreadable | §9.3 | `src/QbAutopost.Core/Hermes/{InvoiceAnswer.cs,prompts/invoice.md}`, `Core/Extract/InvoiceExtractor.cs` (`InvoiceExtractor`, `InvoiceReadResult`), `Core/Models/HoldReasons.cs` (`unreadable`), `src/QbAutopost.Api/Program.cs` (`invoice.md` required at startup, `InvoiceExtractor` registered); tests `Core.Tests/Hermes/InvoiceAnswerTests.cs`, `Core.Tests/Extract/InvoiceExtractorTests.cs`, `Extract/PdfFixtureTests.cs` (+invoice); fixtures `tests/fixtures/invoices/home-depot-88213.pdf(.txt)` (rendered by a throw-away PdfPig program) | `InvoiceAnswerTests` (20), `InvoiceExtractorTests` (21), `PdfFixtureTests` (+1); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 424, Api 73 | done | `ReadAsync(file, company, auditDir, ct)`: `.pdf` → `PdfText` (scanned pages → OCR; OCR off → `scanned-pdf-ocr-disabled`; unopenable → `unreadable`); `.png/.jpg` → `IOcr` when enabled, else `unreadable`; OCR error, no text, or text > 60 000 chars → `unreadable` (no chunking); other extension → `unsupported-extension`; any `HermesException` → `hermes-failed`; cancellation propagates. One request per invoice; the user message names the file and, when known, "Our company" so the model can tell vendor from customer. Validation: party non-blank, role vendor/customer (any case), number null or non-blank, date ISO or null, total present, > 0, ≤ 2 decimals; text trimmed, blank hint → null. Invoices are not read by the pipeline yet (T-402); the sample's `.txt` stand-in is unchanged (Q-28) |
| T-402 | InvoiceMatcher + invoices/<file>.json + unmatchedInvoices | FR-5 | `src/QbAutopost.Core/Mapping/InvoiceMatcher.cs` (`InvoiceMatcher`, `InvoiceMatch`), `Core/Pipeline/AnalysisResult.cs` (`InvoiceSummary`, `AnalysisResult.Invoices`), `Core/Pipeline/JobPipeline.cs` (new `InvoiceExtractor` argument; invoices read after G2), `Core/Output/JobOutputWriter.cs` (`WriteInvoice`), `Core/Output/OutputDocuments.cs` (`unmatchedInvoices` = objects), `Core/Models/HoldReasons.cs` (`no-matching-line`, `no-invoice-date`); sample `samples/jobs/2026-08-tropicana/invoices/home-depot-88213.pdf` replaces the `.txt` stand-in; tests `Core.Tests/Mapping/InvoiceMatcherTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+10, T3 scripted), `TestSupport/TestInvoiceExtractor.cs`, `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor), `Api.Tests/Api/{JobsApiTests,PdfStatementApiTests,SpecGateApiTests}.cs` (the sample now makes a T3 call; no `.txt` in `unreadable`) | `InvoiceMatcherTests` (24), `JobPipelineTests` invoice cases (10); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 458, Api 73 | done | Matching (FR-5): candidates `\|amount − total\| ≤ 0.005` and `\|days\| ≤ InvoiceMatchDays` (both bounds inclusive); one candidate → match whatever its direction or text; several → same direction first (vendor↔debit, customer↔credit; none of that direction → all stay), then the single highest `Fuzzy.MatchScore(description, party)` ≥ `FuzzyThreshold`; a tie or no score ≥ threshold → `ambiguous`. Only lines of statements that passed G1 are candidates (all of them, including lines later skipped or held). Sample: Home Depot invoice (08-21) matches the 184.32 line (08-22); decisions unchanged (8/2/1). `output/invoices/<file>.json` = `{ file, facts (with matchedRequestId), reason, note, errors, candidates }`, written for every invoice file; `result.json.unmatchedInvoices[]` = the same objects for every invoice not matched, unreadable ones included. Invoices are read only when G2 passes; a failed invoice never fails or holds anything. SPEC-GAPs in Q-29. Posting re-analysis calls T3 again (Q-27) |
| T-403 | MappedTxn.InvoiceRef + payee supply | FR-5/6 | `src/QbAutopost.Core/Mapping/Mapper.cs` (optional `invoices` argument, `ResolvePayee`), `Core/Pipeline/JobPipeline.cs` (matched invoices passed to the mapper); tests `Core.Tests/Mapping/MapperInvoiceTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+2); golden `tests/fixtures/output/sample-analysis.golden.json` (Home Depot line `invoiceRef` = `home-depot-88213.pdf`, the only change, reviewed by hand) | `MapperInvoiceTests` (14), `JobPipelineTests.Should_SetInvoiceRefOnHomeDepotLine_…`, `…_SupplyPayeeFromInvoice_…`, `AnalysisGoldenTests`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 474, Api 73 | done | `InvoiceRef` = the matched invoice's file name, set on the line whatever its decision (post, hold or skip). Payee: the description is resolved first (FR-6); only when that finds no name does a matched invoice supply one, and only (a) for lines that take a payee — vendor for card lines and ACH debits, customer for deposits; numbered checks and transfers stay blank — (b) when the invoice role fits (vendor ↔ vendor line, customer ↔ deposit), and (c) when the party resolves to a known name through the same alias/fuzzy rules (known spelling is used). Otherwise the line stays `unknown-payee` and the note names the invoice party. A description payee is never replaced by the invoice's. Note `payee from invoice <file>` records the source. Tier 3 account evidence (`CategoryHint` → T4) is M5 (T-502). Sample decisions unchanged (8/2/1). SPEC-GAPs in Q-30 |
| T-404 | Tests: unique/ambiguous/direction/unmatched | §15 | tests `Api.Tests/Api/InvoiceApiTests.cs` (new), `Core.Tests/Extract/InvoiceExtractorTests.cs` (+1 real client), `Core.Tests/Output/ResultDocumentTests.cs` (+1 round-trip); `src/QbAutopost.Api/Endpoints/JobView.cs` (`unmatchedInvoices`) | `InvoiceApiTests` (5), `InvoiceExtractorTests.Should_ReturnFacts_When_RealClientSucceedsOnRetry`, `ResultDocumentTests.Should_RoundTripUnmatchedInvoices_…`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 476, Api 78, 3 consecutive green runs | done | Gap check against plan T-404 / spec §15. Already covered by T-401…T-403: unique match (one candidate whatever direction, window and tolerance bounds), ambiguous → none (tie, nothing ≥ threshold, one line claimed by two invoices), direction preference (vendor → debit, customer → credit, none preferred → all), unmatched reported (`JobPipelineTests` via `ResultDocument`), T3 validation and holds, `InvoiceRef` and payee supply. Added: the host path (sample matches, nothing unmatched, `invoices/<file>.json` written; unmatched total → reported in view and `result.json` with counts unchanged; Hermes down → job still `ready`, invoice `hermes-failed`; image with OCR off → `unreadable`; duplicate invoice file → both `ambiguous`, nothing extra held), T3 through the real `HermesClient` (invalid answer → retry with the errors → fenced valid answer), and `result.json` read back with unmatched invoices (a failed read would silently drop the whole result). **Addition to spec §6:** `GET /jobs/{id}` now carries `unmatchedInvoices` (same objects as `result.json`), like the existing `unreadable` addition |

## M5 — Tiers 3–4, G3

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-501 | AccountChooser T4, list-constrained validation, candidates | §9.4 | `src/QbAutopost.Core/Hermes/{AccountAnswer.cs,prompts/account.md}`, `Core/Mapping/AccountChooser.cs` (`AccountQuestion`, `AccountChoice`, `AccountChoiceResult`), `Core/Abstractions/IHermesClient.cs` (`HermesRequest.Check`), `Core/Hermes/{JsonReply,HermesClient}.cs` (check runs inside the retry), `Core/Models/HoldReasons.cs` (`no-accounts`), `src/QbAutopost.Api/Program.cs` (`account.md` required at startup, `AccountChooser` registered); tests `Core.Tests/Hermes/AccountAnswerTests.cs`, `Core.Tests/Mapping/AccountChooserTests.cs`, `Core.Tests/TestSupport/ScriptedHermes.cs` and `Api.Tests/TestSupport/FakeHermesClient.cs` (apply `Check`) | `AccountAnswerTests` (18), `AccountChooserTests` (19, two through the real `HermesClient`); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 513, Api 78 | done | `IValidatable.Validate()` cannot see the job's accounts, so `HermesRequest` gained an optional `Check` that the client runs after `Validate()`; a list violation is a failed answer and gets the one §9 retry with the error appended (then `hermes-failed`). Validation: account non-blank and in the list (ordinal, no trimming); confidence present, 0…1 inclusive. `ChoosableAccounts(QbLists)`: when any account has a type, only Expense/Income/CostOfGoodsSold/OtherExpense (any case), else all; blank and repeated names dropped, list order kept. User message = date, description, amount (`0.00`), direction, kind (camelCase), payee, invoice hint (or `none`), then the accounts one per line. `Candidates` = account + alternatives, listed names only, distinct, at most 3. Empty list → `no-accounts`, no call. Threshold and G3 are T-502; the pipeline does not call T4 yet. SPEC-GAPs in Q-31 |
| T-502 | Mapper tiers Rule→History→Invoice→Model; G3 policy | FR-6/7 | `src/QbAutopost.Core/Mapping/ModelTiers.cs` (`ModelTiers`, `ModelTierInput`), `Core/Gates/ConfidenceGate.cs` (G3), `Core/Mapping/Mapper.cs` (`InvoicesByLine` shared), `Core/Mapping/{LineAccountTiers,Rules}.cs` (threshold must be in (0, 1]), `Core/Models/MappedTxn.cs` (`ModelConfidence`), `Core/Models/HoldReasons.cs` (`low-confidence`, `no-prior-posting`), `Core/Pipeline/JobPipeline.cs` (new `AccountChooser` argument; tiers 3–4 after the job gates); tests `Core.Tests/Gates/ConfidenceGateTests.cs`, `Core.Tests/Mapping/{ModelTiersTests,RulesTests}.cs`, `TestSupport/TestAccountChooser.cs`, `Pipeline/JobPipelineTests.cs` (+7, T4 scripted; plumber test now `no-accounts`), `Output/{AnalysisGoldenTests,ResultDocumentTests}.cs` (constructor) | `ConfidenceGateTests` (13), `ModelTiersTests` (18), `RulesTests` (8), `JobPipelineTests` tier cases (7); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 559, Api 78; analysis golden unchanged | done | `Mapper` stays synchronous and still holds tier-1/2 misses `no-account-rule` (payee already resolved); the pipeline then runs `ModelTiers` on those lines, one T4 call each, **after** the job gates, so duplicate, already-posted and unrequested-kind lines never reach Hermes (the kind gate now also applies to `no-account-rule` lines). Tier 3: matched **vendor** invoice with a non-blank hint → T4 with the hint; score ≥ threshold → post `Invoice`, tier 3 (no ledger requirement, FR-7). Otherwise tier 4 with the same answer (no second call): G3 posts `Model` only when score ≥ threshold **and** a live ledger entry has the same payee and line account (ignoring case); else held `low-confidence` (checked first) or `no-prior-posting`, `Tier` 4, `LineAccount` empty, `Candidates` = top-3 listed accounts, `ModelConfidence` kept. Chooser holds (`hermes-failed`, `no-accounts`) keep `Tier` null. The note records `model: <account> (<score>) [with hint from invoice <file>] — <reason>` after any payee note. `rules.json` `ModelConfidenceThreshold` outside (0, 1] now fails the job. Sample job unchanged (no qb-lists → no T4 call). `analysis.json` does not show `ModelConfidence` yet (T-503). Posting re-analysis calls T4 again (Q-27). SPEC-GAPs in Q-32 |
| T-503 | analysis.json complete (tier, confidence, candidates, decision) | §10 | `src/QbAutopost.Core/Output/OutputDocuments.cs` (`AnalysisLine.ModelConfidence`); `tests/fixtures/output/sample-analysis.golden.json` (+`modelConfidence: null` per line); tests `Core.Tests/Output/AnalysisLineTests.cs`, `Core.Tests/Pipeline/JobPipelineTests.cs` (+1) | `AnalysisLineTests` (5: every §10 field present, G3-held tier-4 line, invoice-tier line, no model call, round trip), `JobPipelineTests.Should_WriteTierScoreAndCandidatesToAnalysisJson_When_G3HoldsTheLine`, `AnalysisGoldenTests` (golden diff reviewed: only the 11 added null fields); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 565, Api 78 | done | Every §10 field was already written; the missing piece was the T4 score. `confidence` stays the FR-6 category (a G3-held line reads `hold`, tier 4) and the score is a new `modelConfidence` (null when the model was not asked); held tier-4 lines show the top-3 account `candidates`, `reason` and `decision: hold`. `result.json`/`JobView` held items are unchanged (they already carry `candidates`). SPEC-GAP in Q-33 |
| T-504 | Tests: per-tier, thresholds, list violation, G3 matrix | §15 | tests `Core.Tests/Mapping/ModelTiersTests.cs` (+14: G3 matrix theory, given-threshold theory), `Core.Tests/Pipeline/JobPipelineTests.cs` (+4: `rules.json` threshold reaches the pipeline, rule and history tiers never ask the model; `_rulesFile`/`UseRules` helper), `Api.Tests/Api/AccountTierApiTests.cs` (3: invoice tier posts, G3 hold shows account candidates in `JobView`, unlisted account → `hermes-failed` and absent from `request.qbxml`) | Gap check against §15/FR-6/FR-7: tier 1 `MapperRoutingTests`/`MapperInvoiceTests`, tier 2 `MapperTierTests`, tiers 3–4 `ModelTiersTests`, thresholds `ConfidenceGateTests` + `ModelTiersTests` + `JobPipelineTests.Should_UseRulesThreshold_*`, list violation `AccountAnswerTests`/`AccountChooserTests` (incl. the real client) + `AccountTierApiTests`, G3 matrix `ModelTiersTests.Should_FollowTheG3Matrix_When_ModelAnswers` (10 rows) + `ConfidenceGateTests`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 583, Api 81 | done | No production code changed. Linux run still open |

## M6 — QuickBooks gateway

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-601 | QbSession (COM) + QbGateway with busy timeout; FakeQbGateway; DI switch | FR-11 | `src/QbAutopost.QuickBooks/{QbSession,QbGateway}.cs`, `Core/Gateway/ResilientQbGateway.cs`, `Core/QbXml/QbXmlRequests.cs`, `Core/Abstractions/IQbGateway.cs` (+`QuickBooksBusyException`, `QuickBooksCallException`), `Core/Pipeline/JobPipeline.cs` (busy → `quickbooks-busy`), `Api/QuickBooks/{QbConnection,SimulatedQuickBooks,SimulatedQbGateway,UnconfiguredQbGateway}.cs`, `Api/Program.cs`, `Api/Configuration/AppSettings.cs` (`QuickBooks:Fake`, `RetryDelaySeconds`); tests `Core.Tests/Gateway/ResilientQbGatewayTests.cs`, `Core.Tests/QbXml/QbXmlRequestsTests.cs`, `Api.Tests/Api/QbConnectionTests.cs`, `Api.Tests/TestSupport/{FakeQbGateway,ApiFactory}.cs` | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 597, Api 91; `QbConnectionTests.Should_EndPartialWithQuickBooksBusy_When_PostingCallHangs` | done | COM path not run here (no QuickBooks on the dev box); T-609 checks it. `FakeQbGateway` now answers from `SimulatedQuickBooks` (adds, G4 queries, lists, host, TxnDel); tests replace `QbConnection`, so the host still wraps the fake in `ResilientQbGateway`. New gap Q-34 |
| T-602 | POST /qb/sync-lists + missingInRules | FR-15 | `Core/QbXml/{QbMessageSet,QbListQuery,QbStatus}.cs`, `Core/Pipeline/QbListSync.cs`, `Core/Mapping/Rules.cs` (`AccountNames()`), `Api/Endpoints/QuickBooksEndpoints.cs` (+ shared `QuickBooksProblem` 503/502 mapping), `Api/Jobs/{JobQueue,JobWorker}.cs` (`JobAction.Exclusive`, `RunExclusiveAsync`), `Api/Program.cs`; fixtures `qbxml/list-query.golden.xml`, `qbxml/list-response.xml`; tests `Core.Tests/QbXml/QbListQueryTests.cs` (7), `Core.Tests/Pipeline/QbListSyncTests.cs` (6), `Api.Tests/Api/SyncListsApiTests.cs` (6), `Api.Tests/Jobs/JobWorkerTests.cs` (+2) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 610, Api 99 | done | Sync runs on the single worker (after any job ahead of it), so it never overlaps a job that reads `qb-lists.json`. New gap Q-35 |
| T-603 | G4 live duplicate query + query audit files | FR-8 | `Core/QbXml/QbTxnQuery.cs` (`ExistingTxn`), `Core/Gates/LiveDuplicateGate.cs`, `Core/Pipeline/{JobPipeline,PipelineOptions}.cs` (G4 before the add; `NothingPosted`), `Core/Models/HoldReasons.cs` (`duplicate-check-failed`), `Core/Output/OutputDocuments.cs` (`SkippedItem.Note`), `Api/Program.cs`; fixtures `qbxml/txn-query-{check,deposit}.golden.xml`, `qbxml/{check,deposit}-query-response.xml`; tests `Core.Tests/QbXml/QbTxnQueryTests.cs` (11), `Core.Tests/Gates/LiveDuplicateGateTests.cs` (13), `Api.Tests/Api/DuplicateCheckApiTests.cs` (6), `PostingApiTests` counts writes only | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 634, Api 105 | done | Posting now sends one query message set per (kind, account) before the add; `analysis.json`, `request.qbxml` and the sheets are rewritten when G4 changes a line. Dry runs send nothing. `DepositQuery` with `AccountFilter` to be confirmed in T-609. New gap Q-36 |
| T-604 | Posting: retry once, backup-age guard, posting transition, response.qbxml | FR-11 | `Core/Gateway/ResilientQbGateway.cs` (retry once after `RetryDelay`), `Core/Gates/BackupGuard.cs`, `Core/Jobs/JobStatus.cs` (posting → failed), `Core/Pipeline/{JobPipeline,PipelineOptions,AnalysisResult}.cs`, `Api/Configuration/AppSettings.cs` (backup folder resolved), `Api/Program.cs`; tests `Core.Tests/Gateway/ResilientQbGatewayTests.cs` (+7), `Core.Tests/Gates/BackupGuardTests.cs` (8), `Core.Tests/Jobs/JobRecordTests.cs` (posting → failed allowed), `Api.Tests/Api/PostingSafetyApiTests.cs` (9) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 650, Api 114 | done | The posting transition is saved before the worker runs (since T-104; `Should_PersistPostingBeforeCallingQuickBooks_When_PostIsAccepted`), and `response.qbxml` is written before G5 (since T-103). The backup guard also covers T-703's "failed (backup-too-old)"; T-703 still owns any config work. New gap Q-37 |
| T-605 | G5 verify + ledger (atomic) + result.json + posted/partial | FR-12, §13 | code from T-103 (`Core/Gates/PostVerifier.cs`, `JobPipeline.RecordInLedger`, `Core/Store/LedgerStore.cs` via `AtomicFile`); tests `Core.Tests/Store/LedgerStoreTests.cs` (+1: §13 field names and order), `Api.Tests/Api/PostResultApiTests.cs` (4: `posted` when nothing is held, every ledger field of a posted check, `result.json` posted items and totals, deposit recorded from the `DepositTotal` echo) | Reviewed against FR-12/§13: G5 rules `PostVerifierTests` (7), atomic save `LedgerStoreTests.Should_LeaveNoTempFile_When_Saved`, posted/partial `PostResultApiTests`/`PostingApiTests`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 651, Api 118 | done | No production code changed. The fake echoes what was sent; T-609 compares a real SDK response (checklist item added) |
| T-606 | POST /batches/{id}/undo | FR-13 | `Core/QbXml/QbTxnDelete.cs`, `Core/Pipeline/BatchUndo.cs`, `Api/Endpoints/BatchEndpoints.cs`, `Api/Program.cs`; fixtures `qbxml/txn-del.golden.xml`, `qbxml/txn-del-response.xml`; tests `Core.Tests/QbXml/QbTxnDeleteTests.cs` (7), `Core.Tests/Pipeline/BatchUndoTests.cs` (8), `Api.Tests/Api/UndoApiTests.cs` (8) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 666, Api 126 | done | Runs as exclusive work on the job worker. Job goes `undone` only when every entry of its current batch is undone. A forced re-run after undo posts the lines again (undone entries no longer count for G4). New gap Q-38 |
| T-607 | GET /health/quickbooks | FR-16 | `Core/QbXml/QbHostQuery.cs`, `Core/Pipeline/QbHealth.cs`, `Api/Endpoints/HealthEndpoints.cs`, `Api/Program.cs`; fixtures `qbxml/host-query.golden.xml`, `qbxml/host-response.xml`; tests `Core.Tests/Pipeline/QbHealthTests.cs` (7), `Api.Tests/Api/QuickBooksHealthApiTests.cs` (5) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 673, Api 131 | done | No API key (like `/health/hermes`). Not queued behind jobs, but shares the gateway lock. New gap Q-39 |
| T-608 | Api tests: full post, refused line → partial, hang → busy, undo, sync-lists | §15 | tests `Api.Tests/Api/QuickBooksFlowApiTests.cs` (2: the T-609 order health → sync-lists → dry run → post → undo; `QuickBooks:Fake=true` through the real DI switch with no test gateway) | Gap check against §15: full post `PostingApiTests`/`PostResultApiTests`/`QuickBooksFlowApiTests`, refused line → partial `PostingApiTests.Should_HoldRefusedLineWithSdkMessage_When_QuickBooksRejectsIt`, hang → busy `QbConnectionTests.Should_EndPartialWithQuickBooksBusy_When_PostingCallHangs` + `QuickBooksHealthApiTests`, undo `UndoApiTests` (8), sync-lists `SyncListsApiTests` (6), G4 `DuplicateCheckApiTests` (6), retry/backup `PostingSafetyApiTests` (9); `dotnet build -warnaserror` 0 warnings; `dotnet test` 3 consecutive green runs → Core 673, Api 133 | done | Linux run still open |
| T-609 | **[server]** Manual verification on a COPY of the company file: health → sync-lists → dry run → post → undo | §15 | `scripts/qb-server-check.ps1` (runs each step against the running API; `post`/`undo` need `-ConfirmCopy`; key from `QBAUTOPOST__Api__ApiKey`, never printed) | Prepared against fakes: the same order passes in `QuickBooksFlowApiTests`; the script was run by hand against a local host with `QuickBooks:Fake=true` (health 200, sync, undo 404, post refused without `-ConfirmCopy`). Human checklist below | ready-for-human | Needs the QuickBooks server; see Q-34…Q-39 for the choices to confirm there |

T-609 checklist (human):
- [ ] Before starting: `Company:FilePath` points at a **copy** of the company file; QuickBooks is open on that copy in the same Windows session as the API; `QuickBooks:Fake` is not set
- [ ] `.\scripts\qb-server-check.ps1 -Step health` → ok (certificate dialog answered "Yes, always"; user HermesBridge). If it fails with "not registered", the API bitness does not match QuickBooks
- [ ] `-Step sync` → counts plausible; `missingInRules` empty after fixing rules.json; `qb-audit/sync-lists.response.qbxml` looks like a real SDK answer
- [ ] `-Step dryrun -Folder <real folder>` → `ready`; `analysis.json` reviewed
- [ ] `-Step post -JobId <id> -ConfirmCopy` → `posted` or `partial` as expected; transactions visible in QuickBooks with the right account, payee, date, amount and memo
- [ ] Posting sends `query-1…n.qbxml` first: check the `query-*.response.qbxml` files show status 0 or 1 (confirms `DepositQuery` accepts `AccountFilter`, Q-36)
- [ ] `response.qbxml`: each `*AddRs` has `TxnID`, `EditSequence` and `Amount`/`DepositTotal` equal to the statement (G5, T-605)
- [ ] Re-run the same folder with `force=true` and post again → every line skipped `already-posted` (G4 ledger check)
- [ ] G4 live check: on the copy, enter one statement line by hand (same account, date, amount, payee), then dry run and post a folder that contains it → that line is skipped `already-in-quickbooks`; with the date moved by 1–3 days it is held `possible-duplicate`
- [ ] `-Step undo -BatchId '<id>#<n>' -ConfirmCopy` → all deleted; `undo-*.response.qbxml` shows status 0 per `TxnDelRs`; transactions gone in QuickBooks
- [ ] With QuickBooks closed: `-Step health` → 503 with a clear message, and a post ends `partial` "nothing posted"
- [ ] COM constants confirmed (`OpenConnection2` local = 1, `BeginSession` DoNotCare = 2, `QbSession.cs`)
- [x] Bitness confirmed (x64/x86) and recorded here: **x64** — settled on the server 2026-09-20: the app ran as an x64 process, `QBXMLRP2.RequestProcessor` was created and `GET /health/quickbooks` answered `ok: true`, `"Intuit QuickBooks Enterprise Solutions 24.0 34.0"`, company file `D:\Accountexx Data\...\Tropicana Properties LLC.qbw`. No x86 build needed

## M7 — Rules, logging, hardening

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-701 | POST /rules/alias, /rules/account; validation vs qb-lists; hot reload | FR-14 | `Core/Mapping/RulesEditor.cs` (`AliasKind`, `RuleChange`, `RuleValidationException`), `Core/Mapping/Rules.cs` (`Load` via `AtomicFile.ReadAllText`), `Api/Endpoints/RulesEndpoints.cs`, `Api/Program.cs`; tests `Core.Tests/Mapping/RulesEditorTests.cs` (26), `Api.Tests/Api/RulesApiTests.cs` (13) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 699, Api 146; `RulesApiTests.Should_UseTaughtRulesInNextJob_When_RulesChangeBetweenJobs` (sample job: 2 held → teach 3 rules → post → `posted`, 0 held) | done | Hot reload = rules are read once per run (analysis and re-post), so no watcher. Writes are serialised by a process-wide lock (not the job worker, so teaching never waits for a running job) and atomic. New gap Q-40 (comments lost on rewrite, names checked too, 3-character fragments, 500 on a broken file) |
| T-702 | Serilog console + rolling file; correlation id; secret scrubbing | §14 | `Api/QbAutopost.Api.csproj` (+`Serilog.AspNetCore` 8.0.1, `Serilog.Sinks.File` 5.0.0, both on the allowed list), `Api/Logging/{LoggingSetup,SecretScrubber,ScrubbingSink,JobIdEnricher}.cs`, `Api/Program.cs`, `Api/appsettings.json` (`Logging` → `Serilog:MinimumLevel`); tests `Api.Tests/Logging/SecretScrubberTests.cs` (19), `Api.Tests/Api/LoggingApiTests.cs` (5) | `dotnet build -warnaserror` 0 warnings; `dotnet test` 2 consecutive green runs → Core 699, Api 170. Manual: `dotnet run` (Development, `QuickBooks:Fake=true`, `QBAUTOPOST__Hermes__ApiKey` set) → console and `data/logs/qbautopost-20260917.log` show `[-]` for host lines and `[qb-sync-lists]` for worker lines; the key does not appear in the file | done | File `Paths:Logs/qbautopost-yyyyMMdd.log`, daily, 31 kept, `shared: true`. New gap Q-41 |
| T-703 | Backup-age guard config + failed(backup-too-old) | FR-11 | code from T-604 (`Core/Gates/BackupGuard.cs`, `Api/Configuration/AppSettings.cs`); tests `Core.Tests/Gates/BackupGuardTests.cs` (+4: exactly at the limit, non-default limit, limit 0 or less), `Api.Tests/Api/PostingSafetyApiTests.cs` (+2: `QuickBooks:BackupMaxAgeHours` from configuration, `failed`/`backup-too-old` persisted in `status.json`), `Api.Tests/Configuration/AppSettingsTests.cs` (5: defaults `""`/36, relative folder resolved against the content root, absolute kept, blank kept) | Reviewed against FR-11 with the T-604 tests (`BackupGuardTests`, `PostingSafetyApiTests` backup cases); `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 703, Api 177 | done | No production change. `BackupMaxAgeHours` ≤ 0 refuses every post while a folder is set (posts less; Q-37 covers the rest) |
| T-704 | GET /jobs?status= | §6 | code from T-104 (`Api/Endpoints/JobEndpoints.cs` `ListJobs`, `JobView.cs` `JobSummary`, `Api/Jobs/JobStore.cs` index + `status.json` scan); tests `Api.Tests/Api/JobsApiTests.cs` (+3: upper-case filter, empty result, 401 without key), `RecoveryTests.cs` (+2: a new host lists known jobs from their `status.json` and filters them; recovered `posting` job listed under `partial`), `UndoApiTests.cs` (+1: undone job listed under `undone` with its batch id) | Reviewed against §6 with `JobsApiTests.Should_ListJobsAndFilterByStatus_When_Asked` and `Should_Return400_When_StatusFilterIsUnknown`; `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 703, Api 183 | done | No production change. Filter parses any `JobStatus` ignoring case (numbers refused, 400) |
| T-705 | docs/runbook.md | plan M7 | `docs/runbook.md` (install and bitness, folders, publish, configuration and secrets, rules, first run with the certificate dialog, daily operation, every hold/skip code with the operator action, teaching rules, undo, troubleshooting: unreachable/bitness, dialogs, busy/no response and interrupted jobs, logs, state backup) | Reviewed against `HoldReasons.cs`, `QbSession.cs` messages, `JobAdmission.cs`, `StartupRecovery.cs`, `BatchUndo.cs`, Q-14/Q-18/Q-34…Q-41 and the T-609 checklist; no code change, `dotnet test` unchanged (Core 703, Api 183) | done | Refers to `deploy/README-hermes.md`, `deploy/start-all.ps1`, `deploy/install-task.ps1`, which M8 (T-801/T-802) adds. A person should read it once on the server (plan §6: "lets someone other than the author operate it") |

## M8 — Deploy

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-801 | deploy/hermes docker-compose.yml, .env.example, README-hermes.md (WSL 2 + Docker Engine; Docker Desktop alt) | §4 | `deploy/hermes/docker-compose.yml` (published `127.0.0.1:8642` only; `HERMES_IMAGE`, `HERMES_CONTAINER_PORT`, `HERMES_DATA_PATH`, `API_SERVER_KEY` required with `:?`; optional `provider.env`; `mem_limit`; log rotation), `deploy/hermes/.env.example`, `deploy/hermes/provider.env.example` (placeholders only), `deploy/README-hermes.md` (WSL 2 + Docker Engine on Server, Docker Desktop on 10/11, `.wslconfig` cap, key hand-over, checks), `.gitignore` (+`deploy/hermes/provider.env`); tests `Core.Tests/Architecture/DeployFilesTests.cs` (10), `Core.Tests/TestSupport/RepoRoot.cs` | compose config valid: `docker compose -f deploy/hermes/docker-compose.yml --env-file deploy/hermes/.env.example config` → exit 0, port `host_ip: 127.0.0.1`, published 8642; with an empty env file → refused "required variable … is missing". `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 713, Api 183 | done | The spec never names the Hermes image, its in-container port/variable names or data folder: all required from `.env`, no guessed defaults (new gap Q-42). Never started a container (no image known) |
| T-802 | deploy/start-all.ps1, install-task.ps1 | §4 | `deploy/start-all.ps1` (Docker up in WSL or Docker Desktop → `docker compose up -d` → wait for TCP `127.0.0.1:8642` → start the API once → wait for `GET /health/hermes` 200; exit 0/1/2; log `Paths:Logs`-style `start-all-yyyyMMdd.log`; loopback-only `-ApiBaseUrl`; `-WhatIf`), `deploy/install-task.ps1` (logon trigger + 60 s delay, `-LogonType Interactive`, `-RunLevel Limited`, no password, no time limit, `IgnoreNew`, `-Unregister`, `-WhatIf`), `docs/runbook.md` §2.4; tests `Core.Tests/Architecture/DeployScriptsTests.cs` (13: ASCII only, `-WhatIf`, no keys, interactive logon task, never a service, Hermes checked before the API, loopback only) | Prepared on the dev box: both scripts parse with 0 errors (`Parser::ParseFile`); `start-all.ps1 -WhatIf` → exit 0 with the planned steps; `-ApiBaseUrl http://10.0.0.5:5080` → exit 1; `install-task.ps1 -WhatIf` → prints the task, registers nothing; `-Unregister` of a missing task → exit 0. `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 726, Api 183. **Dry run on server pending** (checklist below) | ready-for-human | Plan verification is "dry run on server". `/health/hermes` is served by the API itself, so "wait for Hermes before the API" waits for the Hermes port, and `/health/hermes` is checked after the API starts. New gap Q-43 |
| T-803 | **[server]** Shadow week: 5 real folders dry run, diff vs manual, rules added | plan M8 | `scripts/shadow-diff.ps1` (read-only: `analysis.json` vs a hand-made CSV `Date,Amount,Payee,Account[,Source]`; statuses agree / account-differs / payee-differs / not-in-manual / skipped-but-entered / held / held-not-entered / manual-only; writes `output/shadow-diff.csv`; exit 0 none, 3 disagreements, 1 bad input); tests `Core.Tests/Architecture/DeployScriptsTests.cs` (+4: ASCII, offline, no key) | Prepared against the golden sample (`tests/fixtures/output/sample-analysis.golden.json`) on the dev box: a matching CSV (mixed date formats, `$`, thousands separator, case/space differences) → 9 agree, 1 held, 1 held-not-entered, exit 0; a CSV with a different account, payee, an entered card payment, missing and extra rows → each status reported, exit 3; missing file → exit 1. `dotnet test` → Core 730, Api 183. Shadow run itself pending (checklist and log below) | ready-for-human | Needs real folders and the person who enters them by hand. The CSV layout is this helper's own choice (spec/plan name none); matching is date + exact amount, payee breaks ties |
| T-804 | **[server]** Go-live one company, then all | plan M8 | tests `Api.Tests/Api/GoLiveConfigApiTests.cs` (4: shipped `appsettings.json` has `DryRunDefault: true`; omitted `dryRun` stops at `ready` with no QuickBooks writes when true and posts when false; a request's `dryRun: true` wins over `false`), `docs/runbook.md` §2.5 (going live, one company per installation); checklist and go-live log below | Prepared against fakes: `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 730, Api 187. No production change. Go-live itself pending (checklist below) | ready-for-human | "Then all" is open: one installation serves one company and `start-all.ps1` starts one API (by exe name), so several companies on one server need an owner decision (new gap Q-44) |
| T-805 | Detailed operational logging: startup, every API request, job steps, every QuickBooks and Hermes call (owner request 2026-09-19) | §14 | `Api/Logging/StartupLog.cs` (process bitness, Windows session, effective settings, files found/missing), `Api/Logging/RequestLog.cs` (`UseSerilogRequestLogging`, host logger, route `{id}` as jobId, health polls at Debug), `Api/Logging/LoggingHermesClient.cs`, `Api/QuickBooks/LoggingQbGateway.cs` + `QbXmlLogSummary.cs` (request names/counts, per-response status, HRESULT, past-busy-timeout warning; never the body), `Api/Jobs/JobLog.cs` + `JobRunner.cs` (requirement, statements, invoices, held/skipped lines by file:line date amount, G4 changes, posted TxnIDs, batch), endpoint and `ApiKeyMiddleware` logging, `Program.cs` (decorators wired, prompts loaded after build, startup failure logged `[FTL]` and host disposed), `QuickBooks/QbGateway.cs` + `QbSession.cs` (optional `Trace` for each SDK session step, no ticket), `docs/runbook.md` §8.5/§8.6; tests `Api.Tests/QuickBooks/QbXmlLogSummaryTests.cs` (3), `LoggingQbGatewayTests.cs` (7), `Api.Tests/Logging/LoggingHermesClientTests.cs` (3), `Api.Tests/Api/LoggingApiTests.cs` (+6), `TestSupport/ListLogger.cs`; `QbConnectionTests` updated for the extra wrapper | `dotnet build -warnaserror` 0 warnings; `dotnet test` 2 consecutive green runs → Core 730, Api 206. Manual: `dotnet run` (Development, `QuickBooks:Fake=true`) → health, sync-lists, a wrong key and an unknown undo each logged as described in runbook §8.6. No package added. SDK session tracing runs only on the server (T-609) | done | Spec §14 "no full statement text at Information" kept: lines named by file:line/date/amount, descriptions and per-response TxnIDs at Debug (`LoggingApiTests.Should_KeepStatementDescriptionsOutOfInformationLog_When_JobRuns`). Core stays logging-free (no `Microsoft.Extensions.Logging` package): logging is done by Api decorators around `IQbGateway`/`IHermesClient` and from the pipeline results. The Hermes decorator is replaced by `FakeHermesClient` in host tests, so it is covered by unit tests only |
| T-806 | POC without AI: `Hermes:Enabled=false`, POC sample data and package for QuickBooks Enterprise 24 (owner request 2026-09-19) | §9, §15 | `Core/Hermes/DisabledHermesClient.cs` (every call "unavailable", ping not ok), `Api/Configuration/AppSettings.cs` (`Hermes:Enabled`, default true), `Api/Program.cs` (disabled → `RegexSpecReader` + `DisabledHermesClient`, decided from bound settings), `Api/Logging/StartupLog.cs` (warns "Hermes: disabled"); `samples/poc/` (`jobs/2026-09-tropicana` bank CSV 10 lines with a correct running balance + card CSV 6 lines, `rules.json` resolving every line, `tropicana-lists.iif` with the 9 accounts / 7 vendors / 2 customers the rules name, `appsettings.json` with an empty API key, `README-POC.md`); `scripts/build-poc-package.ps1` (self-contained win-x64/x86 publish + POC files → `dist/*.zip`, git-ignored); `scripts/qb-server-check.ps1` (+`-Force`); runbook §3.1 row; tests `Api.Tests/Api/PocModeApiTests.cs` (3), `Core.Tests/Hermes/DisabledHermesClientTests.cs` (2), `TestSupport` `Fixtures.Poc*`, `TempDir.CopyJob` | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 732, Api 209. `PocModeApiTests`: POC job with Hermes off → `posted` 15 / held 0 / skipped 1, G1 ok on both statements, zero model calls; every rules name is in the IIF. Package built (53 MB) and run from a copy with the POC settings and `QuickBooks:Fake=true`: health ok → dryrun `ready` 15/0/1 → post `posted` 15 (7 CheckAdd, 4 CreditCardChargeAdd, 1 CreditCardCreditAdd, 3 DepositAdd, all status 0) → undo 15 deleted | ready-for-human | Needs the server: create the test company, import the IIF, run README-POC.md steps 1–6 (certificate dialog on first health). The IIF import itself and the real SDK path are unverified here. New gap Q-45 |

T-802 checklist (human, on the server, as the auto-logon account):
- [ ] `deploy\hermes\.env` and `provider.env` filled (README-hermes.md §2); `docker compose config --quiet` prints nothing
- [ ] `.\deploy\start-all.ps1 -WhatIf` → exit 0, lists the steps, changes nothing
- [ ] `.\deploy\start-all.ps1` (add `-DockerMode Desktop` on Windows 10/11) → exit 0; `docker port qbautopost-hermes` shows `127.0.0.1:8642` only; `/health/hermes` 200; `logs\start-all-yyyyMMdd.log` written and holds no key
- [ ] Run it again → "The API already runs … not starting another", exit 0
- [ ] Stop the container (`docker compose down`), stop the API, run it with `-HermesWaitSeconds 30 -ApiWaitSeconds 30` and Hermes kept down (e.g. rename `.env`) → exit 1 (compose refused) or 2 (API started, Hermes unhealthy) as documented
- [ ] `.\deploy\install-task.ps1 -WhatIf` → prints the task, registers nothing
- [ ] `.\deploy\install-task.ps1` (elevated) → Task Scheduler shows `QbAutopost`: "Run only when user is logged on", at logon of the account, delay 1 minute, not "highest privileges"
- [ ] Sign out and in (or reboot with auto-logon) → the API and Hermes come up without anyone touching the server; `/health/quickbooks` ok with QuickBooks open
- [ ] The API window is in the same session as QuickBooks (a COM call works: `scripts\qb-server-check.ps1 -Step health`)

T-803 checklist (human, shadow week; `DryRunDefault=true`, nothing is posted):
- [ ] T-609 and T-802 done on the server; `/health/quickbooks` and `/health/hermes` ok; `POST /qb/sync-lists` run, `missingInRules` empty
- [ ] Pick five real job folders (different banks/cards, at least one PDF statement and one with invoices) that a person is entering by hand as usual
- [ ] For each: `scripts\qb-server-check.ps1 -Step dryrun -Folder <folder>` → `ready` (or `failed` with a reason worth a rule/fix)
- [ ] After the manual entry: export the same period's transactions from QuickBooks to a CSV with `Date,Amount,Payee,Account[,Source]` and run `scripts\shadow-diff.ps1 -Analysis <folder>\output\analysis.json -Manual <csv>`
- [ ] For each disagreement decide: tool wrong → teach a rule (`POST /rules/alias`, `POST /rules/account`) or log a defect; person wrong → note it; held line → teach a rule if the answer is clear
- [ ] After teaching, run the dry run again (same `-Step dryrun`; `force=true` is only needed once a job is in the ledger) and re-diff
- [ ] Record each run in the log below; the week ends after **two consecutive runs with zero disagreements** (plan §6)
- [ ] Check `output\hermes\` and the logs of one job hold no keys

T-803 shadow log (human):

| Date | Folder | Lines | Agree | Held | Disagreements | Rules taught / defects | By |
|---|---|---|---|---|---|---|---|
| | | | | | | | |

T-804 checklist (human, go-live; only after T-803 shows two clean runs):
- [ ] Owner answers to the questions that matter before posting are recorded (at least Q-27 re-running T2/T3/T4 at post time, Q-37 COM retry/backup, Q-42 Hermes image)
- [ ] A fresh QuickBooks backup exists in `QuickBooks:BackupFolder` (younger than `BackupMaxAgeHours`); the ledger and `rules.json` are backed up (runbook §9)
- [ ] For the first company only: `DryRunDefault=false` in its `appsettings.json`, API restarted; other companies stay on `true`
- [ ] First real folder: dry run → review `analysis.json` → post → check every transaction in QuickBooks; keep the batch id for undo
- [ ] Undo drill on one real batch if the owner agrees (`POST /batches/{id}/undo`), then re-post
- [ ] Monitor a week: every job `posted` or `partial` with understood holds; no `failed` without a reason; no duplicates in QuickBooks (G4); logs clean
- [ ] Then switch the remaining companies one at a time and record each below

T-804 go-live log (human):

| Date | Company | Jobs | Posted lines | Held | Problems | By |
|---|---|---|---|---|---|---|
| | | | | | | |

## M9 — API v1 conversion

Contract: `docs/spec-api-v1.md`. Owner decisions (2026-09-22): **D-1** keep the job folder *and* add direct JSON rows ·
**D-2** two validation endpoints (company file, transaction data) · **D-3** `/api/v1` + an OpenAPI document, reference
UI is **Scalar, not Swagger** · **D-4** remote callers, so per-caller keys, TLS, rate limits, audit.
Every row starts `todo`; "Files" lists the planned paths, "Verified by" is filled in when the row is done.

| ID | Task | Spec | Files | Verified by | Status | Notes |
|---|---|---|---|---|---|---|
| T-901 | `/api/v1` route group; legacy flat routes behind `Api:LegacyRoutes`; content root → `AppContext.BaseDirectory` | api-v1 §2, §9, §12 | `Api/Endpoints/ApiRoutes.cs` (new: prefix + one `IsHealth` for both prefixes), `Api/Endpoints/LegacyRouteLog.cs` (new: once per route per hour, `IClock`), `Api/Configuration/ContentRoot.cs` (new), `Api/Configuration/AppSettings.cs` (`Api:LegacyRoutes`), `Api/Program.cs` (`MapAll` into the group and flat, endpoint filter, `WebApplicationOptions.ContentRootPath`), `Api/Security/ApiKeyMiddleware.cs`, `Api/Logging/RequestLog.cs`, `Api/Logging/StartupLog.cs`, `appsettings.json`; tests `Api.Tests/Api/RouteVersioningTests.cs` (12), `Api.Tests/Configuration/ContentRootTests.cs` (9), `Api.Tests/Routing/LegacyRouteLogTests.cs` (4) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 732, Api 235 (was 210), 2 consecutive green runs. RED first: route tests 7/10 failing on 404, `LegacyRouteLog` CS0246, `ContentRoot` 4/13, warning tests 0 log lines. Evidence: `docs/testing/T-901.tdd.md` | done | Branch `m9-api-v1`, commits `59cc4ea` (RED), `768fd18`, `3db1eed`. Closes session-13 server defect 1 in decision logic only — that a published exe started elsewhere now finds `appsettings.json` still needs the server. The test namespace `…Tests.Endpoints` had to become `…Tests.Routing`: it shadowed the production `Endpoints.JobView` in 4 existing test files. The legacy warning names the route, not the caller — client identity arrives at T-910 |
| T-902 | `GET /api/v1/health` + `/health/ready` | FR-A-1, FR-A-2 | `Api/Health/Readiness.cs` (new), `Api/Health/AppHealth.cs` (new), `Api/Endpoints/HealthEndpoints.cs` (2 routes), `Api/Jobs/JobQueue.cs` (`Depth`), `Api/Program.cs` (DI); tests `Api.Tests/HealthChecks/ReadinessChecksTests.cs` (7), `Api.Tests/Api/AppHealthApiTests.cs` (7) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 732, Api 249 (was 235), 2 consecutive green runs. RED first: `ReadinessChecksTests` CS0234 (`QbAutopost.Api.Health` did not exist), `AppHealthApiTests` 7/7 on 404 then 500. Evidence: `docs/testing/T-902.tdd.md` | done | Commit `957f896`. Zero gateway/Hermes calls asserted against the recording fakes (`Gateway.Requests`, `Hermes.Calls`), not a throwing fake — same guarantee, and it also proves no call was attempted and swallowed. Two bugs found by the tests: `ChannelReader.Count` throws `NotSupportedException` on a single-reader unbounded channel (now an `Interlocked` counter); camelCase renders `QuickBooksGateway` as `quickBooksGateway`, so FR-A-1's `quickbooksGateway` is pinned with `JsonPropertyName`. A missing `qb-lists.json` is a **warning**, not an error: a job every rule resolves still posts |
| T-903 | `IQbSdkProbe` + `QbSdkProbe` (COM) + `GET /api/v1/health/sdk` | FR-A-3 | `Core/Abstractions/IQbSdkProbe.cs` (new: interface + `QbSdkInfo`/`QbSdkRequestProcessor`/`QbSdkProcess`), `QuickBooks/QbSdkProbe.cs` (new, COM, STA), `QuickBooks/PeBitness.cs` (new), `Api/QuickBooks/UnavailableQbSdkProbe.cs` (new), `Api/Endpoints/HealthEndpoints.cs`, `Api/Program.cs`; tests `Api.Tests/Api/SdkHealthApiTests.cs` (5), `Api.Tests/QuickBooks/PeBitnessTests.cs` (11), `Api.Tests/TestSupport/FakeQbSdkProbe.cs`, `ApiFactory.cs`, `FakeQbGateway.cs` (`CompanyFileCalls`) | `dotnet build -warnaserror` 0 warnings; `dotnet test` → Core 732, Api 265 (was 249), 2 consecutive green runs. RED first: CS0246 ×4 for `IQbSdkProbe`/`QbSdkInfo`. Evidence: `docs/testing/T-903.tdd.md`. **The COM probe itself is unverified — server checklist below** | ready-for-human | Commit `a9cab4e`. `CoreIsolationTests` failed on the ProgId string `QBXMLRP2…` placed in Core — the rule-2 guard working; the ProgId is now passed in as data from `QbSession.ProgId`. `ApiFactory` substitutes `FakeQbSdkProbe`, without which the host would pick the real COM probe on a Windows dev box (rule 1). Never calls `BeginSession`, so it answers with QuickBooks closed. `bitnessMatch:false` → not ok (the T-609 failure). **Supported qbXML versions are not enumerated** — reading them needs a session ticket, which FR-A-3 forbids; only the configured version is echoed (new gap Q-55) |
| T-904 | `POST /api/v1/quickbooks/connection/test`; gateway-wait log line; `ConnectionTestTimeoutSeconds` | FR-A-4, §8 | `Core/Pipeline/QbConnectionCheck.cs` (new), `Core/Gateway/ResilientQbGateway.cs` (`Waited` event, `BusyTimeout`, read-only calls with their own timeout), `Api/Endpoints/QuickBooksEndpoints.cs`, `Api/Configuration/AppSettings.cs` (`ConnectionTestTimeoutSeconds`, `AllowCompanyFileOverride`, `AllowedCompanyFolders`, `MaxConnectionTestTimeoutSeconds`), `Api/Program.cs` (one gateway registered twice + wait log); tests `Api.Tests/QuickBooks/QbConnectionCheckTests.cs` (5), `Api.Tests/Api/ConnectionTestApiTests.cs` (6) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 732, Api 276 (was 265), 2 consecutive green runs. RED first: `QbConnectionCheck` CS0246, then the endpoint tests 404. Evidence: `docs/testing/T-904.tdd.md` | done | Commit `ee1b551`. Closes **server defect 2**: a step past the busy timeout is `ok:true` with the "slow: a QuickBooks dialog..." hint (the 101 s `BeginSession` now reads as a slow success, not `quickbooks-busy`), and a lock wait over 1 s logs `Waiting for QuickBooks: {WaitMs} ms behind another call`. Named `QbConnectionCheck`, not `QbConnectionTest`, to stay clear of the existing `QbConnection`/`QbConnectionTests`. `ProcessWithTimeoutAsync` **refuses anything that is not read-only**, so FR-11's rule that a write keeps the busy timeout cannot be widened by accident. One gateway instance is registered twice (interface + concrete) - two instances would mean two locks. **Spec amended**: FR-A-4's six illustrative steps became the three that are measurable (one gateway call = one SDK session); new gap Q-56 |
| T-905 | `POST /api/v1/quickbooks/company-file/validate` | FR-A-5 | `Core/Pipeline/CompanyFileValidator.cs` (new), `Api/Endpoints/QuickBooksEndpoints.cs` (route + `CompanyFileRequest`), `Api/Program.cs` (DI); tests `Api.Tests/QuickBooks/CompanyFileValidatorTests.cs` (10), `Api.Tests/Api/CompanyFileApiTests.cs` (6) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 732, Api 292 (was 276), 2 consecutive green runs. RED first: CS0246 x3, then endpoint 404s. Evidence: `docs/testing/T-905.tdd.md` | done | Commit `1bb151f`. Checks: configured, pathShape, exists, readable, openInQuickBooks, backupFreshness, listsSynced. `openInQuickBooks` is an **error**, not a warning - a different company open is the one misconfiguration that silently posts into the wrong books. `backupFreshness` is a **warning** and reuses FR-11's own `BackupGuard`, so validating and posting can never disagree and this call cannot bless a stale backup. Read-only by construction: the unit stubs throw on `ProcessAsync` and the host test asserts `Gateway.Requests` stays empty. 200 / 422 with the same body. **FR-A-5's `companyName` check is not implemented** - it needs a `CompanyQueryRq` whose response element names cannot be verified here (new gap Q-57) |
| T-906 | Direct-post request model, fingerprints, control total, mapping reuse | FR-A-8, FR-A-9, spec §7 | `Core/Api/DirectRequest.cs` (new: `DirectRow`, `DirectRequest`, `DirectLimits`, `DirectLine`, `DirectRowIssue`, `DirectReadResult`), `Core/Api/DirectRequestReader.cs` (new); tests `Core.Tests/Api/DirectRequestReaderTests.cs` (21), `Core.Tests/Api/DirectAndFolderPathsAgreeTests.cs` (4) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 757 (was 732), Api 292, 2 consecutive green runs. RED first: CS0234/CS0246 x4. Evidence: `docs/testing/T-906.tdd.md` | done | Commit `6d4505f`. Rows become the **same `StatementLine`** a statement produces, so the FR-6 mapper, G4, qbXML and G5 are shared rather than reimplemented. `SourceKind`/`Direction` are **derived from `kind`**, never taken from the caller, so two callers describing one movement fingerprint alike and G4 still catches the repeat. Refuse-vs-hold split is deliberate: control-total mismatch/missing, no rows, too many rows, non-positive or over-precise amount, unsafe reference -> **whole batch refused** and nothing read out of it (rule 3, no amount ever adjusted); amount over cap, date outside window, missing account -> **that row held**, so one bad line cannot fail a batch of hundreds. The control total is checked over **every submitted row including held ones** - it describes what the caller sent, not what survived. `DirectAndFolderPathsAgreeTests` pins the two-entrance property: same fingerprint, byte-identical qbXML |
| T-907 | `POST /api/v1/quickbooks/transactions/validate`; `DirectPlanner` (map → G3 → G4 → qbXML, offline) | FR-A-6 | `Core/Api/DirectPlan.cs` (new: `DirectOutcome`, `DirectPlanRow`, `DirectUnknownNames`, `DirectPlan`), `Core/Api/DirectPlanner.cs` (new), `Core/Api/DirectRequest.cs` (`DirectLine.Account`), `Core/Api/DirectRequestReader.cs` (passes it), `Api/Endpoints/ValidationResponse.cs` (new: the FR-A-6 body), `Api/Endpoints/QuickBooksEndpoints.cs` (route + handler), `Api/Configuration/AppSettings.cs` (`Api:MaxTransactionsPerRequest`, `QuickBooks:MaxLineAmount`, `QuickBooks:AllowedDateWindow`), `Api/Program.cs` (DI), `appsettings.json`; tests `Core.Tests/Api/DirectPlannerTests.cs` (17), `Api.Tests/Api/TransactionValidateApiTests.cs` (12) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 774 (was 757), Api 304 (was 292), 2 consecutive green runs. RED first: `CS0246 DirectPlan`, then the endpoint tests **11 of 12 failing on 404**. Evidence: `docs/testing/T-907.tdd.md` | done | Offline by construction, asserted on the recording fakes: `Gateway.Requests` and `Hermes.Calls` both stay empty. The caller's `account` is injected into a **scoped copy of the rules** per (kind, last4) group, so `Mapper` decides exactly as it would for a statement instead of being post-processed - without that, a row whose last four no rule knows loses payee and tier resolution too. One last four sent as two accounts holds both rows (`conflicting-last4`). A control-total mismatch is **400** (§6.1) while a gate verdict is **422 with the same body** as the 200; `skipped` includes `duplicates` so the four counts add up to `submitted`. Tiers 3-4 do not run (offline), so a plan may be gloomier than the post but never rosier. **The qbXML body is withheld even for `?includeQbXml=true`** until scopes exist (T-910), with a note saying so. New gap Q-58: the caller's payee is honoured on a numbered check, where FR-6 leaves it blank |
| T-908 | `POST /api/v1/quickbooks/transactions`; batch evidence folder; `GET /api/v1/batches/{id}` | FR-A-8, FR-A-10, FR-A-11 | `Core/Api/DirectPostResult.cs` (new: `DirectPostedRow`, `DirectPostCounts`, `DirectPostTotals`, `DirectPostResult`), `Core/Store/ApiBatchStore.cs` (new: the five-file folder, attempt counting, traversal refusal), `Api/Jobs/DirectPostRunner.cs` (new), `Api/Endpoints/QuickBooksEndpoints.cs` (post route, 202 path), `Api/Endpoints/BatchEndpoints.cs` (`GET /batches/{id}`), `Api/Configuration/AppSettings.cs` (`Api:SyncPostTimeoutSeconds`, `Paths:ApiBatches`), `Api/Program.cs` (DI), `appsettings.json`, `Api.Tests/TestSupport/ApiFactory.cs` (pins `Paths:ApiBatches`); tests `Core.Tests/Store/ApiBatchStoreTests.cs` (13), `Api.Tests/Api/DirectPostApiTests.cs` (16) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 787 (was 774), Api 320 (was 304), 2 consecutive green runs. RED first: `CS0246` x2 for `ApiBatchStore`/`DirectPostResult`, then the endpoint tests **15 of 16 failing on 404**. Evidence: `docs/testing/T-908.tdd.md` | done | **No parallel store**: the ledger keeps being the one record of what posted, so `BatchUndo` and the undo endpoint needed **no change at all** and FR-13 works on a direct batch (asserted). `ApiBatchStore` holds only evidence - `request.json` is written **before** the first qbXML leaves the process, proven by a test where QuickBooks throws and the file is there anyway. A refused request gets no batch id and no folder. `Unavailable` is an explicit field, not a parsed message, so §2's 503 (QuickBooks unreachable, backup guard) never depends on error text; the body is still the batch. The 202 path reuses `RunExclusiveAsync`, which already runs work to the end when the caller stops waiting, so the timeout bounds only the wait and `Prefer: respond-async` is the same path with a zero budget. **Two defects found by the tests**: the traversal guard used `Path.GetInvalidFileNameChars()`, which permits `\` and `:` on Linux and would have failed CI (rule 1) - now an ASCII allow-list; and adding `Paths:ApiBatches` to the shipped `appsettings.json` made 5 tests write batches to the **real** `C:\qb-autopost\api-batches` (trap #4), fixed by pinning it in `ApiFactory`, debris deleted, 0 entries after a full run. New gap Q-59: FR-A-8's "post a `ready` batch later" route does not exist |
| T-909 | `Idempotency-Key` store and middleware | FR-A-12 | `Core/Store/IdempotencyStore.cs` (new: `IdempotencyVerdict`, `IdempotencyEntry`, `IdempotencyClaim`, `IdempotencyLog`, the store), `Api/Security/IdempotencyMiddleware.cs` (new), `Api/Endpoints/ApiRoutes.cs` (`IsIdempotent`), `Api/Configuration/AppSettings.cs` (`Api:IdempotencyRetentionDays`), `Api/Program.cs` (DI + middleware behind the key check), `appsettings.json`; tests `Core.Tests/Store/IdempotencyStoreTests.cs` (12), `Api.Tests/Api/IdempotencyApiTests.cs` (9) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 799 (was 787), Api 329 (was 320), 2 consecutive green runs. RED first: `CS0246 IdempotencyStore`, then the host tests **7 of 10 failing**. Evidence: `docs/testing/T-909.tdd.md` | done | Replay returns the stored body with `Idempotency-Replayed: true` and **exactly one write reaches QuickBooks** (asserted); a different body under one key is `409 idempotency-key-reused`; a retry while the first is wedged is `409 idempotency-in-progress` with `Retry-After: 5`. Undo is covered as FR-A-12 requires - two undos with one key delete once - while **validate deliberately is not**: replaying a validate would hide a change in the rules or the ledger, which is what a validate exists to reveal. Keys are scoped by (key, route, client), so a caller numbering requests 1,2,3 cannot make an undo collide with a post; `clientId` is already in the key and stays null until T-910, so adding clients changes no behaviour. **A non-2xx frees the key** (new gap Q-60): holding it would lock a caller out of their own key over a typo, and would block the very retry a 503 exists to invite - safe because idempotency is the convenience layer while G4 and the ledger are the safety layer. The replayed answer keeps its **original** status code rather than §6.5's blanket 200, since replaying a 202 as 200 would claim a batch had finished. Three of the four remaining REDs were **my tests, not the code**: an empty header is indistinguishable from an absent one over HTTP, a fixture nested a request where a row belonged, and one test read the response body twice |
| T-910 | `clients.json`, per-caller keys, scopes, rotation; `scripts/new-api-client.ps1` | FR-A-13, §2.2 | `Core/Security/ApiClient.cs` (new: `ApiScopes`, `ApiClient`, `ApiClientList`), `Core/Security/ApiClientStore.cs` (new: salted hashing, constant-time compare, `ApiClientResolver` with rotation), `Api/Security/ApiKeyMiddleware.cs` (rewritten: resolve caller, CIDR, scope), `Api/Endpoints/ApiRoutes.cs` (`ScopeFor`), `Api/Endpoints/ValidationResponse.cs` (the qbXML body, scope-gated), `Api/Endpoints/QuickBooksEndpoints.cs` (`qb:debug` check), `Api/Configuration/AppSettings.cs` (`Api:AllowLegacyKey`, `Paths:Clients`), `Api/Program.cs` (DI + fail-closed startup), `appsettings.json`, `.gitignore`, `scripts/new-api-client.ps1` (new); tests `Core.Tests/Security/ApiClientsTests.cs` (19), `Api.Tests/Api/ClientScopeApiTests.cs` (12), `Api.Tests/Api/AuthTests.cs` (+2), `Core.Tests/Architecture/DeployScriptsTests.cs` (+2), and 18 existing tests moved (see below) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 820 (was 799), Api 344 (was 329), 2 consecutive green runs. RED first: `CS0234` for the `Security` namespace, then the scope tests **8 of 12 failing**. Evidence: `docs/testing/T-910.tdd.md` | done | Only a salted hash is ever stored - asserted by grepping the written file for the plaintext. A read-only client posting is **403** naming the scope; an unknown key **401**; a revoked client 401 on its next request, because the list is re-read per request. Rotation keeps the old key working inside its window. Scopes map **by effect, not by URL**, and an unmapped route falls through to `admin`, so a new endpoint is closed until someone decides otherwise. `?includeQbXml=true` is now gated on `qb:debug`, which **closes T-907's recorded SPEC-GAP**. Startup refuses when no enabled client and no usable shared key exist, and warns whenever the all-scopes legacy key is accepted alongside real clients. `new-api-client.ps1` reimplements the hash in PowerShell, so a recorded (key, salt, hash) triple from one run is asserted against `ApiClientStore.Matches` - otherwise a divergence would silently reject every key it issues. **Behaviour change**: api-v1 §2.2 narrows spec.md §6, so only `/health` and `/health/ready` stay key-free; `/health/sdk`, `/health/quickbooks` and `/health/hermes` now need `health:read`, because they open a QuickBooks session or probe COM and D-4 makes them remotely reachable. That moved 18 existing tests, each read and changed deliberately (new gap Q-61: tell the operator before their monitoring gets 401) |
| T-911 | TLS, HSTS/CORS/headers, rate limiting, body and field limits, path allow-lists | FR-A-14, FR-A-15, FR-A-17 | `Api/Security/TransportGuard.cs` (new), `TlsCertificate.cs` (new), `SecurityHeadersMiddleware.cs` (new), `RateLimitPolicy.cs` (new), `AuthBrake.cs` (new), `RequestSizeMiddleware.cs` (new), `ApiKeyMiddleware.cs` (the brake), `Core/Security/PathAllowList.cs` (new), `Core/Api/DirectRequestReader.cs` (field bounds), `Api/Jobs/JobAdmission.cs` (job-root allow-list), `Api/Endpoints/QuickBooksEndpoints.cs` (company-file allow-list), `Api/Configuration/AppSettings.cs` (`TlsSettings`, `CorsSettings`, `RateLimitSettings`, `AllowInsecureRemote`, `MaxRequestBodyBytes`, `Paths:AllowedJobRoots`), `Api/Program.cs`, `appsettings.json`; tests `Api.Tests/Hardening/TransportTests.cs` (21), `RateLimitTests.cs` (19), `AuthBrakeTests.cs` (8), `InputLimitTests.cs` (12), `Core.Tests/Security/PathAllowListTests.cs` (14), `ApiFactory` (`WithSettings`, limiter off) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 834 (was 820), Api 405 (was 344), 2 consecutive green runs. RED first: **26 errors** (`TransportGuard`, `Tls`, `Cors`, `AllowInsecureRemote`, `AllowedJobRoots`), then **22** (`RateLimitBucket`, `AuthBrake`), then Core `CS0103 PathAllowList` x12 and the host **7 of 12 failing**. Evidence: `docs/testing/T-911.tdd.md` | done | Tests live in `Api.Tests/Hardening`, not `…/Security` as this row first planned: that namespace shadows the production `QbAutopost.Api.Security` the tests import (trap 2). **No existing test was changed or deleted** - the limiter ships off in `ApiFactory` (trap 14) and the allow-list ships empty, so the 1164 earlier tests still pin the same behaviour. Judgement calls: an empty `Paths:AllowedJobRoots` means *unconfigured*, so startup **refuses a non-loopback bind** while it is empty rather than refusing every job or silently allowing any path (SPEC-GAP, **Q-63**); `POST /jobs` draws on the 10/min posting bucket though its scope is `jobs:write`, because it ends in money moving; the brute-force brake counts **unrecognised keys only**, so a 403 for a missing scope cannot lock a legitimate client out of its own API; over-long fields are a 400 for the batch, not a held row, because a 4 KB memo is a broken mapping rather than a caller meaning what they said. `Api:Tls:PfxPassword` is deliberately **not** in `appsettings.json` (environment only) though §12 lists it. Honest limits in evidence §8: no TLS handshake, HSTS header, `AddServerHeader` or production rate limit has ever been exercised - all four need the server. One unreproduced single-test failure recorded as **Q-62** |
| T-912 | Audit trail + `requestId`/`clientId` on every log line | FR-A-16, §2.6, §8 | `Api/Logging/RequestId.cs` (new), `RequestIdMiddleware.cs` (new), `AuditLog.cs` (new: `AuditCounts`, `AuditEntry`, the writer), `AuditMiddleware.cs` (new), `RequestContextEnricher.cs` (new), `LoggingSetup.cs` (template + enricher), `RequestLog.cs` (client on the request line), `Api/Endpoints/ApiRoutes.cs` (`IsMutating`), `Api/Security/ApiKeyMiddleware.cs` (`ClientIdProperty`, `LogContext` push), `Api/Configuration/AppSettings.cs` (`Api:AuditRetentionDays`), `Api/Program.cs` (DI, problem-detail extension, two middlewares), `appsettings.json`; tests `Api.Tests/Logging/RequestIdTests.cs` (19), `AuditLogTests.cs` (9), `AuditTrailApiTests.cs` (13), `Api.Tests/TestSupport/TempDir.cs` (retrying teardown, Q-62) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 834 (unchanged), Api 447 (was 405), 2 consecutive green runs. RED first: `CS0246 AuditLog`/`AuditEntry`, then one behavioural RED that was **my test's** fault (a host with `AllowLegacyKey` false and no `clients.json` refuses to start). Evidence: `docs/testing/T-912.tdd.md` | done | `audit-yyyyMMdd.jsonl` beside the operational log but **not a Serilog sink** (api-v1 §8), 400 days, `Api:AuditRetentionDays` <= 0 keeps everything rather than deleting it. The middleware sits **in front of** `ApiKeyMiddleware` and records after the pipeline returns (handoff trap 17), so a `403` for a missing scope and a `429` from the limiter are both audited under the caller who was refused; an **unknown key is not** audited, because FR-A-16 says *authenticated* and a stranger must not be able to grow the money evidence (**Q-64**). Only four fields are read back out of the response body - `batchId`, `jobId`, `counts`, `totals.posted` - because the rest of it is payees, memos and account names; asserted by a test that greps the file for the key, the memo, the payee and the account. `outcome` and `totalAmount` are given meanings the FR leaves undefined (**Q-65**). `requestId` is a 26-character ULID, honoured inbound only against `^[A-Za-z0-9_-]{8,64}$`, echoed via `OnStarting` so an early refusal carries it, and added to every problem detail. The log template gains `[{requestId}] [{clientId}]` beside `[{jobId}]`, which keeps `LoggingApiTests`' whole-column grep working. **Q-62 identified**: the unexplained single failure is `TempDir.Dispose` racing Serilog's file handle at teardown - measured 0/3 without this task, 1/3 with it, 0/6 after the retry; no assertion was weakened |
| T-913 | `wwwroot/openapi.json` + drift test; then the **Scalar** UI at `/api/v1/reference` | FR-A-18, §10, §11.6 | `Api/wwwroot/openapi.json` (new, hand-authored, 20 operations), `Api/Endpoints/ReferenceEndpoints.cs` (new), `Api/Endpoints/ApiRoutes.cs` (`IsReference`, `OpenApiPath`, `ReferencePath`, `ScopeFor`), `Api/Configuration/AppSettings.cs` (`ReferenceSettings`), `Api/Program.cs`, `Api/QbAutopost.Api.csproj`, `appsettings.json`, `appsettings.Development.json`; tests `Api.Tests/Api/OpenApiDriftTests.cs` (13), `Api.Tests/Api/ReferenceUiTests.cs` (12) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 834 (unchanged), Api 447 -> 472, 2 consecutive green runs; `ls -A /c/qb-autopost` empty. RED first: CS0246/CS0117 x3, then **14 of 23** host tests on 401. Evidence: `docs/testing/T-913.tdd.md` | done | Commit `T-913`. **Package added: `Scalar.AspNetCore` 2.13.13** (Q-46 approved; the 7th package, already on `CLAUDE.md`s approved list since 2026-09-23). Two separate things: the **document** is hand-authored because .NET 8 has no generator and Swashbuckle is excluded, and the **drift test** walks `EndpointDataSource` so it cannot go stale - a route with no operation, an operation with no route, or an operation missing its summary, response schema or scope fails the build, and `x-required-scope` must **equal** `ApiRoutes.ScopeFor`, not merely exist. Disabled means **not mapped** (404, not 403). The RED found a real gap: api-v1 §2.2 calls `Authorization: Bearer` preferred but `ApiKeyMiddleware` reads only `X-Api-Key`, so the document declares both schemes and **offers** the one that works (**Q-67**). Reference needs `health:read` in every environment, not only Production (SPEC-GAP, **Q-66**). Scalar: `HideTestRequestButton`/`HideClientButton` from `AllowTryIt`, `BundleUrl` (not the obsolete `CdnUrl`) from `UseCdn`, telemetry and remote default fonts **off unconditionally** |
| T-914 | `GET /api/v1/quickbooks/lists`, `POST /api/v1/quickbooks/lists/sync`, `POST /api/v1/jobs/validate`; docs, runbook, POC and scripts moved to the v1 paths and the new auth rules | api-v1 §3, FR-A-7, §9 | `Core/Pipeline/FolderValidator.cs` (new), `Core/Pipeline/StatementCheck.cs` (new: G1 extracted from `JobPipeline`), `Core/Jobs/FolderReader.cs` (`Read(folder, createOutput)`), `Core/Pipeline/JobPipeline.cs`, `Api/Endpoints/JobEndpoints.cs` (`POST /jobs/validate`), `Api/Endpoints/QuickBooksEndpoints.cs` (`GET /quickbooks/lists`, `POST /quickbooks/lists/sync`), `Api/Program.cs` (DI), `Api/wwwroot/openapi.json` (3 operations, 5 schemas); docs `docs/spec.md` §6 + §12, `docs/runbook.md` (§1, §2.2, §2.4, §3.1, new §3.4 keys and scopes, §3.5 limits and job roots, §3.6 the reference, §5.1, §8.1, §8.5, new §8.7 the audit file), `steps.md`, `samples/poc/README-POC.md`, `samples/poc/appsettings.json` (every M9 setting), `scripts/qb-server-check.ps1`, `scripts/shadow-diff.ps1`, `deploy/start-all.ps1`; tests `Core.Tests/Pipeline/FolderValidatorTests.cs` (6), `Core.Tests/Architecture/OperatorDocsTests.cs` (19), `Api.Tests/Api/FolderValidateApiTests.cs` (8), `Api.Tests/Api/QbListsApiTests.cs` (7) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 859 (was 834), Api 487 (was 472), 2 consecutive green runs; `ls -A /c/qb-autopost` empty. RED first: `CS0246 FolderValidator`, then **11 of 15** host tests on 404/405, then `OpenApiDriftTests` on the undocumented routes, then **15 of 19** `OperatorDocsTests`. Evidence: `docs/testing/T-914.tdd.md` | done | **Two real defects found in the operator scripts, not cosmetics**: `deploy/start-all.ps1` waited on `/health/hermes` and `scripts/qb-server-check.ps1 -Step health` called `/health/quickbooks`, both without a key and both with a comment saying none was needed - since T-910 that is a 401, so the logon script would have exited 2 on every boot with the API fine, and the T-609 checklist would have stopped at step 1. start-all now waits on the key-free `/api/v1/health/ready` (it must hold no secret - `DeployScriptsTests` forbids the string `ApiKey` in it) and Hermes stays proved by its own port; qb-server-check sends the key. `OperatorDocsTests` is the new guard: every route named in the five operator files must carry `/api/v1`, none may promise a key-free health check, and seven M9 subjects must be in the runbook. **No existing test was changed or deleted**; `FolderReader.Read` gained an optional parameter and G1 moved verbatim into `StatementCheck`, so the validate and the job cannot reach different verdicts. `Api:LegacyRoutes` left **true** (Q-53). New gaps Q-68 (the validate answers 422 where `POST /jobs` answers 400), Q-69 (when `/qb/sync-lists` goes), Q-70 (`GET /lists` has no paging) |
| T-915 | `allowModelAccounts` / `qb:post:ai` refused instead of silently ignored | api-v1 §6.1, Q-51 | `Core/Api/DirectRequestReader.cs` (request-level refusal), `Api/wwwroot/openapi.json` (the description now says NOT IMPLEMENTED); tests `Core.Tests/Api/DirectRequestReaderTests.cs` (+3), `Api.Tests/Hardening/ModelAccountsTests.cs` (5, new) | `dotnet build -warnaserror` 0 warnings; `dotnet test` -> Core 862 (was 859), Api 492 (was 487), 2 consecutive green runs. RED first: **Core 2 of 24 failing, Api 3 of 5 failing**. Evidence: `docs/testing/T-915.tdd.md` | done | **Not a planned M9 task** - found while writing `docs/M9-COMPLETE.md` §2.4, authorised by the owner after the milestone closed. The flag was declared on `DirectRequest`, described in `openapi.json` as working, given its own scope, and **read by no line of production code**; `qb:post:ai` is checked nowhere. A caller could set it, hold the scope, and watch every unresolved row be held as `unknown-account` with nothing saying the model was never asked. Refused rather than accepted-with-a-note, because the flag asks for something the server cannot do; refused in the **reader**, so validate and post cannot disagree. **Not implemented**, because Q-51 is unanswered and building it unattended would put a model in charge of which account money lands in (rules 3 and 4). `docs/spec-api-v1.md` §6.1 is now knowingly out of step with the code and was left alone - an agent does not rewrite the contract to match what it built (new gap **Q-71**) |

T-903 checklist (human, on the server, with the app running):
- [ ] `GET /api/v1/health/sdk` with QuickBooks **closed** → answers (does not hang, does not raise the certificate dialog); `requestProcessor.registered` true, `canOpenConnection` true, `quickBooks.running` false
- [ ] Same call with QuickBooks **open** on the test company → `quickBooks.running` true, `name` `QBW.EXE`, `bitness` `x64`, `bitnessMatch` true, `ok` true
- [ ] `processBitness` reads `x64` (confirms T-609 from inside the app rather than from the install folder)
- [ ] The call opens **no** company file: the QuickBooks title bar does not change and no new certificate prompt appears
- [ ] Two calls in a row both answer within a second (the STA thread is created and released each time)
- [ ] With the x86 build of the package (if still to hand): `bitnessMatch` false, `ok` false, message names both bitnesses
- [ ] `qbXmlVersion` echoes `QuickBooks:QbXmlVersion` from `appsettings.json`
- [ ] Log line `QuickBooks SDK health: …` present; no company file path and no key in it

## Questions raised during implementation

| # | Task | Question | Conservative choice taken | Answer (human) |
|---|---|---|---|---|
| Q-0 | T-002 | The POC (`poc/QbAutopost`) is not available. Will it be supplied? | Core written from the spec (ADR-0007); every item below is a guess to confirm | Owner: build from spec (2026-09-16) |
| Q-1 | T-002 | What is the `rules.json.CsvLayouts` format, and what are the real bank/card CSV columns? | Layout = `HeaderContains[]` + column names + `DateFormat` + one signed `AmountColumn` (`PositiveIsDebit`) or `DebitColumn`/`CreditColumn`, plus optional check-number/balance/last-four columns. No layout or 2+ layouts match → statement held. A date that fails the layout format is a row error (no fallback). Sample layouts imitate Chase exports | |
| Q-2 | T-002 | What does `Normalize(description)` do? | Trim, collapse whitespace, upper-case invariant; digits and punctuation kept. Fuzzy matching also drops apostrophes and turns other punctuation into spaces | |
| Q-3 | T-002 | Fingerprint field spelling (kind, direction, missing check number)? | `bank`/`card`, `debit`/`credit`, missing check number = "" | |
| Q-4 | T-002 | G1 "both orientations" — row order, balance sign, or both? | Row order only (file order or reversed). The sign is fixed by kind: bank credits raise the balance, card charges raise it. A partly filled balance column fails G1 | |
| Q-5 | T-002 | Which columns do the paste-ready sheets have, and in what date format? | Checks: Bank, Date, Number, Payee, Account, Amount, Memo. Card: Card, Date, Number, Payee, Type, Account, Amount, Memo. Deposits: Date, Received From, From Account, Memo, Amount, Deposit To. ISO dates, UTF-8 BOM, CRLF; text cells starting with `= + - @` get a leading `'` | |
| Q-6 | T-002 | What is the base `rules.json` schema, and what are the defaults? | Keys as in `Rules.cs`; `FuzzyThreshold` 0.85, `HistoryMinCount` 3. Missing `HoldingExpenseAccount` → numbered checks held; missing `DepositIncomeAccount` → deposits held. Pattern `Match` is a plain substring, not a regex | |
| Q-7 | T-002 | What happens when a check number is over 11 characters, or several aliases / equal fuzzy scores match? | Line held (`refnumber-too-long` / `unknown-payee` with a note); never truncated or guessed. Memo over 4095 characters is truncated (no money impact) | |
| Q-8 | T-001 | CLAUDE.md's NuGet list doesn't include the xUnit runtime packages (`Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`) | Added — xUnit tests can't run without them | |
| Q-9 | T-002 | File name with several 4-digit groups (e.g. `2026-chase-4521.csv`)? | No last-four taken from the name; fall back to the content, else hold `unknown-account` | |
| Q-10 | T-002 | One unparseable row in a CSV: hold the row or the whole statement? | Whole statement held (`unparsable-rows`), because a missing row makes the statement incomplete | |
| Q-11 | T-003 | How should `requirement.txt` state real last-four values? | Assumed as 4-digit groups on the account lines ("Bank Account=… 4521"). Please share a real example | |
| Q-12 | T-101 | Which characters may a job folder name (= job id) contain? | Only letters, digits, `.`, `_`, `-`; anything else → 400. The id appears in URLs and in batch ids `<jobId>#<attempt>` | |
| Q-13 | T-101 | Subfolders inside `statements\` / `invoices\`? | Not read; each is listed in `result.json.unreadable[]` with reason `subfolder-ignored`. `.jpeg`/`.tif` invoices are `unsupported-extension` (spec lists only pdf/png/jpg) | |
| Q-14 | T-102 | Where are the "known job folders" (spec §6 `GET /jobs`, startup recovery) recorded? | New setting `Paths.JobIndex` (default `jobs.json`) maps job id → folder and is rewritten atomically when a job is first saved. A job found `queued` at startup is set `failed (interrupted)`, not re-queued, because it might be a non-dry run | |
| Q-15 | T-103 | Must the company named in `requirement.txt` match `Company.Name`? | Yes: a different name (compared ignoring case and punctuation) fails G2, because the app always posts to the configured company file. The `Company` placeholder or no name → the configured name is used. An empty `Company.Name` fails every job | |
| Q-16 | T-103 | Statement-level hold codes the spec does not name | `reconcile-failed` (G1), `extractor-not-available` (xlsx/pdf until M3), `kind-mismatch` (layout says bank but the requirement lists the last-four only as a card, or the reverse) | |
| Q-17 | T-103 | Line-level: identical lines in one job (same fingerprint → same requestID), and kinds the requirement did not ask for | Every copy of an identical line is held `duplicate-line` (never post one of them); a postable line whose kind is not in the spec's kinds is held `kind-not-requested`. Precedence: skip-pattern → duplicate-line → already-posted → kind-not-requested | |
| Q-18 | T-103 | What happens when the QuickBooks call itself fails? | Any exception → job `partial`, every sent line held `quickbooks-no-response`, and the batch is recorded in the ledger with 0 posted, so `POST /jobs` refuses a re-run until someone checks QuickBooks and uses `force`. `QuickBooksUnavailableException` (thrown only before anything is sent) → `partial` "nothing posted", no ledger record. A response line with status 0 but a different echoed amount is held `amount-mismatch`, and its note names the TxnID to delete | |
| Q-19 | T-104 | API key defaults | An empty `Api:ApiKey` stops the host at startup; the shipped `change-me` is accepted only in Development. `appsettings.Development.json` uses `dev` (plan §4 curl) and local `data/` paths | |
| Q-20 | T-105 | result.json fields beyond spec §10 | Adds `company`, `error`, `counts.posted`, `totals.posted`; held items carry `file`, `lineNo`, `kind`, `note`, `candidates`. `unmatchedInvoices` stays `[]` until M4. The sample's `invoices/home-depot-88213.txt` stand-in is reported `unsupported-extension` (invoices accept pdf/png/jpg only) | |
| Q-21 | T-201 | Should a Hermes transport failure (timeout, connection refused, HTTP 5xx, malformed envelope) be retried, and must the `Authorization` header be sent when `Hermes:ApiKey` is empty? | No retry: spec §9 names only the validation retry, and the caller already falls back (T1) or holds (T2–T4). Empty key → no header (a local Hermes may run without one). Audit copies are written for every HTTP attempt, including failed ones | |
| Q-22 | T-202 | FR-2 falls back to the regex parser when T1 fails validation twice. What if Hermes is unreachable (timeout, refused, HTTP 5xx)? | The job fails (`analysis failed: Hermes Spec call failed: …`) instead of falling back, because the regex reading was not what the operator expected and the rule is to post less when unsure. Re-submit once Hermes is back. Changing this to a fallback is one `catch` in `HermesSpecReader` | |
| Q-23 | T-301 | FR-3 says a date that fails the layout format falls back to invariant `DateOnly.TryParse`; Q-1 chose "row error, no fallback". Which wins, and how are XLSX date cells handled? A workbook that cannot be opened? | Q-1 kept (session 4, owner said go ahead with it): a text date must match the layout format, otherwise the row fails and the statement is held, because an invariant (US) parse can silently swap day and month. XLSX real date cells are read as their date (`yyyy-MM-dd`), so the XLSX parser also accepts an exact ISO date; CSV does not. A plain-number "date" is a row error. A formula with no saved result is blank → row error. A workbook ClosedXML cannot open → statement held `unreadable-statement`. Following the spec instead is one `\|\|` in `StatementGridParser.TryParseDate` | |
| Q-24 | T-302 | FR-3 "text per page < 40 characters → scanned": are spaces and line breaks counted? What if OCR is enabled but a scanned page has no images, or an image OCR cannot read? Which OCR languages? | Whitespace is not counted (a page of blanks is scanned). OCR disabled → any scanned page holds the whole file, even a near-blank last page. OCR enabled: a scanned page without images keeps its own short text (nothing to OCR; T2 and G1 still check the rows); an image that is neither JPEG nor decodable by PdfPig, or any OCR error → file held `unreadable-statement`. English (`eng`) only. `Ocr:Enabled` with missing language data stops the host at startup | |
| Q-25 | T-303 | §9.2 T2: reason codes for a Hermes failure; how statement-level fields (kind, last-four, period, balances, count) combine across chunks; which last-four wins; is a statement with zero rows valid? | `hermes-failed` (validation twice or unreachable; later parts are not sent) and `extraction-conflict`. Each field must be the same in every part that reports it (null = not shown); any difference holds the statement — including a "balance forward" misread as the opening balance on a later page. File-name last-four wins (spec F5); a different one in the text → `conflicting-last4` (since T-305 the same for a CSV/XLSX account column). Zero rows is accepted here; G1 (T-304) decides with the totals. `transactionCount` is only what the statement prints; the model is told never to count | |
| Q-26 | T-304 | FR-4's T2 formula `opening + Σcredits − Σdebits = closing` fits bank statements. For cards? And what if the T2 answer has no opening or closing balance (FR-4 only says CSV/XLSX without totals pass `not-verifiable`)? Partly printed running balances? | Card: charges (debits) raise the balance, as for CSV G1. T2 without both opening and closing balance → G1 fails `not-verifiable`, statement held — model-read amounts are never posted unchecked (CLAUDE.md rule 3). Running balances printed on only some rows are checked between the printed ones (for CSV a partly filled balance column still fails, Q-4). A zero-row statement passes when opening = closing (and the printed count, if any, is 0) | |
| Q-27 | T-304 | FR-10 re-runs the analysis before posting. For PDFs that means a second T2 call, whose rows could differ from the dry run the operator reviewed (still G1-checked). Should posting reuse the reviewed `rows.json` when the PDF is unchanged? | Not decided; today T2 is called again (temperature 0) and the post uses whatever passes G1 then. Suggest before M6: store the PDF's SHA-256 in `rows.json` and reuse those rows when the hash matches, so what is posted is what was reviewed. Since T-402 the same applies to invoices (T3 is called again at post time; `invoices/<file>.json` could be reused the same way) | |
| Q-28 | T-401 | §9.3 T3: hold codes for invoices, long invoices, and how the model tells vendor from customer | `unreadable` (plan M4) for an image with OCR off, an unopenable PDF, an OCR error, no text, or text over 60 000 characters (one request per invoice, never chunked, so a total is never read from part of an invoice); `scanned-pdf-ocr-disabled` for a scanned PDF with OCR off; `hermes-failed` for a Hermes failure. The job's company name is sent as "Our company" so the model can decide the role. A held invoice never holds a line; how it is reported (`result.json.unreadable[]` vs `unmatchedInvoices[]`) is decided in T-402. Sample `invoices/home-depot-88213.txt` still reported `unsupported-extension`; T-402 replaces it with the PDF fixture | |
| Q-29 | T-402 | FR-5 matching details the spec leaves open: invoice without a date; no candidate in the preferred direction; what "payee similarity" compares before payees are resolved; one line matched by several invoices; which lines are candidates; where unreadable invoices are reported | No date → no match (`no-invoice-date`). No same-direction candidate → all candidates go to the similarity step. Similarity = `Fuzzy.MatchScore(line description, invoice party)` (the FR-6 payee is resolved later). A line matched by 2+ invoices → none of them match (`ambiguous`, note names the other files). Candidates = every line of a statement that passed G1. No candidate → `no-matching-line`. Unreadable/`hermes-failed` invoices are listed in `result.json.unmatchedInvoices[]` with their hold code as `reason` (not in `unreadable[]`, which stays the folder-level F2 list); answers Q-28's open point | |
| Q-30 | T-403 | FR-5 "supplies Payee (if the line has none)": which value is `InvoiceRef`; may the invoice party be used when it is not a QuickBooks name; which lines and roles qualify | `InvoiceRef` = invoice file name (unique in the job; the invoice number may be missing). The party is used only when it resolves to a known vendor/customer (QuickBooks lists, `VendorAccounts` keys, ledger payees, aliases) — otherwise QuickBooks would reject the line or a new name would be invented. Only lines that take a payee (FR-6 table) and only a fitting role: vendor invoice → card line / ACH debit, customer invoice → deposit. Numbered checks ("Blank if Check") and transfers never get one | |
| Q-31 | T-501 | §9.4 T4: how is the account list filtered when only some accounts have a type; is OtherIncome allowed; must `alternatives` be listed names; what if there are no accounts? | When any account has a type, untyped accounts are dropped and only Expense, Income, CostOfGoodsSold and OtherExpense are kept (OtherIncome is not named in §9.4); when none has a type, all are kept. An unlisted alternative does not reject the answer (§9.4 rejects only on `account`) but is dropped from `candidates`, so a held line never suggests an unknown name. No accounts (qb-lists.json not synced) → line held `no-accounts`, Hermes not called. A list violation is retried once like any invalid answer, then `hermes-failed` | |
| Q-32 | T-502 | FR-6/FR-7 tiers 3–4 and G3: what happens when the invoice-hinted answer is below the threshold; which invoices count as tier-3 evidence; G3 hold codes; what "prior posting to the same account" compares; is the threshold range checked? | A tier-3 answer below the threshold becomes the tier-4 answer (Hermes is not asked again without the hint) and G3 holds it. Only a matched **vendor** invoice with a non-blank `categoryHint` is evidence (tiers only run for vendor lines). Holds: `low-confidence` (score < threshold, checked first), `no-prior-posting` (no live ledger entry with the same payee and line account, compared ignoring case as in tier 2). Held model lines show no `lineAccount`; the suggestion is `candidates[0]` and in the note. T4 runs only for lines that passed the job gates. `ModelConfidenceThreshold` must be in (0, 1], else `rules.json` fails to load and the job fails | |
| Q-33 | T-503 | §10 lists one `confidence` field per `analysis.json` line. Is it the FR-6 category (rule/history/invoice/model/holding/hold) or the Hermes T4 score? | Both are written: `confidence` keeps the category (a line held by G3 reads `hold` with `tier` 4) and the score is an extra field `modelConfidence` (0…1, null when the model was not asked). `result.json` / `JobView` held items do not show the score | |
| Q-34 | T-601 | Plan T-601 says DI picks the fake gateway when `QuickBooks:Fake=true` or the host is not Windows, but a fake that answers "posted" would put invented TxnIDs in the real ledger. Also: what happens to a call still running when the busy timeout fires, and may another call start meanwhile? | `QuickBooks:Fake=true` → an in-memory simulated company (`SimulatedQuickBooks`, TxnIDs `SIM-n`), allowed only in the Development or Testing environment (startup fails otherwise). Not Windows and not faked → `UnconfiguredQbGateway`: nothing is ever sent and posting ends `partial` "nothing posted". Every QuickBooks call (worker, undo, sync-lists, health) goes one at a time through `ResilientQbGateway`. A call past `BusyTimeoutSeconds` is abandoned (`quickbooks-busy`; lines held and batch recorded as in Q-18) but keeps the lock until the COM call really ends; a caller that cannot get the lock within the busy timeout gets `quickbooks-unavailable` (nothing sent). Empty `Company:FilePath` → unavailable. `QuickBooks:RetryDelaySeconds` (default 5) exists only so tests do not wait | |
| Q-35 | T-602 | FR-15 details: which name field is stored, how `missingInRules` compares names, where sync's audit copies go (FR-17 names only job files), and what happens when QuickBooks refuses a list query? | Accounts and customers store `FullName` (sub-accounts `Utilities:Electric`, jobs `Customer:Job`), vendors `Name`; `AccountType` kept as the type. `missingInRules` = distinct account names in `BankAccounts`, `CardAccounts`, `VendorAccounts`, `KeywordAccounts`, `TransferPatterns`, `HoldingExpenseAccount`, `DepositIncomeAccount` not found **exactly** (ordinal) in QuickBooks, so a case difference is reported. Audit copies: `qb-audit/sync-lists.request.qbxml` / `.response.qbxml` next to `qb-lists.json` (overwritten each sync). Queries use `onError="stopOnError"`; status 1 (no match) = empty list; any other status → 502, and `qb-lists.json` is not changed. Unreadable `rules.json` → nothing sent, 500. The sync waits for the job worker (it may wait behind a running job) | |
| Q-36 | T-603 | FR-8 G4 details: how the query filters, how payees and RefNumbers compare, what a deposit's "payee" is, what happens when a query fails, and how the audit files are named (FR-17 says `query-<n>.qbxml`) | One message set per (kind, header account, compared ignoring case) with `TxnDateRangeFilter` + `AccountFilter`; results whose header account differs are ignored (QuickBooks' `AccountFilter` also matches line accounts). Same amount = exactly equal. "Exact" = same date and (same RefNumber other than `ACH`, ignoring case, or same payee ignoring case); a deposit's payee is its first line's `EntityRef` (`IncludeLineItems`). Several candidates → the first exact one skips the line, else one hold note lists them all. A refused or unreadable query → that group's lines held `duplicate-check-failed`; a gateway failure (unavailable, busy, COM) → nothing is added, job `partial` "nothing posted", no ledger record. Files: `output/query-<n>.qbxml` (request) and `query-<n>.response.qbxml`, old ones deleted at the start of each post. A skipped line's note (TxnID) is now shown in `result.json.skipped[].note` | |
| Q-37 | T-604 | FR-11 retries "a COMException" once after 5 s, but a COM error from `ProcessRequest` on an add may already have posted the lines. And FR-11 says a stale backup makes the job `failed`, while §6 allows only `posting → posted/partial` | Retried once: a session that could not open (nothing sent) and a COM error on a read-only (query) message set. **Not** retried: a COM error on an add or delete (lines held `quickbooks-no-response`, batch recorded, Q-18), a busy timeout, or a wait for a still-running abandoned call. Backup guard runs in `posting` just before G4 and only when there is something to post: blank `BackupFolder` → no check; missing folder, no `*.QBB` (top level only, ignoring case), or newest `LastWriteTimeUtc` older than `BackupMaxAgeHours` → job `failed` with `backup-too-old: …`, nothing sent, no ledger record (so a plain re-submit works). `posting → failed` was added to the state machine for this case only | |
| Q-38 | T-606 | FR-13 details: what counts as a successful delete, what happens to a transaction someone already deleted by hand (QuickBooks answers 3120 "cannot be found"), a batch with no posted lines (Q-18), a QuickBooks failure mid-undo, the job status, and where the audit copies go | Marked `undone` only for `statusCode 0` whose echoed `TxnID` is the one requested; anything else (including 3120 and a missing answer) is reported in `failed[]` with `code: message` and stays live, so the batch and job are not undone (a person checks QuickBooks; a later undo retries only the live entries). A batch with no live entry → 200 `deleted: 0`, QuickBooks not called, nothing changed. A gateway failure → 503, nothing marked (a delete that did happen then shows up as a 3120 failure on the next undo). Unknown batch → 404. Batch ids compare ignoring case. Job `posted/partial → undone` only when its current `batchId` is the undone batch. Audit: `output/undo-<batch id with # as ->.request.qbxml` / `.response.qbxml` in the job folder, or `qb-audit/` next to the ledger when the job is unknown | |
| Q-39 | T-607 | FR-16 `/health/quickbooks` returns `{ ok, companyFile, message }` but does not say what `message` holds, where `companyFile` comes from, or whether the check waits for a running post | `ok` = `HostQuery` status 0. `message` = product name and version (e.g. `QuickBooks Desktop Pro 2023 33.0`), or the error plus the process bitness (`process is x64`), the usual cause of a failed session. `companyFile` = the SDK's `GetCurrentCompanyFileName` in a second session; if only that fails, `ok` stays true with `companyFile: null` and the reason in `message`. The check is not queued behind jobs but waits for the gateway lock, so during a post it may take up to `BusyTimeoutSeconds` and then return 503. It does not compare the open file with `Company:FilePath` | |
| Q-40 | T-701 | FR-14 details: `rules.json` has `//` comments that a rewrite cannot keep; must the alias name / vendor also be in `qb-lists.json` (spec §6 names only the account); how short may a fragment be; what if `rules.json` is broken or missing; does teaching wait for a running job? | Comments are dropped on the first taught rule (the file is re-serialised through `JsonNode`, other keys and their order kept; sections found ignoring case, created when missing). When `qb-lists.json` exists, the account, the vendor (`/rules/account`) and the alias name (vendor or customer list by `kind`) must all be in it **exactly** (ordinal, as Q-35); a case-only difference is refused with "Did you mean …". Without lists anything non-blank is accepted. Fragment is stored normalised (Q-2) and needs ≥ 3 characters; an existing fragment with the same normalised form, or a vendor key equal ignoring case, is replaced and returned as `previous`. `kind` other than `vendor`/`customer` → 400. Broken or missing `rules.json` → 500, file untouched (never recreated). Writes use a process-wide lock, not the job worker, so they never wait for a job; a running job keeps the rules it loaded at its start | |
| Q-41 | T-702 | §14 logging details: file name and retention, which levels are configurable, what "no secrets" covers, and what the correlation id is on lines that are not about a job | File `Paths:Logs/qbautopost-yyyyMMdd.log`, a new file each day and at 1 GB, 31 files kept, shared so two hosts can write. Sinks (console + file) are fixed in code so a sink added in configuration cannot bypass scrubbing; only `Serilog:MinimumLevel:Default` / `Override:<category>` are read (defaults: Information, `Microsoft` Warning, `Microsoft.Hosting.Lifetime` Information, `System.Net.Http.HttpClient` Warning). Every event is scrubbed before any sink: the values of `Api:ApiKey`, `Hermes:ApiKey` and any configuration key ending in `ApiKey`/`Password` (or named Authorization, Token, Secret, API_SERVER_KEY) are masked `***` in the message, properties and exception text (values under 4 characters are masked only by label, e.g. the Development key `dev`); the value after `X-Api-Key:`, `Authorization: Bearer`, `apiKey=` etc. is masked; properties with such names are masked. `jobId` = the worker scope (job id, or the label `qb-sync-lists` / the batch's job id for exclusive work), else the `{JobId}` named in the message, else `-` | |
| Q-42 | T-801 | §4 says Hermes runs in Docker with port 8642 on loopback and `API_SERVER_KEY` required, but never names the image, the port the API listens on inside the container, the container's variable names for enabling the API server, where it keeps its data, or which provider key variable it needs (Q5) | `HERMES_IMAGE`, `HERMES_CONTAINER_PORT`, `HERMES_DATA_PATH` and `API_SERVER_KEY` are **required** in `deploy/hermes/.env` with no defaults, so compose refuses to start rather than pull or run a guessed image. The container gets `API_SERVER_ENABLED=true`, `API_SERVER_HOST=0.0.0.0` (container side only), `API_SERVER_PORT`, `API_SERVER_KEY`; the provider key goes in an optional, git-ignored `provider.env` under whatever name the provider needs. Host side is fixed at `127.0.0.1:8642`. Memory cap 4g by default. Needs the owner to name the image and confirm the variable names | |
| Q-43 | T-802 | Plan T-802: "WSL/Docker up → compose up → wait for `/health/hermes` → start API". But `/health/hermes` is an endpoint of the API itself, so it cannot answer before the API starts. What should the script wait for, and what if Hermes never comes up? Should it run elevated? | The script waits for Hermes' own port (`127.0.0.1:8642` accepting TCP, up to 300 s), then starts the API, then waits for `/health/hermes` = 200 (up to 180 s). If Hermes stays down the API is **still started** (undo and `/health/quickbooks` need no Hermes; a new job fails at T1 and posts nothing) and the script exits 2 with a warning; Docker/compose/API start failures exit 1 and the API is not started when Docker or compose failed. Only one API process (by exe name) is ever started. The task runs with limited rights (no elevation), interactive logon, 60 s delay; registering it needs an elevated shell. Docker Engine in WSL is started as root inside the distribution when `docker info` fails | |
| Q-44 | T-804 | Plan T-804: "go-live one company, then the rest". The app configures one `Company` (name, file, rules) and QuickBooks Desktop has one company file open per session. How are several companies run: one installation per company (own folder, port, ledger, rules, task), one at a time with QuickBooks switching files, or a multi-company app? | Nothing added: one installation = one company. `start-all.ps1` starts at most one API process (checked by exe name), so a second installation on the same server would be seen as "already running" and not started. Switch companies one at a time until the owner decides; the T-804 checklist goes live for the first company only | |
| Q-45 | T-806 | The owner wants a POC without Hermes. The spec makes Hermes part of every job (T1 reads the requirement; FR-2 falls back to regex only after two invalid answers). What should "no AI" do? | New `Hermes:Enabled` (default `true`, so production is unchanged). `false`: the requirement is read by `RegexSpecReader`; every other Hermes call fails as "unavailable", so a PDF statement is held, an invoice is unmatched and a line no rule resolves is held `hermes-failed` — nothing is guessed. `/health/hermes` answers 503 "turned off". Ask the owner whether this mode should stay after the POC | |
| Q-46 | T-913 | May we add `Scalar.AspNetCore` for the reference UI? The owner ruled out Swagger/Swashbuckle entirely (2026-09-22) and `CLAUDE.md` forbids new packages without approval. The OpenAPI document itself needs no package | The document (`wwwroot/openapi.json`, hand-authored, drift-tested against `EndpointDataSource`) ships either way; the UI waits for the answer. **No Swagger fallback** — if Scalar cannot be used, there is no UI. The version chosen must target `net8.0` and ship its assets locally (the server may have no outbound internet) | **ANSWERED 2026-09-23: yes, add `Scalar.AspNetCore`.** The owner approved the package explicitly when setting up the unattended run. It is the 7th package; `CLAUDE.md`s list must be updated with it. Served from our own host, so no CDN call. |
| Q-47 | T-911 | D-4 says remote callers. From where exactly — the same LAN, a VPN, or the public internet? It decides whether a CIDR allow-list is enough or a reverse proxy and managed certificates are needed | TLS required off-loopback (startup refuses without it unless `AllowInsecureRemote`), per-client CIDR allow-list, rate limits, no CORS origins by default | |
| Q-48 | T-910 | Who issues and revokes caller keys, and where do they live between issue and use? `clients.json` on the server is the simplest thing; it is not a secret manager | `clients.json` beside `rules.json`, git-ignored, **hash + salt only**; keys generated by `scripts/new-api-client.ps1`, printed once, never logged or written to `output/`; rotation via `previousKeyHash` + expiry | |
| Q-49 | T-906 | On the direct path there is no statement, so G1 cannot run. Is `controlTotal` an acceptable substitute, and should a mismatch refuse the whole batch or post the rows that are internally consistent? | Refuse the whole batch, post nothing, `400 control-total-mismatch`. The API never adjusts an amount to make a total agree (`CLAUDE.md` rule 3) | |
| Q-50 | T-904 | May a caller name the company file per request? That is multi-company by the back door (cf. Q-44) | No: `QuickBooks:AllowCompanyFileOverride=false`, a supplied `companyFile` is `400` and **no** gateway call is made. When enabled it must be absolute, `.QBW`, and inside `AllowedCompanyFolders[]` | |
| Q-51 | T-906 | May a direct post use Hermes to choose accounts (`allowModelAccounts`), or must every row carry `lineAccount` or match a rule? | Default `false`: an unresolved row is **held**, never guessed. `true` needs `Hermes:Enabled` **and** the `qb:post:ai` scope | |
| Q-52 | T-912 | How long must batch evidence (`request.json`, qbXML, audit lines) be kept? | 400 days for `audit-yyyyMMdd.jsonl` (money evidence), 30 days for idempotency records, 31 days for the operational log as today. All three configurable | |
| Q-53 | T-914 | When may `Api:LegacyRoutes` default to false and the flat routes be deleted? | Stays `true` until the owner confirms no caller uses the flat paths; each legacy hit is logged at Warning once per route per hour. T-914 removes the POC package's own dependency on them first | |
| Q-57 | T-905 | FR-A-5 lists a `companyName` check — compare the name QuickBooks reports with `Company:Name`. Getting that name needs a `CompanyQueryRq`, which the app has never sent: no builder, no parser, no golden file, and no way here to verify the response element names (`CompanyRet/CompanyName`?) against a real SDK. Should the check be added, and is a company **name** comparison worth it once the company **file** is already compared exactly? | Not implemented rather than guessed (rule 4). The six other checks ship, and `openInQuickBooks` already answers the dangerous version of the question — it compares the actual open file path, which is stricter than a display name. Adding `CompanyQuery` means a new qbXML request plus a golden file, and the element names must be confirmed on the server first | |
| Q-56 | T-904 | FR-A-4's example lists `openConnection`, `beginSession`, `companyQuery` and `endSession` as separately timed steps, but one `IQbGateway` call is one whole SDK session (FR-11), so those phases cannot be timed from outside without widening the gateway interface — and `QbSession` would have to report each step back per call, which is COM code only the server can verify. Is a per-phase breakdown worth that, or is "slow `hostQuery`" enough? | The three measurable steps are reported (`waitForGateway`, `hostQuery`, `companyFile`); no number is invented. A slow phase still surfaces, because the whole `hostQuery` is marked slow and carries the dialog hint — which is the action the operator needs either way. The spec's example was amended to match | |
| Q-55 | T-903 | FR-A-3 lists "supported qbXML versions readable" among the SDK checks, but the SDK exposes that list through `QBXMLVersionsForSession(ticket)`, and a ticket only exists after `BeginSession` — which the same requirement forbids, since opening a company file is exactly what this probe must avoid. Which wins? | Neither is guessed: the probe reports the **configured** `QuickBooks:QbXmlVersion` and no `supported` list. Nothing claims the configured version is accepted, so an operator is not told a version works when it may not. If the owner wants the list, the probe needs its own short session and stops being safe to call while QuickBooks is busy | |
| Q-54 | T-907 | Does the folder-based job model stay for good, or is the direct API meant to replace it once callers migrate? It decides whether `/jobs/validate` and the folder documentation are worth maintaining | Both maintained, nothing removed. One engine, two entrances; a golden test keeps their qbXML identical | |
| Q-58 | T-907 | FR-6 leaves the payee blank on a numbered check ("Blank if Check" in the requirement), but FR-A-8 gives a direct caller a `payee` field for every kind. When a caller sends a check number **and** a payee, which wins? | The caller's payee is used. Dropping a name they explicitly sent would silently discard an instruction, which is worse than a full QuickBooks record; the folder path is unchanged. This is the one place the two entrances decide differently, so it is worth confirming | |
| Q-59 | T-908 | FR-A-8 says a dry run leaves "a persisted batch in `ready` state that `POST /api/v1/batches/{id}/post` can later post", but that route is in neither the §3 route table nor the M9 task list. Should it exist, or does a caller re-send the batch with `dryRun:false`? | Not built. Re-sending is one code path instead of two, and it re-runs every gate against the rules and ledger **as they are at posting time** rather than posting a plan made earlier — which is the safer of the two, since a `ready` batch could otherwise post an account a rule has since changed | |
| Q-60 | T-909 | FR-A-12 says what a replay and a reuse do, but not what becomes of a key whose request **failed**. Should a key be held for ever once used, or released when nothing was accepted? | Released on any non-2xx. Holding it would lock a caller out of a key they chose because of a typo, and — worse — a `503` from an unreachable QuickBooks (where nothing posted) would permanently block the retry that response exists to invite. Safe because idempotency is the convenience layer: G4 and the ledger catch a genuine repeat with or without a key | |
| Q-61 | T-910 | `docs/spec.md` §6 leaves **all** of `GET /health/*` open; `docs/spec-api-v1.md` §2.2 opens only `/health` and `/health/ready`. Which applies, and has the owner's monitoring been told? | The narrower rule, on both surfaces. `/health/quickbooks` opens a QuickBooks session and `/health/sdk` probes COM, so with remote callers (D-4) leaving them open lets a stranger make the server hammer QuickBooks. One rule for both surfaces, because a rule that depends on which path you used is one nobody remembers. **An operator polling those routes without a key will start getting 401 after this upgrade** - they should be told first | |
| Q-62 | T-911 | One Api test run reported `Failed: 1, Passed: 404` immediately after the `appsettings.json` edit; the name scrolled away and it did not recur in 11 further full runs of the same code. Which test was it, and is it a real race or a pre-existing polling flake? | None taken. Recorded rather than written off: T-911 adds no shared mutable state to the default test configuration (the limiter and brake are inert unless a test enables them, and each `WithSettings` host has its own container), so the suspicion falls on an existing timing test - but that is a hypothesis. **The next agent should re-run `dotnet test` a few times and capture the failing name if it reappears** | **Answered by T-912 (2026-09-23)**: not a race in the application. Two names captured - `SyncListsApiTests.Should_Return503AndKeepFile_When_QuickBooksIsUnavailable` and `RouteVersioningTests.Should_NotRequireApiKey_When_RouteIsLiveness("/api/v1/health/ready")` - both with the same stack: `System.IO.IOException ... 'qbautopost-20260923.log' ... used by another process` thrown from `TempDir.Dispose`, **after** the test body had passed. `WebApplicationFactory.Dispose` returns before Serilog's shared file sink releases the handle. Measured: 0 failures in 3 runs without T-912, 1 in 3 with it (42 more tests, 13 more hosts), 0 in 6 after `TempDir.Dispose` was made to retry. No assertion changed |
| Q-63 | T-911 | FR-A-17 requires every caller path to sit inside `Paths:AllowedJobRoots`, and §12 ships that list **empty**. Enforcing an empty list literally refuses every job, so no installation would start; ignoring it is the permissive reading rule 4 forbids once callers are remote. Which did the owner mean? | Empty means **unconfigured**: unrestricted while the bind is loopback, and **startup refuses a non-loopback bind while it is empty** - the same shape as the TLS refusal in FR-A-14. Today's loopback deployment is unaffected; exposing the port forces the owner to say where jobs live. Same rule for `QuickBooks:AllowedCompanyFolders`, which is additionally gated by `AllowCompanyFileOverride` (false by default). Marked `// SPEC-GAP T-911` | |
| Q-64 | T-912 | FR-A-16 audits every *authenticated* mutating request. Does a request whose key was **not** recognised (401), or one refused by the brute-force brake (429 before the key is even compared), belong in the audit file too? | **No line.** The word in the FR is "authenticated", and the operational log already records every refusal with its address and the reason. Auditing unauthenticated traffic would let anyone holding a wrong key grow the money-evidence file at will, and would bury the postings it exists to record. A **403** *is* audited, because the caller is identified - that is the case the middleware was placed in front of the key check for. Marked `// SPEC-GAP T-912` | |
| Q-65 | T-912 | FR-A-16 lists `outcome` and `totalAmount` as fields but never says what values they take. Which vocabulary, and is `totalAmount` the money submitted or the money that posted? | `outcome` is one of `ok` (2xx), `accepted` (202), `replayed` (an `Idempotency-Key` replay), `refused` (4xx) or `failed` (5xx or an unhandled exception) - what became of the request, not what the caller asked for. `totalAmount` is the batch's `totals.posted`, **the money that actually moved**, so a dry run records `0`; the alternative would put a real number in the money column of a batch that posted nothing. Both are one-way doors for a reader of old files, so they are recorded rather than assumed | |
| Q-66 | T-913 | FR-A-18 says the reference and the document are served "only when `Api:Reference:Enabled=true` … **and, in Production**, require the `health:read` scope". Does that mean Development serves them to anyone who can reach the port? | **`health:read` in every environment.** `ApiRoutes.ScopeFor` is a pure function of method and path with no environment in it, and rule 4 asks for the reading that reveals less — this page lists every route, every scope and every request shape. The cost, which the owner should know: the key travels in a **header**, so the page cannot be opened in a plain browser at all (that is already true of the Production rule the spec does state); it is read with a client that can set `X-Api-Key`. Relaxing it for Development is a one-line change in `ScopeFor` plus an `IHostEnvironment` | |
| Q-67 | T-913 | api-v1 §2.2 says `Authorization: Bearer <key>` is the **preferred** form and `X-Api-Key` the alternative, but `ApiKeyMiddleware` reads `X-Api-Key` and nothing else — every bearer request is a 401. Should the middleware learn `Authorization: Bearer`, or should §2.2 be corrected to match the code? | **Nothing changed in the middleware**: accepting a second authentication header is a change to the security surface and belongs with T-910's row, not with a documentation task. `openapi.json` declares **both** schemes and *offers* `apiKeyAuth`, whose header name a test pins to `ApiSettings.KeyHeader`; the `bearerAuth` description says plainly that this build does not accept it. Found by T-913's RED: 14 of 23 new tests failed with 401 because they had believed the spec | |
| Q-68 | T-914 | FR-A-7 gives `POST /api/v1/jobs/validate` only `200`/`422`, but `POST /api/v1/jobs` answers **400** for exactly the same folder errors (F1: missing `requirement.txt`, empty `statements\`). Two routes, one verdict, two status codes - which is right? | The validate answers **422 with the body**, because FR-A-7 names only 200/422 and FR-A-6 already settled that a validator returning a body only on success is useless. A *malformed* request - no `folder` field at all - is still 400, as is a folder outside `Paths:AllowedJobRoots`. `POST /jobs` was not changed: nothing about creating a job is made safer by moving its status code unattended. Marked `// SPEC-GAP T-914` | |
| Q-69 | T-914 | api-v1 §3 marks `POST /api/v1/quickbooks/lists/sync` as a **rename** of `POST /qb/sync-lists`, but §13's task list gave T-914 only the two new routes. Is the old spelling to be deleted, and when? | **Both are mapped.** The v1 spelling was added, the old one kept and marked `deprecated` in the OpenAPI document; nothing was deleted, because removing a working path a caller may still use is not a decision to take with nobody watching. It should go with the rest of the legacy surface when Q-53 is answered - and unlike the flat paths, `/api/v1/qb/sync-lists` is *versioned*, so it needs its own answer | |
| Q-70 | T-914 | `GET /api/v1/quickbooks/lists` returns every cached account, vendor and customer in one response. A company with tens of thousands of names would return all of them. Should it page or filter? | **No paging.** It is a local read of a file this app wrote, so nothing is unbounded that was not already on disk, and the caller's whole purpose is to pick from the list. If the owner's real chart of accounts makes the response unwieldy, the answer is a `?q=` filter rather than paging - but no test sends a large one and nobody has measured it | |
| Q-71 | T-915 | `docs/spec-api-v1.md` §6.1 says `allowModelAccounts: true` "permits tier 3-4 (Hermes) account choice"; T-915 made the code **refuse** it with 400, because nothing ever implemented it and Q-51 is unanswered. Which is right - implement the spec, or amend it? | The code refuses and says so; the spec was **left unchanged on purpose**, because "spec wins over code" means an agent must not quietly rewrite the contract to match what it built. Whichever way the owner answers, one of the two files changes. Related: Q-51 (may a model choose accounts at all) and the `qb:post:ai` scope, which is granted by `new-api-client.ps1` and confers nothing | |

## Decisions

| Date | Decision | Why |
|---|---|---|
| 2026-09-23 | **M9 finishes as an unattended relay**: one agent per task, each updating `handoff.md` and committing before handing over to a fresh agent. The owner is away and will not answer anything mid-run | Context does not survive between agents, so `handoff.md` is the only state that carries forward. One task per agent keeps each handover small enough to be trustworthy |
| 2026-09-23 | **Agents push `m9-api-v1` after every task**; `main` is never touched and no PR is opened | An unattended multi-day run on one machine is one disk failure away from losing everything. A feature branch is reversible; a merge is not |
| 2026-09-23 | **Q-46 answered: `Scalar.AspNetCore` is approved** (the 7th package). Swagger/Swashbuckle stays ruled out | The owner approved it directly. Serving the reference UI from our own host beats a CDN call for a server that posts money |
| 2026-09-23 | **The run stops at the end of M9.** Server-verification tasks are left `ready-for-human` and no new milestone is started | T-903's checklist, T-802/803/804 and the direct-post check need a person at the QuickBooks machine. An agent cannot do them and must not claim to |
| 2026-09-15 | Core is cross-platform; COM isolated in QbAutopost.QuickBooks | tests run anywhere; server only for M6/M8 |
| 2026-09-15 | Invoices are evidence only (Q1 assumed) | avoids a second posting pipeline in v1 |
| 2026-09-15 | Deposits post straight to income (Q2 assumed) | pending answer on open customer invoices |
| 2026-09-16 | Build Core from the spec; POC unavailable (ADR-0007) | owner chose to proceed without the POC; gaps listed in Questions |
| 2026-09-16 | ADRs live in `docs/adr/` (0001–0007) | architecture decisions recorded with their alternatives |
| 2026-09-16 | `global.json` pins SDK 8.0.x (`rollForward: latestFeature`) | SDK 10 is installed on the dev box and would otherwise be used |
| 2026-09-16 | JSON enums are written camelCase (`"ready"`, `"post"`, `"ccCharge"`) and read case-insensitively | spec §6/§10 show lowercase values; `rules.json` `"Kind": "Bank"` still loads; `ledger.json` `kind` becomes `"check"` (no real ledger exists yet) |
| 2026-09-16 | M1 already posts through `IQbGateway` (G5 + ledger), with an `UnconfiguredQbGateway` that sends nothing until M6 | T-103 names `RunPostAsync`; the state machine needs a real posting path to test; M6 tasks keep the COM, live-G4, retry, busy and backup work |
| 2026-09-16 | Repo root is `D:\qb_post` (plan calls it `qb-autopost/`); `CLAUDE.md` copied to the root, `docs/CLAUDE.md` kept | plan T-005; copies must be kept in sync until one is removed |
| 2026-09-22 | The "API conversion" is M9, a restructure — not a rewrite. Contract in `docs/spec-api-v1.md`; `spec.md` §6 rewritten at T-914 | the app is already an ASP.NET Core 8 minimal API with 10 routes, API-key auth, problem details and file logging (handoff §3, §6) |
| 2026-09-22 | D-1: keep the job-folder pipeline **and** add a direct JSON-rows post; one engine, two entrances | owner answer; a golden test keeps the qbXML of both paths byte-identical so the engine cannot fork |
| 2026-09-22 | D-2: two validation endpoints — the QuickBooks company file, and the transaction data | owner answer; one overloaded "validate" would mix a COM round trip with an offline check |
| 2026-09-22 | D-3: `/api/v1` prefix, legacy flat routes kept one release; **Scalar** for the reference UI, **no Swagger/Swashbuckle** | owner answer (UI) and owner instruction (2026-09-22, no Swagger). Consequence: .NET 8 has no built-in OpenAPI generator, so the document is hand-authored and drift-tested |
| 2026-09-22 | D-4: remote callers — per-caller keys with scopes, TLS off-loopback, rate limits, 400-day audit | owner answer; the single shared key was safe only because the bind was loopback |

## Blockers

| Date | Task | Blocker | Owner | Resolved |
|---|---|---|---|---|
| | | | | |

## Session log

| Date | Session | Tasks touched | Result |
|---|---|---|---|
| 2026-09-16 | 1 | ADRs, T-001…T-005 | M0 done on Windows: build has 0 warnings; 92 tests pass (Core 91, Api 1); scripts run. Linux test run still open. Found and fixed a `.gitignore` rule that hid `Output/` sources |
| 2026-09-16 | 2 | T-101…T-107 | M1 done on Windows: 235 tests (Core 181, Api 54), 8 consecutive green runs; sample job reaches `ready` via HTTP in tests and by hand. No owner answers to Q-1…Q-11 yet, so the conservative choices stand. New gaps Q-12…Q-20. Found and fixed a Windows file-sharing race between polling readers and atomic replace. Linux test run still open |
| 2026-09-16 | 3 | T-201…T-205 | M2 done on Windows: 331 tests (Core 263, Api 68), 6 consecutive green runs. Hermes client (retry with errors, audit copies, ping), T1 prompt + `SpecAnswer`, `HermesSpecReader` with regex fallback, G2 via Hermes, `/health/hermes`, `spec.json` golden. No owner answers yet. New gaps Q-21 (no transport retry; empty key → no header) and Q-22 (Hermes unreachable → job fails, no fallback). **Behaviour change:** without a running Hermes the sample job now fails instead of reaching `ready`. Linux test run still open |
| 2026-09-16 | 4 | T-301…T-305 | XLSX statements parsed (ClosedXML → shared grid/layout logic) and reconciled like CSV (T-301, Core 283 / Api 68). PDF text per page with PdfPig, scanned-page detection, `IOcr` with a Tesseract engine behind `Ocr:Enabled` (T-302); 369 tests (Core 298, Api 71); real Tesseract checked by hand on Windows. New gap Q-24 (what counts as a character; scanned page without images). Still no owner answers in the tracker. Date rule decided: Q-1 kept, with ISO accepted for XLSX date cells (Q-23). New hold code `unreadable-statement`. Found: ClosedXML never saves formula results, so tests imitate Excel's saved `<v>`. T-303: Hermes T2 extractor (prompt, validation, 60 000-char page-group chunking, merge) with 44 new tests → Core 342, Api 71; not yet wired into the pipeline (T-304). New gap Q-25. T-304: T2 wired into the pipeline via `StatementReader`, G1 for T2 (totals, count, period, partial running balances; card sign reversed; no totals → held), `rows.json` with `totals` → Core 365, Api 73; a text-PDF bank statement gives the same 8/2/1 as the CSV. New gaps Q-26, Q-27 (T2 re-run at post time). T-305: gap check — grid-parser tests (ambiguous layout was untested), committed PDF fixtures for both sample statements, card PDF end to end; an account column contradicting the file name now holds the statement; binary fixtures marked in `.gitattributes`. **M3 done on Windows: 455 tests (Core 382, Api 73).** Linux test run still open |
| 2026-09-16 | 5 | T-401, T-402 | Hermes T3 invoice extractor: `InvoiceAnswer` validation, `invoice.md` prompt (required at startup), `InvoiceExtractor` (PDF via PdfText, PNG/JPG via OCR or `unreadable`, Hermes failure → `hermes-failed`), committed invoice PDF fixture → Core 424, Api 73. No owner answers yet. New gap Q-28. Not yet wired into the pipeline (T-402). Linux test run still open. T-402: `InvoiceMatcher` (FR-5 amount/date window, direction, payee similarity, ties → `ambiguous`, one line claimed by two invoices → neither), invoices read after G2, `output/invoices/<file>.json`, `result.json.unmatchedInvoices[]` as objects; sample invoice is now a PDF and matches the Home Depot line → Core 458, Api 73. New gap Q-29 |
| 2026-09-17 | 5 (cont.) | T-403 | Mapper takes the matched invoices: `InvoiceRef` = invoice file on the matched line; payee supplied from a fitting invoice only when the description names none and the party is a known name; numbered checks and transfers stay blank. Golden: Home Depot line gains `invoiceRef` → Core 474, Api 73. New gap Q-30 |
| 2026-09-17 | 5 (cont.) | T-404 | Gap check: API tests for invoices (match, unmatched, Hermes down, image without OCR, duplicate invoice), T3 through the real client, `result.json` round-trip with unmatched invoices; `GET /jobs/{id}` shows `unmatchedInvoices`. **M4 done on Windows: 554 tests (Core 476, Api 78).** No owner answers yet (Q-1…Q-30). Linux test run still open |
| 2026-09-17 | 6 | T-501 | Owner answers checked: none yet. Hermes T4: `AccountAnswer` (account non-blank, 0 ≤ confidence ≤ 1), list check through the new `HermesRequest.Check` (inside the client's retry), `account.md` prompt (required at startup), `AccountChooser` (§9.4 type filter, top-3 listed candidates, `no-accounts`, `hermes-failed`) → Core 513, Api 78. Not wired into mapping yet (T-502). New gap Q-31. Linux test run still open |
| 2026-09-17 | 6 (cont.) | T-502 | Tiers 3–4 + G3: `ModelTiers` pass after the job gates (lines held `no-account-rule`), `ConfidenceGate` (invoice tier ≥ threshold; model tier ≥ threshold and a prior ledger posting of the payee to the account), holds `low-confidence` / `no-prior-posting` with top-3 candidates, `MappedTxn.ModelConfidence`, threshold range checked in `rules.json` → Core 559, Api 78. Sample job and golden unchanged. New gap Q-32 |
| 2026-09-17 | 7 | T-503 | Owner answers checked: none yet. `analysis.json` lines gain `modelConfidence` (T4 score; `confidence` stays the category); every §10 field checked by `AnalysisLineTests`; golden updated (11 null fields only) → Core 565, Api 78. New gap Q-33 |
| 2026-09-17 | 7 (cont.) | T-504 | Gap check for tiers and G3: G3 matrix (10 rows) and threshold theories in `ModelTiersTests`, `rules.json` threshold and tier precedence through the pipeline, `AccountTierApiTests` (tiers 3–4 through the host) → Core 583, Api 81. **M5 done on Windows.** No owner answers yet (Q-1…Q-33). Linux test run still open |
| 2026-09-17 | 8 | T-601 | Owner answers checked: none yet (Q-1…Q-33). `QbSession`/`QbGateway` (late-bound COM on an STA thread, one session per call), `ResilientQbGateway` (one call at a time, busy timeout → `quickbooks-busy`), DI switch via `QbConnection` (SDK / simulated with `QuickBooks:Fake` in Development / unavailable), `FakeQbGateway` backed by `SimulatedQuickBooks` → Core 597, Api 91. COM path unverified until T-609. New gap Q-34 |
| 2026-09-17 | 8 (cont.) | T-602 | `POST /qb/sync-lists`: one read-only message set (Account/Vendor/Customer query, active only) → `qb-lists.json` + `missingInRules`; runs as exclusive work on the job worker; QuickBooks failures → 503/502 problem details → Core 610, Api 99. New gap Q-35 |
| 2026-09-17 | 8 (cont.) | T-603 | G4 against QuickBooks while posting: `QbTxnQuery` (Check/CreditCardCharge/CreditCardCredit/Deposit query over [min−W, max+W]), `LiveDuplicateGate` (exact → `already-in-quickbooks`, else `possible-duplicate`; refused query → `duplicate-check-failed`), `query-<n>.qbxml` audit files; a gateway failure during the check posts nothing and records nothing → Core 634, Api 105. New gap Q-36 |
| 2026-09-17 | 8 (cont.) | T-604 | FR-11 posting safety: one retry after 5 s when repeating cannot double-post (session not opened; COM error on queries), backup-age guard (`failed` `backup-too-old`, new `posting → failed` move), tests for the persisted `posting` state and `response.qbxml` → Core 650, Api 114. New gap Q-37 |
| 2026-09-17 | 8 (cont.) | T-605 | G5/ledger/result reviewed against FR-12 and §13; added tests for the `posted` end state, §13 ledger field names, ledger entry contents, `result.json` posted items, deposit echo → Core 651, Api 118. No production change |
| 2026-09-17 | 8 (cont.) | T-606 | `POST /batches/{id}/undo`: one `TxnDelRq` message set for the live ledger entries, entries marked `undone` per confirmed delete, batch and job `undone` when none is left; 404/503 problem details; runs on the job worker → Core 666, Api 126. New gap Q-38 |
| 2026-09-17 | 8 (cont.) | T-607 | `GET /health/quickbooks`: `HostQuery` status 0 → 200 `{ ok, companyFile, message }`, else 503 with the reason and process bitness; hang → 503 after the busy timeout → Core 673, Api 131. New gap Q-39 |
| 2026-09-17 | 8 (cont.) | T-608 | M6 API gap check: added the T-609 order end to end and a `QuickBooks:Fake=true` host run; every §15 QuickBooks scenario mapped to a test; 3 consecutive green runs → Core 673, Api 133 |
| 2026-09-17 | 8 (cont.) | T-609 | **[server]** prepared: `scripts/qb-server-check.ps1` (health/sync/dryrun/post/undo against the running API, `-ConfirmCopy` guard), checklist extended (G4 query answers, G5 echo, undo answers, closed QuickBooks, COM constants, bitness). Row set `ready-for-human`. **M6 done on Windows apart from the server check: Core 673, Api 133.** No owner answers yet (Q-1…Q-39). Linux test run still open |
| 2026-09-17 | 9 | T-701 | Owner answers checked: none yet (Q-1…Q-39). `POST /rules/alias` and `POST /rules/account` through `RulesEditor` (atomic rewrite under a lock, validation against `qb-lists.json`, rules re-read by the next job or re-post); `Rules.Load` reads share-safe → Core 699, Api 146. New gap Q-40 |
| 2026-09-17 | 9 (cont.) | T-702 | Serilog (console + daily rolling file under `Paths:Logs`), `jobId` on every line (worker scope, `{JobId}` in the message, else `-`), secret scrubbing wrapper sink for configured keys, secret labels and secret-named properties → Core 699, Api 170; checked by hand with `dotnet run`. Packages added: `Serilog.AspNetCore` 8.0.1, `Serilog.Sinks.File` 5.0.0 (allowed list). New gap Q-41 |
| 2026-09-17 | 9 (cont.) | T-703 | Backup guard (built in T-604) confirmed against FR-11; added tests for the configured limit, the exact limit, a zero limit, relative/absolute/blank `BackupFolder` resolution and the persisted `failed` status → Core 703, Api 177. No production change |
| 2026-09-17 | 9 (cont.) | T-704 | `GET /jobs?status=` (built in T-104) confirmed against §6; added tests for case, empty result, API key, the list after a restart (status.json scan, recovered status) and `undone` → Core 703, Api 183. No production change |
| 2026-09-17 | 9 (cont.) | T-705 | `docs/runbook.md` written (install, configure, first run, daily operation, hold codes, rules teaching, undo, troubleshooting, logs, state backup) and checked against the code and Q-rows. **M7 done on Windows: Core 703, Api 183.** No owner answers yet (Q-1…Q-41). Linux test run still open |
| 2026-09-17 | 10 | T-801 | Owner answers checked: none yet (Q-1…Q-41). `deploy/hermes/docker-compose.yml` (loopback-only 8642, required key/image/port/data path, optional git-ignored `provider.env`), `.env.example`/`provider.env.example` placeholders, `deploy/README-hermes.md`; `docker compose config` valid with the example env → Core 713, Api 183. New gap Q-42 |
| 2026-09-17 | 10 (cont.) | T-802 | `deploy/start-all.ps1` (Docker in WSL or Desktop → compose up → wait for the Hermes port → start the API once → wait for `/health/hermes`; exit 0/1/2; `-WhatIf`) and `deploy/install-task.ps1` (interactive logon task, limited rights, `-Unregister`, `-WhatIf`), runbook §2.4 synced; parse-checked and dry-run on the dev box → Core 726, Api 183. Set **ready-for-human** (server dry run, checklist added). New gap Q-43 |
| 2026-09-17 | 10 (cont.) | T-803 | Shadow week prepared: `scripts/shadow-diff.ps1` (read-only dry run vs manual CSV, exit 0/3/1) checked against the golden sample; T-803 checklist and shadow log, T-804 checklist and go-live log added → Core 730, Api 183. Set **ready-for-human** |
| 2026-09-17 | 10 (cont.) | T-804 | Go-live prepared: `GoLiveConfigApiTests` (shipped `DryRunDefault: true`, omitted `dryRun` follows the default, request `dryRun` wins), runbook §2.5, checklist and log → Core 730, Api 187 (2 consecutive green runs). Set **ready-for-human**. New gap Q-44 (several companies). **M8 agent work done**; T-609, T-802, T-803, T-804 wait for the server. No owner answers yet (Q-1…Q-44) |
| 2026-09-17 | 11 | Linux run, CI | No agent task left (tracker has no todo/doing rows). Linux test run done: `git archive HEAD` in `mcr.microsoft.com/dotnet/sdk:8.0` (SDK 8.0.425) → `dotnet build -warnaserror` 0 warnings, `dotnet test` Core 730 / Api 187 passed. Added `.github/workflows/ci.yml` (build + test on ubuntu-latest and windows-latest; runs once pushed). Remaining: server tasks (T-609, T-802…T-804), owner answers Q-1…Q-44, push |
| 2026-09-19 | 12 | T-609 | Owner confirmed QuickBooks Enterprise Solutions 24.0 (`QBW` in `C:\Program Files\Intuit\...`). Bitness first recorded as x64 from the install folder, then withdrawn: the owner saw "x86" reported. Checklist item reopened until the `qbw.exe` PE header is checked. No code change. T-609 stays **ready-for-human** |
| 2026-09-19 | 12 (cont.) | T-805 | Owner asked for logging of startup, API processing and posting to QuickBooks. Added startup summary (bitness, session, settings, files), request log, job step log, QuickBooks call log with SDK session steps, Hermes call log, endpoint/auth logging; no statement text at Information → Core 730, Api 206 (2 consecutive green runs). Runbook §8.6 lists what to look for |
| 2026-09-19 | 12 (cont.) | T-806 | Owner: POC with no AI, post sample data into QuickBooks Enterprise 24 (Batch Enter Transactions types). Added `Hermes:Enabled=false` (regex requirement reader, all model calls held), `samples/poc` (September job, rules, IIF lists, settings, README-POC.md), `scripts/build-poc-package.ps1`; package smoke-tested with simulated QuickBooks (15 posted, undo 15) → Core 732, Api 209. Set **ready-for-human**. New gap Q-45 |
| 2026-09-20 | 13 | T-609 / T-806 | **First real QuickBooks connection.** POC package on the owner's server (Windows Server 2019, session 9), company `Tropicana Properties LLC` under `D:\Accountexx Data\...\18_Takoma Park Grocery Store Inc\`. `GET /health/quickbooks` → `ok: true`, `"Intuit QuickBooks Enterprise Solutions 24.0 34.0"`. **Bitness x64 confirmed** (T-609 checklist). Two findings: (1) the exe reads `appsettings.json` from the working directory, so it must be started from the app folder — started elsewhere it had an empty `Company:Name` and Hermes enabled; (2) the certificate dialog held `BeginSession` for 101 s, past the 60 s busy timeout, so the first health call reported `quickbooks-busy` while the session itself succeeded; the wait for the gateway lock is not logged |
| 2026-09-21 | 13 (cont.) | T-806 | Second POC job added: `samples/poc/jobs/2026-08-tropicana-xlsx` (one XLSX bank statement, 9 rows, real date cells, balance chain verified) → posts 7 Checks + 2 Deposits, 0 held, 0 skipped, no model call (`PocModeApiTests`) → Core 732, Api 210. Package script falls back to a fresh staging folder when the old one is locked. `handoff.md` rewritten for session 14: the owner brings an API-conversion spec; the app is already an ASP.NET Core minimal API (10 routes, API key, health endpoints, Serilog file logging), so the handoff lists what exists, the two server defects (content root read from the working directory; gateway-lock wait not logged / first-call busy timeout) and the questions the spec must settle |
| 2026-09-22 | 14 | M9 planning (no code) | API conversion specified, **no code written**. Read the existing surface first (`Program.cs`, the 5 endpoint files, `QbHealth`, `QbConnection`, `FolderReader`) and confirmed the handoff's §6 claim: it is already an API, so M9 is a restructure. Four owner decisions taken (D-1…D-4, Decisions table); then the owner ruled out Swagger and chose **Scalar**, which also removed the OpenAPI *generator* (.NET 8 has none built in), so the document became hand-authored + drift-tested. Written: `docs/spec-api-v1.md` (15 sections, FR-A-1…FR-A-18, route table, request/response shapes, security, testing, config). Added here: M9 milestone row, 14 task rows T-901…T-914 (all `todo`), questions Q-46…Q-54, five decision rows; `docs/plan.md` gained the M9 section, the M9 risks, the v1 verification commands and the Scalar/Swashbuckle package note. Both server defects from session 13 are now tasks (T-901 content root, T-904 gateway-wait logging and first-call timeout). Build and tests untouched: still Core 732 / Api 210 from session 13 |
| 2026-09-22 | 14 (cont.) | T-901 | First M9 code, on branch `m9-api-v1`, test-first in two RED/GREEN cycles. (A) Routing: RED = `RouteVersioningTests` 7 of 10 failing (`/api/v1/*` 404, `Api:LegacyRoutes=false` disabling nothing); GREEN after `ApiRoutes` + `MapAll` into the `/api/v1` group and again flat + `ApiKeyMiddleware`/`RequestLog` asking `ApiRoutes.IsHealth` → Api 220. (B) RED = `LegacyRouteLog` CS0246, `ContentRoot` 4 of 13 against a null-returning skeleton, warning tests 0 log lines; GREEN after `ContentRoot.Select` (exe folder unless `Testing`), `LegacyRouteLog` (once per route per hour) and the legacy endpoint filter → **Core 732, Api 235, twice in a row, `-warnaserror` 0 warnings**. `Api:LegacyRoutes` added to `appsettings.json` and the startup summary. Evidence `docs/testing/T-901.tdd.md`. Two things worth carrying: the test namespace `…Tests.Endpoints` shadowed production `Endpoints.JobView` in 4 files and had to be renamed `…Tests.Routing`; and the content-root fix is proven in logic only — a published exe started from another folder is still a server check |
| 2026-09-22 | 14 (cont.) | T-902 | `GET /api/v1/health` + `/health/ready`, test-first again. (A) `Readiness.Evaluate` — RED was CS0234 (`QbAutopost.Api.Health` did not exist) → 7 unit tests green: settings/rules/logFolder/worker are errors, a missing `qb-lists.json` is a **warning** because a job every rule resolves still posts. (B) endpoints — RED 7/7 on 404, then 500. The tests found two real bugs: `ChannelReader.Count` throws `NotSupportedException` on a single-reader unbounded channel (`JobQueue.Depth` is now an `Interlocked` counter decremented as the worker takes an item), and camelCase renders `QuickBooksGateway` as `quickBooksGateway` while FR-A-1 says `quickbooksGateway` (pinned with `JsonPropertyName`; a record-level unit test would never have caught it). Health still needs no key and makes **zero** gateway/Hermes calls, asserted against the recording fakes. → **Core 732, Api 249, twice in a row, `-warnaserror` 0 warnings**. Evidence `docs/testing/T-902.tdd.md`, commit `957f896` |
| 2026-09-22 | 14 (cont.) | T-903 | `GET /api/v1/health/sdk` + the COM probe, **set ready-for-human** (CLAUDE.md: a [server] task is implemented against fakes, then a person verifies). RED was CS0246 ×4 for `IQbSdkProbe`/`QbSdkInfo`; then `CoreIsolationTests` failed because I had put the ProgId string `QBXMLRP2…` in Core — the rule-2 guard catching a real violation, fixed by carrying the ProgId as data from `QbSession.ProgId`. Added `QbSdkProbe` (own STA thread; registration → create → `OpenConnection2` → `CloseConnection`, **never `BeginSession`**, so it answers with QuickBooks closed), `PeBitness` (PE machine field → x64/x86/arm64, 11 unit tests, the T-609 comparison testable on any OS), `UnavailableQbSdkProbe`, and `FakeQbSdkProbe` wired into `ApiFactory` — without which the host would pick the real COM probe on a Windows dev box (rule 1). → **Core 732, Api 265, twice in a row, `-warnaserror` 0 warnings**, commit `a9cab4e`. **The COM path itself is unverified**: checklist added above. New gap Q-55 — FR-A-3 wants the supported qbXML versions, but the SDK only gives that list from a session ticket, which the same requirement forbids; the probe reports the configured version and claims nothing more. Evidence `docs/testing/T-903.tdd.md` |
| 2026-09-22 | 14 (cont.) | T-904 | `POST /api/v1/quickbooks/connection/test`, **closing server defect 2**. RED was CS0246 for `QbConnectionCheck`, then 404s on the endpoint. The check reports three timed steps — `waitForGateway`, `hostQuery`, `companyFile` — and a step past the busy timeout stays `ok:true` with a "slow: a QuickBooks dialog…" hint, so the 101 s `BeginSession` now reads as a slow success instead of `quickbooks-busy`. `ResilientQbGateway` gained a `Waited` event (a lock wait over 1 s logs `Waiting for QuickBooks: {WaitMs} ms behind another call` — the silence the handoff complained about) and read-only calls with their own timeout; `ProcessWithTimeoutAsync` **refuses non-read-only qbXML**, so FR-11's rule that a write keeps the busy timeout cannot be widened by accident. The gateway is now one instance registered twice (interface + concrete) — two registrations would have meant two locks and overlapping sessions. Both refusals (company-file override Q-50, timeout above the cap) happen **before** any gateway call, asserted by an empty `Gateway.Requests`. → **Core 732, Api 276, twice in a row, `-warnaserror` 0 warnings**, commit `ee1b551`. **Spec amended**: FR-A-4 listed six steps, but one gateway call is one SDK session, so `openConnection`/`beginSession`/`endSession` cannot be timed separately without widening `IQbGateway`; the example now matches what is measured (new gap Q-56). Evidence `docs/testing/T-904.tdd.md` |
| 2026-09-22 | 14 (cont.) | T-905 | `POST /api/v1/quickbooks/company-file/validate`. RED was CS0246 ×3 for `CompanyFileValidator`, then endpoint 404s. Seven checks: configured, pathShape, exists, readable, **openInQuickBooks**, backupFreshness, listsSynced. Two deliberate severity calls: a different company file open in QuickBooks is an **error** (it is the one misconfiguration that silently posts into the wrong books), while a stale backup is a **warning** that reuses FR-11's own `BackupGuard` — one implementation, so validating and posting can never disagree, and this call cannot become what blesses a stale backup. Read-only is enforced twice: the unit stubs **throw** on `ProcessAsync`, and the host test asserts `Gateway.Requests` stays empty while `CompanyFileCalls` rises. A third RED came from my own wrong assumption — the test host inherits `Company:FilePath` from the shipped `appsettings.json`, so "nothing configured" was not the default; the test was corrected, not the code. → **Core 732, Api 292, twice in a row, `-warnaserror` 0 warnings**, commit `1bb151f`. **FR-A-5's `companyName` check was not implemented**: it needs a `CompanyQueryRq` whose response element names cannot be verified here, and guessing them would break rule 4 (new gap Q-57). Evidence `docs/testing/T-905.tdd.md` |
| 2026-09-22 | 14 (cont.) | T-906 | Direct-post request model in Core — the first piece of the JSON path (D-1). RED was CS0234/CS0246 ×4. `DirectRequestReader` turns rows into the **same `StatementLine`s** a statement produces, so the FR-6 mapper, G4, qbXML and G5 are shared rather than reimplemented; `SourceKind`/`Direction` are derived from `kind` and never taken from the caller, so two callers describing one movement fingerprint alike. The refuse-vs-hold split is the heart of it: a control-total mismatch (or missing total, no rows, too many rows, a non-positive or over-precise amount, an unsafe reference) **refuses the whole batch and reads out no lines** — rule 3, no amount is ever adjusted to make a total fit — while an over-cap amount, an out-of-window date or a missing account **holds just that row**, so one bad line cannot fail a batch of hundreds. The control total is checked over every submitted row **including held ones**: it describes what the caller sent, not what survived. `DirectAndFolderPathsAgreeTests` pins the property the two-entrance design rests on — same fingerprint and byte-identical qbXML from either path — and passed on its first run (an invariant over existing code, so no RED; non-vacuity shown by the checkNo test). → **Core 757, Api 292, twice in a row, `-warnaserror` 0 warnings**, commit `6d4505f`. Evidence `docs/testing/T-906.tdd.md` |
| 2026-09-22 | 15 | T-907 | `POST /api/v1/quickbooks/transactions/validate` — the offline dry run, in two RED/GREEN cycles. (A) `DirectPlanner` in Core: RED was `CS0246 DirectPlan` → 17 unit tests green. (B) the endpoint: RED was **11 of 12 failing on 404** → 12 green. A third RED came from my own test, not the code: I had faked a ledger fingerprint as `requestId + 48 zeros` while G4 compares the whole SHA-256, so the duplicate test said `OK` instead of `422`; computing it with `Fingerprints.Compute` fixed the test *and* made it an independent check of the spec §7 formula rather than an echo of the API. The design decision worth carrying: the caller's `account` is injected into a **scoped copy of the rules** per (kind, last4) group, so the FR-6 `Mapper` decides exactly as it would for a statement — post-processing its output instead would have lost the payee and tier resolution, because the mapper stops as soon as it holds on `unknown-account`. Two rows sending one last four as two different accounts hold each other (`conflicting-last4`). Status split follows §6.1/§2: a control-total mismatch is **400** (the request contradicts itself), a gate verdict is **422 with the same body as the 200**. `skipped` counts duplicates too, so `wouldPost + held + skipped = submitted` and a caller can check the arithmetic of the answer as well as of the request. Offline is asserted, not assumed: `Gateway.Requests` and `Hermes.Calls` both stay empty. **The qbXML body is withheld even when `?includeQbXml=true` asks for it** — FR-A-6 gates it on a `qb:debug` scope that does not exist until T-910, and handing it to every holder of the shared key is the less careful reading (rule 4); the response says so. Honest limit recorded in the evidence: tiers 3–4 do not run offline, so a plan can be gloomier than the post, and a clean validate is still not a guaranteed post (G4's QuickBooks half, the backup guard and G5 remain). → **Core 774, Api 304, twice in a row, `-warnaserror` 0 warnings**. New gap Q-58 — the caller's payee is honoured on a numbered check, where FR-6 leaves it blank. Evidence `docs/testing/T-907.tdd.md` |
| 2026-09-22 | 15 (cont.) | T-908 | `POST /api/v1/quickbooks/transactions` — the direct post, the last of the two pieces that carry money. RED in two cycles: `CS0246` for `ApiBatchStore`/`DirectPostResult`, then **15 of 16 endpoint tests failing on 404**. The design question the handoff flagged — "the batch id feeds the ledger and undo, which today assume a job id per folder" — resolved by **not** inventing a parallel store: `ApiBatchStore` holds evidence only (`request.json`, `status.json`, `request.qbxml`, `response.qbxml`, `result.json` under `Paths:ApiBatches/<batchId>/`), while the `LedgerJob`/`LedgerEntry` rows are written exactly as a folder job writes them. The payoff is that `BatchUndo` and the undo endpoint needed **no change whatsoever** and FR-13 works on a batch that never had a folder — asserted, not assumed. Rule 7 is proven by a test in which QuickBooks throws and `request.json` is on disk regardless: nothing after the send could have written it. A refused request gets no batch id and no folder, so a malformed call leaves nothing undoable-looking behind. §2's 503 is decided by an explicit `Unavailable` field rather than by matching error text, and the body is still the batch so the caller learns its id. The 202 path adds no machinery: `RunExclusiveAsync` already runs work to the end when the caller stops waiting, so the timeout bounds only the wait, and `Prefer: respond-async` is that same path with a zero budget. **Two real defects the tests caught.** The traversal guard used `Path.GetInvalidFileNameChars()`, which on Linux permits `\` and `:` — `C:\windows\system32` would have sailed through and CI would have failed (rule 1); it is now an explicit ASCII allow-list. And adding `Paths:ApiBatches` to the shipped `appsettings.json` made 5 tests write batch evidence to the **real machine folder** `C:\qb-autopost\api-batches` (handoff trap #4 exactly), creating 32 folders before it was noticed; `ApiFactory` now pins the path as it pins every other, the debris was deleted, and a full run afterwards leaves that folder with 0 entries. → **Core 787, Api 320, twice in a row, `-warnaserror` 0 warnings**. New gap Q-59 — FR-A-8's "post a `ready` batch later" route is in no route table and was not built; re-sending with `dryRun:false` re-runs every gate against the rules as they are now, which is the safer of the two. Honest limits in the evidence: no real QuickBooks has seen any of it, evidence is never aged out (Q-52), and two concurrent posts of one reference could still pick the same attempt number — T-909's idempotency store is where that claim belongs. Evidence `docs/testing/T-908.tdd.md` |
| 2026-09-23 | 15 (cont.) | T-909 | `Idempotency-Key` (FR-A-12), in two RED/GREEN cycles: `CS0246` for `IdempotencyStore`, then **7 of 10 host tests failing**. The behaviour that matters is one assertion — the same post sent twice under one key yields one batch, one TxnID, `Idempotency-Replayed: true`, and **exactly one write reaching QuickBooks**. Undo is covered too (two undos, one delete), while **validate deliberately is not**: replaying a validate would hide a change in the rules or the ledger, which is precisely what a validate exists to reveal. Keys are scoped by (key, route, client) so a caller numbering their requests cannot make an undo collide with a post; `clientId` is already part of the key and stays null until T-910, so adding clients later changes no behaviour. Two judgement calls are recorded rather than buried: **a non-2xx releases the key** (Q-60) — holding it would lock a caller out of their own key over a typo, and would block the very retry a 503 invites, which is safe only because idempotency is the convenience layer while G4 and the ledger are the safety layer; and **a replay keeps its original status code** instead of §6.5's blanket 200, since replaying a 202 as 200 would tell a caller their batch had finished when it had not. The middleware sits behind `ApiKeyMiddleware`, so an unauthenticated caller can neither claim a key nor learn from a 409 which keys are in use. Of the four failures after the middleware landed, **three were my tests, not the code**: an empty header cannot be told from an absent one over HTTP, a fixture nested a whole request where a row belonged (the framework rejected it with an empty body before the endpoint ran), and one test read the response body twice, which surfaced as `ObjectDisposedException` rather than a wrong status. The in-progress test also pins a 2 s synchronous budget, so a middleware that stops short-circuiting fails in seconds instead of hanging for two minutes behind a wedged request — which is exactly what the RED run did. → **Core 799, Api 329, twice in a row, `-warnaserror` 0 warnings**, and `C:\qb-autopost` still holds 0 entries after a full run. Evidence `docs/testing/T-909.tdd.md` |
| 2026-09-23 | 15 (cont.) | T-910 | Per-caller keys and scopes (FR-A-13). RED in two cycles: `CS0234` for the new `QbAutopost.Core.Security` namespace, then **8 of 12** scope tests failing. Only a salted hash is stored, asserted by grepping the written file for the plaintext; a read-only client posting gets **403** naming the scope, an unknown key 401, and a revoked client 401 on its **next** request because the list is re-read per request rather than cached to the next restart. Rotation keeps the retiring key working inside its own window. Scopes map **by effect, not by URL**, and an unmapped route falls through to `admin`, so a new endpoint is closed until someone decides otherwise. Gating `?includeQbXml=true` on `qb:debug` **closes the SPEC-GAP T-907 recorded**. Startup now refuses when no enabled client and no usable shared key exist - an empty allow-list answering 401 for ever is a worse failure than one that says so once at boot - and warns whenever the all-scopes legacy key is accepted alongside real clients, since while it is on it outranks every scope decision in `clients.json`. `scripts/new-api-client.ps1` prints the key once and writes only the hash; because it reimplements that hash in PowerShell, a recorded (key, salt, hash) triple from an actual run is asserted against `ApiClientStore.Matches` - without it, a divergence would silently reject every key the script issues, which is the kind of failure discovered at 2am. **A real behaviour change, deliberately taken**: `docs/spec.md` §6 leaves all of `/health/*` open, api-v1 §2.2 opens only `/health` and `/health/ready`, and the narrower rule won on both surfaces because `/health/quickbooks` opens a QuickBooks session and `/health/sdk` probes COM - open to unauthenticated remote callers (D-4), either lets a stranger hammer QuickBooks. That moved **18 existing tests**: some re-pointed at the routes that genuinely need no key, some inverted with the reason written in, some simply given a key, and `LoggingApiTests` updated because a key belonging to no client is now "unknown" rather than "invalid". None was changed merely to silence a failure; §5 of the evidence lists every one. New gap **Q-61** - the owner's monitoring must be told before it starts getting 401. → **Core 820, Api 344, twice in a row, `-warnaserror` 0 warnings**. Evidence `docs/testing/T-910.tdd.md` |
| 2026-09-23 | 16 | T-911 | Transport, rate limiting and input hardening (FR-A-14, FR-A-15, FR-A-17) in three RED/GREEN cycles: **26** compile errors for `TransportGuard` and the new settings, **22** for `RateLimitBucket`/`AuthBrake`, then `CS0103 PathAllowList` x12 in Core and — the useful RED — **7 of 12 host tests failing** on behaviour. The result worth stating first: **no existing test was changed or deleted**. That was not luck; it is the consequence of two deliberate defaults. The limiter ships **on** (10 post / 120 default / 600 health a minute) but is turned **off in `ApiFactory`**, because the suite sends far more than 120 requests a minute to these same routes (trap 14) — so the shipped numbers are asserted on the settings object and the *mechanism* is asserted against tiny limits a test sets itself, which is honestly less than watching a real minute throttle a real caller, and §8 of the evidence says so. And the path allow-list ships **empty**, which forced the task's one real spec conflict: FR-A-17 requires caller paths to sit inside `Paths:AllowedJobRoots` while §12 ships that list empty. Enforcing it literally refuses every job and nothing starts; ignoring it is exactly the permissive reading rule 4 forbids now that D-4 makes callers remote. The resolution keeps both halves true — empty means *unconfigured*, unrestricted while the bind is loopback, and **startup refuses a non-loopback bind while the list is empty**, the same shape as FR-A-14's TLS refusal. Today's deployment is untouched; the moment the owner exposes the port they must say where jobs live (**Q-63**). Three smaller judgement calls are recorded rather than buried: `POST /jobs` draws on the **posting** bucket though its scope is `jobs:write`, because it reaches QuickBooks more slowly but not less certainly, and the cheaper bucket is the one that would have to be defended later; the brute-force brake counts **unrecognised keys only**, so a client polling one route it lacks the scope for cannot accumulate its way into a five-minute block of its own API; and an over-long field is a **400 for the whole batch** rather than a held row, because a 4 KB memo is a broken mapping, unlike `amount-over-limit` where the caller meant precisely what they said. `Api:Tls:PfxPassword` was deliberately left **out** of `appsettings.json` although §12 lists it — shipping the key invites someone to fill it in — and the guard warns, naming the file and never the value, if it is found there anyway. The startup rules live in `TransportGuard` as pure functions rather than inline in `Program.cs`, because a guard nobody can exercise is a guard nobody trusts; the same reason `PathAllowList` is Core with 14 tests built from `Path.Combine` under a temp root, so it means the same thing on Linux (trap 11, which is how T-908's batch-id guard shipped broken). -> **Core 834, Api 405, twice in a row, `-warnaserror` 0 warnings**, and `C:\qb-autopost` still holds 0 entries. **One run reported `Failed: 1, Passed: 404`** right after the `appsettings.json` edit; the name scrolled, it did not recur in 11 further full runs, and I could not reproduce it — logged as **Q-62** rather than written off. Evidence `docs/testing/T-911.tdd.md`, whose §8 lists what has never run: no TLS handshake, no HSTS header, no `AddServerHeader` on a real socket, no production rate limit — every one of those needs the server |
| 2026-09-23 | 17 | T-912 | Audit trail and correlation ids (FR-A-16, api-v1 §2.6/§8). RED first: `CS0246 AuditLog`/`AuditEntry` - and worth noting that Roslyn reported **only** those two, because they sit in method signatures and a declaration-phase error stops body binding, so the other two test files' errors were never printed. The one behavioural RED was **my test's** fault, not the code's: it switched `Api:AllowLegacyKey` off without issuing a client, and T-910's fail-closed startup refused to start the host. The placement decision is the whole task: the audit middleware sits **in front of** `ApiKeyMiddleware` and records after the pipeline returns, because the key check answers `403` by returning early (handoff trap 17) - so a caller refused for a missing scope, and a caller refused by the rate limiter, both leave a line, while an **unknown key leaves none** (Q-64: the FR says *authenticated*, and a stranger must not be able to grow the money evidence). The line carries only `batchId`, `jobId`, `counts` and `totals.posted` out of the response body; a test greps the whole file for the key, the memo, the payee and the account name and finds none of them. `Api:AuditRetentionDays` <= 0 keeps every file rather than deleting them all. `requestId` is a 26-character ULID (ordinal sort = time sort), honoured inbound only against §2.6's pattern, echoed through `OnStarting` so an early refusal carries it, and present in every problem detail; the log template gains two columns beside `[{jobId}]`. **Q-62 is answered**: the unexplained single failure is teardown, not the application - `TempDir.Dispose` deleting a folder while Serilog still holds the log file, measured at 0/3 without this task, 1/3 with it, 0/6 after the delete was made to retry. Core 834 (unchanged), Api 405 -> 447, 2 consecutive green runs; `C:\qb-autopost` still empty. New gaps Q-64, Q-65 |
| 2026-09-23 | 18 | T-913 | OpenAPI document + Scalar reference UI (FR-A-18, api-v1 §11.6). RED first: `CS0246 Scalar` / `CS0117 ApiRoutes.OpenApiPath` x3, then a second compile RED from `-warnaserror` turning Scalar's `CdnUrl` deprecation into a stop (`BundleUrl` is the one it kept). The behavioural RED was **14 of 23** new tests on `401` — my tests had believed api-v1 §2.2 and sent `Authorization: Bearer`, which `ApiKeyMiddleware` does not read at all (**Q-67**); the document now declares both schemes and offers `apiKeyAuth`, with the header name pinned to `ApiSettings.KeyHeader` by a test. A third RED came from the page naming the document **relatively** (`api/v1/openapi.json`); rather than guess, I fetched Scalar's own loader from the host under test and found `getBasePath` strips the reference prefix off `location.pathname`, so it resolves against the app root — the code was right, the assertion was wrong, and the test now reproduces that resolution. **No existing test was touched**; the only two test files in the commit are new. Document is hand-authored (20 operations) and kept honest by a drift test over `EndpointDataSource` where `x-required-scope` must **equal** `ApiRoutes.ScopeFor`. Disabled means not mapped (404). Package added: `Scalar.AspNetCore` 2.13.13 (Q-46). New gaps Q-66 (scope in every environment, not only Production) and Q-67 → Core 834, Api 447 → **472**, 2 consecutive green runs, `ls -A /c/qb-autopost` empty |
| 2026-09-23 | 19 | T-914 | The last M9 task: the two routes api-v1 §3 names and nobody had built, plus the documentation catch-up. RED in four cycles: `CS0246 FolderValidator`; **11 of 15** host tests on 404/405 (`/jobs/validate` answered **405**, not 404, because `GET /jobs/{id}` already matched the shape - the four that passed were the 401/403 cases, since T-910's key check runs in front of routing); `OpenApiDriftTests` on the three undocumented routes (trap 22 working as designed, and the routes were **not** excluded from it); then **15 of 19** `OperatorDocsTests`. **The documentation RED found two real defects, not typos.** `deploy/start-all.ps1` waited on `GET /health/hermes` and `scripts/qb-server-check.ps1 -Step health` called `GET /health/quickbooks`, both without a key, both with a comment stating none was needed - and both have needed `health:read` since session 15 (Q-61). The logon script would have logged "not healthy" for its full 180 s budget on every boot and exited 2 with the API perfectly fine; the T-609 server checklist would have stopped at step 1 with a 401. start-all cannot be given a key - `DeployScriptsTests.Should_NotTouchSecrets_When_ReadingDeployScript` forbids the string `ApiKey` anywhere in it, deliberately, because it runs unattended at logon - so it now waits on the key-free `/api/v1/health/ready`, and Hermes stays proved by `Wait-HermesPort` on 127.0.0.1:8642 before the API starts; a host that never becomes ready is exit **1** ("the API could not be started"), which is what that actually is. `qb-server-check.ps1` goes through `Invoke-Api`, which always sends the key. `OperatorDocsTests` is the guard that keeps this from drifting back: across five operator files every route named must carry `/api/v1`, none may promise a key-free health check, and seven M9 subjects (the key issuer, `Api:AllowLegacyKey`, the audit file, `Api:Reference:Enabled`, `Retry-After`, `Paths:AllowedJobRoots`, `qb:post`) must appear in the runbook. **No existing test was changed, weakened or deleted.** `FolderReader.Read` gained an optional `createOutput` so every caller is untouched, and `JobPipeline`'s G1 block moved **verbatim** into `StatementCheck.Reconcile` - which is the point: FR-A-7 is only worth having if its verdict is the job's verdict, and two copies of "which reconcile check applies" would eventually disagree, quietly, in the direction of telling an operator a statement was fine. Judgement calls recorded rather than buried: the validate answers **422** where `POST /jobs` answers 400 for the same folder (**Q-68** - FR-A-7 names only 200/422, and a validator with a body only on success is useless); `Paths:AllowedJobRoots` applies to the validate although FR-A-7 never mentions it, because without it the route answers "requirement.txt is missing" for any path on the server, which is a directory probe with a friendly name; `POST /api/v1/quickbooks/lists/sync` was added **beside** `/qb/sync-lists` rather than replacing it, the old one marked `deprecated` in the document (**Q-69**); and a missing `qb-lists.json` is 200 with empty lists and `syncedUtc: null`, agreeing with T-902's readiness warning instead of inventing a second opinion. `Api:LegacyRoutes` left **true** (Q-53). -> **Core 834 -> 859, Api 472 -> 487, twice in a row, `-warnaserror` 0 warnings**, `ls -A /c/qb-autopost` empty, `%TEMP%\qbautopost-validate` gone after the run. New gaps Q-68, Q-69, Q-70. Evidence `docs/testing/T-914.tdd.md`, whose §9 is honest about what is untested: no PDF statement has ever gone through the validate, neither PowerShell script has been executed, the POC package has not been rebuilt, and `OperatorDocsTests` checks that the documents say the right words, never that the sentences around them are true |
| 2026-09-23 | 20 | M9 final report (no code) | **M9 closed.** Wrote `docs/M9-COMPLETE.md`: (1) every `/api/v1` route with what it does, the FR-A it satisfies and the task that built it, plus the eight middlewares that now sit in front of them and the three startup refusals; (2) the full Questions list — 68 still open, with the conservative behaviour each one has **today**, and Q-0, Q-46, Q-62 marked answered rather than listed as open; (3) one consolidated "needs the owner or the server" section: T-903's 8-item SDK checklist, T-609, T-802, T-803, T-804, T-806, the six owner decisions that must land before deployment, a **new 4-step direct-post server checklist** (dry run → one real batch, checked by hand in QuickBooks → undo it → the failure paths) and the TLS/transport verification T-911 could not do; (4) the honest list of what has never met a real QuickBooks, which is all of T-907…T-914 plus T-903's COM probe. Two authorised factual corrections: `docs/plan.md` line 50 no longer says `Scalar.AspNetCore` is "pending Q-46" (answered yes 2026-09-23; the package is referenced in `QbAutopost.Api.csproj` at 2.13.13), and `docs/CLAUDE.md`'s package line now matches the root `CLAUDE.md`. **No production code and no test was touched**, so the verification is a regression check: `dotnet build -warnaserror` → **0 warnings, 0 errors**; `dotnet test` → **Core 859 passed / 0 failed, Api 487 passed / 0 failed (1346)**. Three contradictions found while writing and recorded rather than fixed (fixing is a task, not this one): `docs/plan.md` **line 144** still says the Scalar UI is "pending Q-46" — outside the one sentence this task was authorised to touch; the M9 milestone row says "T-901, T-902, T-904…T-914 done" while T-903 is `ready-for-human`, which is right but reads as if 14/14 were `done`; and FR-A-8's `allowModelAccounts` / the `qb:post:ai` scope are carried through the request model and the scope table but **do nothing at all** (Q-51), which no single tracker row states outright. Remaining work is the §10 server list and needs a person |
| 2026-09-23 | 21 | T-915 | **The silent no-op**, found while writing the M9 report and fixed on the owner's say-so after the milestone had closed. `allowModelAccounts` had shipped through all of M9 declared on `DirectRequest`, described in the hand-authored `openapi.json` as "let the model choose an account for a row no rule resolves", specified in api-v1 §6.1, and given a scope of its very own - while being **read by no line of production code**. `qb:post:ai` is checked nowhere either. The failure mode is the expensive kind: a caller sets the flag, is granted the scope, and watches every row no rule resolves come back held as `unknown-account`, with nothing in the response, the log or the audit file to suggest the model was never consulted. It looks exactly like a data problem at their end. T-910's evidence had noted in passing that the scope "has no effect yet"; no tracker row ever said the feature was unimplemented, and the document a caller actually reads said it worked. RED first: Core 2 of 24, Api 3 of 5 - the 2 Api tests that passed are the ones pinning the untouched default, deliberately written so the change is recorded as a change to what is *refused*. Three judgement calls worth keeping: **refuse rather than accept-with-a-note**, because a note is only read by a caller who is already suspicious and the flag asks for something the server simply cannot do; **refuse in `DirectRequestReader`**, not in either endpoint, because both already render its `Errors` as a 400 and a validate that accepted what the post refused is the precise bug that makes a caller trust a dry run they should not; and **do not implement it**, because Q-51 is unanswered and building it unattended would put a model in charge of which account money lands in (rules 3 and 4). `docs/spec-api-v1.md` §6.1 is now knowingly out of step with the code and was left that way - an agent does not rewrite the contract to match what it built - recorded as **Q-71**. -> **Core 862, Api 492, twice in a row, `-warnaserror` 0 warnings**, `C:\qb-autopost` 0 entries, and T-913's OpenAPI drift test still green against the corrected description. Evidence `docs/testing/T-915.tdd.md`, whose §5 admits the gap that matters: **nothing detects the next no-op of this kind** - no test asserts that every field on `DirectRequest` and every scope in `ApiScopes.All` is read somewhere in production, and this one survived eight tasks and a drift-tested API document |
